---
phase: 08-safety-transparency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/page-client.tsx
  - src/components/dashboard/nav-sidebar.tsx
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "Admin can navigate to Safety Rules from the sidebar navigation"
    - "Dashboard home quick link for Safety Rules no longer shows 'Coming soon' badge"
    - "Clicking Safety Rules quick link navigates to /safety-rules"
  artifacts:
    - path: "src/components/dashboard/nav-sidebar.tsx"
      provides: "Safety Rules nav item in sidebar"
      contains: "safety-rules"
    - path: "src/app/(dashboard)/page-client.tsx"
      provides: "QUICK_LINKS with comingSoon removed from Safety Rules entry"
  key_links:
    - from: "src/components/dashboard/nav-sidebar.tsx"
      to: "/safety-rules"
      via: "nav link href"
      pattern: "href.*safety-rules"
    - from: "src/app/(dashboard)/page-client.tsx"
      to: "/safety-rules"
      via: "QUICK_LINKS array entry"
      pattern: "href.*safety-rules"
---

<objective>
Wire up navigation to the Safety Rules page: add it to the sidebar nav and remove the "Coming soon" badge from the dashboard quick links.

Purpose: Parents need to discover the Safety Rules page through normal navigation paths, not just direct URL.
Output: Sidebar shows Safety Rules link; dashboard quick link is active without "Coming soon" badge.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/components/dashboard/nav-sidebar.tsx:
```typescript
const activeNavItems = [
  { href: "/", label: "Dashboard", icon: Shield },
  { href: "/conversations", label: "Conversations", icon: MessageSquare },
  { href: "/users", label: "Users", icon: Users },
  { href: "/analytics", label: "Analytics", icon: BarChart3 },
  { href: "/alerts", label: "Safety Alerts", icon: AlertTriangle },
];
```

From src/app/(dashboard)/page-client.tsx:
```typescript
export const QUICK_LINKS: QuickLink[] = [
  {
    href: "/safety-rules",
    label: "Safety Rules",
    description: "View content boundaries and system prompt",
    icon: Shield,
    comingSoon: true,  // <-- remove this
  },
  // ... other links
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Safety Rules to sidebar nav and activate quick link</name>
  <files>src/components/dashboard/nav-sidebar.tsx, src/app/(dashboard)/page-client.tsx</files>
  <action>
**nav-sidebar.tsx:**
Add a "Safety Rules" entry to the `activeNavItems` array. Place it after "Safety Alerts" (last position) or just before it — logically grouping safety items together. Use the `BookOpen` icon from lucide-react (to differentiate from the Shield icon used for Dashboard). Import BookOpen at the top.

```typescript
{ href: "/safety-rules", label: "Safety Rules", icon: BookOpen },
```

**page-client.tsx:**
In the `QUICK_LINKS` array, find the Safety Rules entry and remove the `comingSoon: true` property. The entry should become:

```typescript
{
  href: "/safety-rules",
  label: "Safety Rules",
  description: "View content boundaries and system prompt",
  icon: Shield,
},
```

Do NOT remove the comingSoon from the Test Mode entry — that is Phase 9 work.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Sidebar navigation includes "Safety Rules" link pointing to /safety-rules
- Dashboard home quick links grid shows Safety Rules without "Coming soon" badge
- Test Mode quick link still shows "Coming soon" badge (unchanged)
- Build passes with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx next build` passes
- Sidebar has 6 nav items including "Safety Rules"
- Quick links grid: Safety Rules is active (no badge), Test Mode still has "Coming soon"
</verification>

<success_criteria>
Safety Rules page is discoverable from both the sidebar navigation and the dashboard home quick links. No "Coming soon" badge on Safety Rules. Test Mode remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08-safety-transparency/08-02-SUMMARY.md`
</output>
