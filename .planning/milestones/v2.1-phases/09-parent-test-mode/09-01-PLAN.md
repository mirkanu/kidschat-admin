---
phase: 09-parent-test-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/system-prompt.ts
  - src/app/(dashboard)/safety-rules/page.tsx
  - src/app/api/test-chat/route.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "POST /api/test-chat accepts a messages array and returns a streamed AI response using the safety system prompt"
    - "The system prompt is defined in one shared module, not duplicated"
  artifacts:
    - path: "src/lib/system-prompt.ts"
      provides: "Shared SYSTEM_PROMPT constant"
      exports: ["SYSTEM_PROMPT"]
    - path: "src/app/api/test-chat/route.ts"
      provides: "POST endpoint calling Claude Haiku with safety prompt"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/test-chat/route.ts"
      to: "src/lib/system-prompt.ts"
      via: "import SYSTEM_PROMPT"
      pattern: "import.*SYSTEM_PROMPT.*from.*system-prompt"
    - from: "src/app/api/test-chat/route.ts"
      to: "@anthropic-ai/sdk"
      via: "Anthropic client"
      pattern: "new Anthropic"
---

<objective>
Create the backend for Parent Test Mode: extract the safety system prompt to a shared module and build the /api/test-chat API route that calls Claude Haiku 4.5 with that prompt.

Purpose: The chat sandbox needs a backend that applies the SAME safety system prompt used in production. Extracting the prompt to a shared module ensures consistency between the Safety Rules display page and the test chat endpoint.
Output: A working POST /api/test-chat endpoint that accepts messages and returns AI responses.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/safety-patterns.ts
@src/app/api/alerts/route.ts
@src/app/(dashboard)/safety-rules/page.tsx

<interfaces>
<!-- Auth pattern used by all API routes -->
From src/app/api/alerts/route.ts:
```typescript
import { auth } from "@/auth";
// ...
const session = await auth();
if (!session) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}
```

<!-- Safety detection function for Plan 02 to use on responses -->
From src/lib/safety-patterns.ts:
```typescript
export function detectSafetyEvent(
  text: string,
  isCreatedByUser: boolean
): {
  detected: boolean;
  type: "safety_redirect" | "jailbreak_attempt" | null;
  matchedPattern: string | null;
}
```

<!-- Current SYSTEM_PROMPT location (will be extracted) -->
From src/app/(dashboard)/safety-rules/page.tsx:
```typescript
const SYSTEM_PROMPT = `You are a safe, helpful AI assistant...`;
// This is currently a local const — will be moved to src/lib/system-prompt.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SYSTEM_PROMPT to shared module</name>
  <files>src/lib/system-prompt.ts, src/app/(dashboard)/safety-rules/page.tsx</files>
  <action>
1. Create `src/lib/system-prompt.ts` — move the entire `SYSTEM_PROMPT` string constant from `safety-rules/page.tsx` into this file as a named export: `export const SYSTEM_PROMPT = ...`
2. Update `src/app/(dashboard)/safety-rules/page.tsx` — replace the local `SYSTEM_PROMPT` const with `import { SYSTEM_PROMPT } from "@/lib/system-prompt"`. Remove the old inline string. Everything else stays the same.
3. Verify the Safety Rules page still renders correctly (the import works, no broken references).
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>SYSTEM_PROMPT exported from src/lib/system-prompt.ts, safety-rules/page.tsx imports it, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Install Anthropic SDK and create /api/test-chat route</name>
  <files>src/app/api/test-chat/route.ts</files>
  <action>
1. Install `@anthropic-ai/sdk` via `npm install @anthropic-ai/sdk`
2. Create `src/app/api/test-chat/route.ts` with a POST handler:
   - Auth check: `const session = await auth(); if (!session) return 401`
   - Parse request body: `{ messages: Array<{ role: "user" | "assistant"; content: string }> }`
   - Validate: messages array must be non-empty, last message must be role "user"
   - Create Anthropic client: `new Anthropic()` (reads ANTHROPIC_API_KEY from env automatically)
   - Call `client.messages.create({ model: "claude-haiku-4-5-20250414", max_tokens: 1024, system: SYSTEM_PROMPT, messages })` — use non-streaming for simplicity
   - Return JSON response: `{ role: "assistant", content: response.content[0].text }`
   - Wrap in try/catch, return 500 with error message on failure
   - Import SYSTEM_PROMPT from `@/lib/system-prompt`
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>POST /api/test-chat exists, compiles, uses auth gate, calls Claude Haiku with shared SYSTEM_PROMPT</done>
</task>

</tasks>

<verification>
- `src/lib/system-prompt.ts` exists and exports SYSTEM_PROMPT
- `src/app/(dashboard)/safety-rules/page.tsx` imports from shared module (no local SYSTEM_PROMPT const)
- `src/app/api/test-chat/route.ts` exists with POST handler, auth check, Anthropic SDK call
- `@anthropic-ai/sdk` in package.json dependencies
- `npx next build` passes
</verification>

<success_criteria>
- Build succeeds with zero TypeScript errors
- SYSTEM_PROMPT is defined in exactly one place (src/lib/system-prompt.ts)
- API route uses auth gate pattern consistent with other routes
- API route calls claude-haiku-4-5 model with the safety system prompt
</success_criteria>

<output>
After completion, create `.planning/phases/09-parent-test-mode/09-01-SUMMARY.md`
</output>
