---
phase: 260417-cs0-preset-aware-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-friendly-tutor.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-casual-buddy.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-balanced-helper.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-standard-formal.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-drawing-studio.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-image-generator-deleted.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-friendly-tutor.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-casual-buddy.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-balanced-helper.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-standard-formal.json
  - .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-drawing-studio.json
  - (MongoDB) agents collection — instructions field on 5 agents, 1 agent deleted
  - (MongoDB) aclentries collection — any bindings to agent_F6ITBo7EuorE7vqrXsNAm removed
autonomous: false
requirements:
  - CS0-GUIDANCE-TEXT-AGENTS (4 text presets redirect drawing requests to Drawing Studio)
  - CS0-GUIDANCE-DRAWING-STUDIO (Drawing Studio refuses off-topic while preserving clarification flow)
  - CS0-CLEANUP-IMAGE-GENERATOR (stray Image Generator agent removed from MongoDB + ACLs)

must_haves:
  truths:
    - "When a child asks a text preset to draw, they receive a friendly redirect pointing to Drawing Studio — no image is generated and no DALL-E token cost is incurred."
    - "When a child asks Drawing Studio an off-topic question (e.g. math homework), they receive a gentle refusal naming all 4 chat presets — no image is generated."
    - "When a child sends a drawing request in Drawing Studio, the clarification-first flow still works (agent asks 'what kind?' and generates on the follow-up reply)."
    - "The stray Image Generator agent (agent_F6ITBo7EuorE7vqrXsNAm) is no longer reachable — it does not exist in the MongoDB agents collection, and no aclentries reference it."
    - "The guidance-injection script is idempotent — running it twice does not duplicate the guidance block."
    - "Rollback is possible — before-snapshots exist in the task directory for all 5 modified agents and the 1 deleted agent."
  artifacts:
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs"
      provides: "Idempotent MongoDB update script that injects DRAWING REQUESTS guidance into 4 text agents and STAY ON TOPIC guidance into Drawing Studio; writes before/after JSON snapshots."
      contains: "KIDCHAT_PRESET_GUIDANCE_v1"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs"
      provides: "Deletes agent_F6ITBo7EuorE7vqrXsNAm from agents collection + removes any aclentries bindings; writes before-snapshot of deleted doc."
      contains: "agent_F6ITBo7EuorE7vqrXsNAm"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-friendly-tutor.json"
      provides: "Pre-edit instructions field for Friendly Tutor (rollback)"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-casual-buddy.json"
      provides: "Pre-edit instructions field for Casual Buddy (rollback)"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-balanced-helper.json"
      provides: "Pre-edit instructions field for Balanced Helper (rollback)"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-standard-formal.json"
      provides: "Pre-edit instructions field for Standard Formal (rollback)"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-drawing-studio.json"
      provides: "Pre-edit instructions field for Drawing Studio (rollback)"
    - path: ".planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-image-generator-deleted.json"
      provides: "Full document snapshot of deleted Image Generator agent (recovery)"
  key_links:
    - from: "apply-preset-guidance.mjs"
      to: "MongoDB agents collection (5 agents by id)"
      via: "mongodb driver updateOne({id}, {$set: {instructions}}) per agent"
      pattern: "updateOne.*\\$set.*instructions"
    - from: "4 text agent instructions"
      to: "child UI (kid sees redirect)"
      via: "LibreChat system prompt construction pipes agents.instructions into every turn"
      pattern: "KIDCHAT_PRESET_GUIDANCE_v1.*DRAWING REQUESTS"
    - from: "Drawing Studio instructions"
      to: "existing clarification-first training (preserved)"
      via: "new STAY ON TOPIC section permits clarification replies explicitly; placed additively"
      pattern: "clarification|clarifying"
    - from: "delete-stray-image-generator.mjs"
      to: "MongoDB agents + aclentries collections"
      via: "deleteOne({id: 'agent_F6ITBo7EuorE7vqrXsNAm'}) + deleteMany aclentries where resourceId matches the agent's _id"
      pattern: "agent_F6ITBo7EuorE7vqrXsNAm"
---

<objective>
Inject preset-aware guidance into the `instructions` field of all 5 in-UI agents so kids are redirected to the right preset for the right job, and delete the stray Image Generator agent that has DALL-E access but no UI entry point.

Purpose: Prevent accidental DALL-E token cost on text presets (kids asking "draw me a dog" on Friendly Tutor), stop Drawing Studio from being used as a general chat that burns through DALL-E budget on off-topic requests, and eliminate the back-door DALL-E access via the stray agent.

Output: 5 agents with updated `instructions` in MongoDB, 1 stray agent deleted (+ its ACL bindings), before/after JSON snapshots for rollback, and idempotent scripts committed to the quick task directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/260417-cs0-CONTEXT.md
@.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-01-SUMMARY.md

<interfaces>
<!-- Key MongoDB shape the executor needs — no exploration required -->

MongoDB connection (external TCP proxy):
```
mongodb://mongo:bnwf4anlnxzvdrkwlvi4ki6q7p52o33q@switchyard.proxy.rlwy.net:57501
```
Database: `test`
Collection: `agents`

Agent document shape (relevant fields):
```javascript
{
  _id: ObjectId,           // internal Mongo id — aclentries.resourceId binds to this
  id: "agent_...",         // stable public id — used for all $set operations
  name: "KidsChat ...",
  instructions: "...",     // <-- the ONLY field we mutate on 5 agents
  tools: [] | ["dalle"],
  // ... other fields untouched
}
```

Target agent IDs (verified 2026-04-17):
```
agent_wxgt6su7d3pcosiil3           → KidsChat Friendly Tutor    (tools:[])       — inject DRAWING REQUESTS
agent_y4w1cvoyg77p9thed9           → KidsChat Casual Buddy      (tools:[])       — inject DRAWING REQUESTS
agent_64q6z5s57552cpgl0hr          → KidsChat Balanced Helper   (tools:[])       — inject DRAWING REQUESTS
agent_aiv99mzvdzquym6y89k          → KidsChat Standard Formal   (tools:[])       — inject DRAWING REQUESTS
agent_kidschat_drawing_1775634945891 → KidsChat Drawing Studio   (tools:["dalle"]) — inject STAY ON TOPIC
agent_F6ITBo7EuorE7vqrXsNAm        → Image Generator (stray)    (tools:["dalle"]) — DELETE
```

aclentries shape (from Phase 15-safety-alert findings, STATE.md line 81):
```javascript
{
  principalId: ObjectId,     // user
  resourceId: ObjectId,      // MUST resolve agent _id first (NOT the public "id" string)
  // ...
}
```

Idempotency marker (MUST be in injected block):
```
<!-- KIDCHAT_PRESET_GUIDANCE_v1 -->
```

Script must: (1) read current `instructions`; (2) if marker present, skip and log; (3) else append the guidance block with the marker, $set back.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject preset guidance into 5 agents via idempotent MongoDB script</name>
  <files>
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-friendly-tutor.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-casual-buddy.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-balanced-helper.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-standard-formal.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-drawing-studio.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-friendly-tutor.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-casual-buddy.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-balanced-helper.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-standard-formal.json
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-drawing-studio.json
  </files>
  <action>
Write a Node ESM script at `.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs` that connects to the external MongoDB TCP proxy and updates the `instructions` field on all 5 in-UI agents.

**Connection:** `mongodb://mongo:bnwf4anlnxzvdrkwlvi4ki6q7p52o33q@switchyard.proxy.rlwy.net:57501` — DB `test`, collection `agents`. Use the `mongodb` driver already present in the project's node_modules (per Phase 17 pattern: `import { MongoClient } from 'mongodb';`). If not in node_modules, install locally with `npm install --no-save mongodb` before running. Do NOT add it to package.json.

**Idempotency marker (REQUIRED):**
```
<!-- KIDCHAT_PRESET_GUIDANCE_v1 -->
```

**Per-agent flow:**
1. `findOne({ id: agentId })`; if missing, exit with error (don't continue partially).
2. Write the full current doc (at minimum `id`, `name`, `instructions`) to `snapshots/before-<slug>.json` with 2-space pretty JSON. Slug mapping:
   - `agent_wxgt6su7d3pcosiil3` → `friendly-tutor`
   - `agent_y4w1cvoyg77p9thed9` → `casual-buddy`
   - `agent_64q6z5s57552cpgl0hr` → `balanced-helper`
   - `agent_aiv99mzvdzquym6y89k` → `standard-formal`
   - `agent_kidschat_drawing_1775634945891` → `drawing-studio`
3. If `instructions.includes('KIDCHAT_PRESET_GUIDANCE_v1')` → log `[skip] <name>: marker already present` and DO NOT write. Still write the after-snapshot (it will equal before).
4. Otherwise, compose `newInstructions = currentInstructions + '\n\n' + guidanceBlock` where `guidanceBlock` is agent-class-specific (see below). `updateOne({ id: agentId }, { $set: { instructions: newInstructions } })`.
5. Re-read the doc, write to `snapshots/after-<slug>.json`.

**Guidance block for the 4 text presets (Friendly Tutor, Casual Buddy, Balanced Helper, Standard Formal):**
```
<!-- KIDCHAT_PRESET_GUIDANCE_v1 -->
## DRAWING REQUESTS

If the child asks you to draw, generate, create, or make an image/picture/drawing, DO NOT attempt to draw — you don't have image tools in this mode. Instead, redirect them warmly:

"I can't draw here! Tap the preset picker at the top and choose **Drawing Studio** to make pictures. ✨"

Keep the redirect short and friendly. Don't lecture. After the redirect, don't continue the conversation about drawing — they need to switch presets first.
```

**Guidance block for Drawing Studio (agent_kidschat_drawing_1775634945891) — nuanced gate that preserves clarification flow:**
```
<!-- KIDCHAT_PRESET_GUIDANCE_v1 -->
## STAY ON TOPIC

Your purpose is drawings and drawings only. BUT asking clarifying questions about a drawing-in-progress is on-topic (e.g. "what color?", "indoor or outdoor scene?", "what kind of dog?") — those are part of your drawing workflow, and a child's reply to one of your clarifying questions is ALSO on-topic even if it looks like a short plain answer.

Two kinds of text turns are always allowed:
1. A drawing request (draw me X, can you make a picture of Y, etc.)
2. A clarification reply the child sends in response to YOUR most recent clarifying question in this conversation.

For anything else — homework, general questions, just chatting, math, trivia — gently refuse and redirect:

"I only help with drawings here! To chat, switch to any chat preset — Friendly Tutor, Casual Buddy, Balanced Helper, or Standard Formal. 🎨"

After a drawing generates successfully, you MAY add one short line to remind them they can switch back:

"Want to chat about something else? Switch to any chat preset to save your daily budget."
```

Note: the guidance must be APPENDED to the existing `instructions` — never replace. The existing training (including Drawing Studio's clarification-first behavior established in Phase 19-01) must remain intact.

**Script output:** Print a summary table at the end: `name | skipped? | before_length | after_length`.

**How to run:** `node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs` from the project root.

Run the script once. Then run it a second time to prove idempotency — the second run must report `[skip]` for all 5 agents and NOT change the stored `instructions` (compare an after-snapshot from run 1 with a fresh `findOne` after run 2 — they should be identical).
  </action>
  <verify>
    <automated>
cd /data/home/KidAI && \
  node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs && \
  node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/apply-preset-guidance.mjs 2>&1 | tee /tmp/cs0-second-run.log && \
  grep -c '\[skip\]' /tmp/cs0-second-run.log | grep -q '^5$' && \
  for f in friendly-tutor casual-buddy balanced-helper standard-formal drawing-studio; do \
    test -s .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-$f.json || exit 1; \
    test -s .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-$f.json || exit 1; \
    grep -q 'KIDCHAT_PRESET_GUIDANCE_v1' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-$f.json || exit 1; \
  done && \
  grep -q 'DRAWING REQUESTS' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-friendly-tutor.json && \
  grep -q 'DRAWING REQUESTS' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-casual-buddy.json && \
  grep -q 'DRAWING REQUESTS' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-balanced-helper.json && \
  grep -q 'DRAWING REQUESTS' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-standard-formal.json && \
  grep -q 'STAY ON TOPIC' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-drawing-studio.json && \
  grep -qi 'clarif' .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-drawing-studio.json && \
  echo "VERIFY PASS: 5 agents updated, marker + section headings present, second run idempotent"
    </automated>
  </verify>
  <done>
- Script file exists and runs cleanly against live MongoDB.
- 10 snapshot files exist (5 before + 5 after), all non-empty valid JSON.
- All 5 `after-*.json` snapshots contain the `KIDCHAT_PRESET_GUIDANCE_v1` marker.
- 4 text-preset `after-*.json` contain `DRAWING REQUESTS` section heading.
- Drawing Studio `after-drawing-studio.json` contains BOTH `STAY ON TOPIC` and a clarification-related word ("clarif...") — proves the nuanced gate is present.
- Second invocation of the script prints `[skip]` for all 5 agents (idempotency proven).
- The drawing-studio `after` snapshot's `instructions` length > `before` length (content was appended, not replaced).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete stray Image Generator agent + clean up ACL bindings</name>
  <files>
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs
    .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-image-generator-deleted.json
  </files>
  <action>
Write a Node ESM script at `.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs` that removes the stray `Image Generator` agent and any ACL entries pointing at it.

**Same connection string as Task 1:** `mongodb://mongo:bnwf4anlnxzvdrkwlvi4ki6q7p52o33q@switchyard.proxy.rlwy.net:57501`, DB `test`.

**Steps (order matters — ACL first, then agent, so we capture resourceId before it's gone):**

1. `const agent = await db.collection('agents').findOne({ id: 'agent_F6ITBo7EuorE7vqrXsNAm' })`.
   - If `null` → log `[skip] Image Generator already deleted` and exit cleanly (idempotent). Still emit a before-snapshot file with `{status: "already_deleted", timestamp: ...}` if it doesn't exist.
   - Else write the ENTIRE doc to `snapshots/before-image-generator-deleted.json` (pretty JSON, 2 spaces). This is the recovery artifact — if deletion is regretted, the doc can be re-inserted verbatim.
2. Capture `const resourceId = agent._id` (per STATE.md line 81 — aclentries.resourceId is the Mongo ObjectId, not the public `id` string).
3. `const aclMatches = await db.collection('aclentries').find({ resourceId }).toArray()` and log the count and principalIds. Append these to the before-snapshot under a top-level `aclentries` key so they're recoverable too.
4. `await db.collection('aclentries').deleteMany({ resourceId })` — log `deletedCount`.
5. `await db.collection('agents').deleteOne({ id: 'agent_F6ITBo7EuorE7vqrXsNAm' })` — log `deletedCount`.
6. Final verification within the script: `findOne({ id: 'agent_F6ITBo7EuorE7vqrXsNAm' })` MUST now be `null`; `countDocuments({ resourceId })` MUST now be `0`. If either fails, script exits non-zero.

**Running a second time must be safe:** the `if (agent === null)` guard at step 1 handles idempotency.

**How to run:** `node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs` from project root.

Execute the script once, then run it again to verify it exits cleanly on a second invocation with no errors.
  </action>
  <verify>
    <automated>
cd /data/home/KidAI && \
  node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs && \
  test -s .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-image-generator-deleted.json && \
  node .planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/scripts/delete-stray-image-generator.mjs 2>&1 | grep -q 'already_deleted\|already deleted\|\[skip\]' && \
  node -e "
    import('mongodb').then(async ({ MongoClient }) => {
      const c = new MongoClient('mongodb://mongo:bnwf4anlnxzvdrkwlvi4ki6q7p52o33q@switchyard.proxy.rlwy.net:57501');
      await c.connect();
      const db = c.db('test');
      const a = await db.collection('agents').findOne({ id: 'agent_F6ITBo7EuorE7vqrXsNAm' });
      if (a !== null) { console.error('FAIL: agent still exists'); process.exit(1); }
      console.log('VERIFY PASS: Image Generator deleted, second run idempotent, snapshot saved');
      await c.close();
    });
  "
    </automated>
  </verify>
  <done>
- Script file exists and runs cleanly against live MongoDB.
- `snapshots/before-image-generator-deleted.json` exists, is non-empty, and contains the full deleted doc + any aclentries matches (recovery artifact).
- `db.agents.findOne({id: 'agent_F6ITBo7EuorE7vqrXsNAm'})` returns `null` after the script runs.
- `db.aclentries.countDocuments({resourceId: <original _id>})` returns `0` after the script runs.
- Running the script a second time exits cleanly (no crash, no partial state).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — parent verifies preset guidance works end-to-end from a child account</name>
  <what-built>
Tasks 1 and 2 have already run: all 5 in-UI agents now have preset-aware guidance in their `instructions` field (DRAWING REQUESTS block on the 4 text presets; STAY ON TOPIC block on Drawing Studio), and the stray Image Generator agent has been deleted from MongoDB along with any ACL bindings. Before-snapshots are in the task directory for rollback.

No code was deployed. LibreChat reads `agents.instructions` live on every turn, so the new guidance is active immediately — no redeploy needed.
  </what-built>
  <how-to-verify>
Parent logs into LibreChat at https://librechat-production-bff2.up.railway.app as a **child account** (not admin) and runs the following three tests. Do NOT clear conversations between tests — each test needs a fresh conversation to avoid history contaminating behavior.

**Test A — Text preset rejects drawing request (Friendly Tutor):**
1. Open a new conversation.
2. Select preset: **Friendly Tutor** (top preset picker).
3. Send: `draw me a dog`
4. **Expected:** Agent responds with a friendly redirect along the lines of "I can't draw here! Tap the preset picker at the top and choose Drawing Studio to make pictures. ✨". NO image is generated. Prompt token count stays low (similar to a normal text turn — roughly 2,000-3,000 credits at Haiku rates).
5. **Fail signal:** Agent tries to describe/draw or generates an image.

**Test B — Drawing Studio refuses off-topic question:**
1. Open a new conversation.
2. Select preset: **Drawing Studio**.
3. Send: `what's 7x8?`
4. **Expected:** Agent responds with a gentle refusal naming all 4 chat presets, e.g. "I only help with drawings here! To chat, switch to any chat preset — Friendly Tutor, Casual Buddy, Balanced Helper, or Standard Formal. 🎨". NO image is generated. No math answer.
5. **Fail signal:** Agent answers "56" or generates an unrelated image.

**Test C — Drawing Studio clarification flow still works (CRITICAL — this proves the nuanced gate):**
1. Open a new conversation.
2. Select preset: **Drawing Studio**.
3. Send: `draw me a dog`
4. **Expected turn 1:** Agent asks a clarifying question (e.g. "what kind of dog? what color? indoor or outdoor scene?") — NO image yet, NO refusal. This is the existing clarification-first training preserved.
5. Send the reply: `golden retriever, outdoor, sunny day`
6. **Expected turn 2:** Agent generates an image of a golden retriever. This proves the new STAY ON TOPIC guidance correctly classifies the short plain reply as a clarification, not an off-topic chat.
7. **Fail signal:** On turn 1, agent draws immediately without clarifying (existing training regressed) OR refuses with the STAY ON TOPIC message (new guidance over-blocked). On turn 2, agent refuses the reply as off-topic (nuanced gate broken) OR never generates.

**Bonus check — Image Generator no longer reachable:**
8. In the preset picker, confirm there is no "Image Generator" option (was never expected to be there — just confirming the picker still shows exactly 5 presets).

Report results in format:
```
Test A (text redirect):    PASS / FAIL — [notes]
Test B (DS off-topic):     PASS / FAIL — [notes]
Test C (DS clarification): PASS / FAIL — [notes]
Picker shows 5 presets:    PASS / FAIL — [notes]
```

If any test fails, describe the actual agent response so we can adjust the guidance wording without rolling back the whole change.
  </how-to-verify>
  <resume-signal>Type "approved" (with the 4-line test result summary) to mark the quick task complete. If any test FAILed, describe the failure and we'll iterate on the guidance wording (re-running the idempotent script after editing the guidance block text).</resume-signal>
</task>

</tasks>

<verification>
All automated verification runs as part of Tasks 1 and 2 (see each task's `<verify>` block). Task 3 is the only manual step and cannot be automated — it requires a real child UI session to confirm the LLM actually follows the new guidance in practice (no amount of snapshot-diffing proves the model behaves correctly at runtime).

Pre-UAT sanity check the executor should run before handing off to the parent:
```bash
# Confirm no regressions in the existing Drawing Studio clarification training —
# the before-snapshot should still be a substring of the after-snapshot (append-only).
cd /data/home/KidAI && \
  node -e "
    const fs = require('fs');
    const before = JSON.parse(fs.readFileSync('.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/before-drawing-studio.json', 'utf8'));
    const after = JSON.parse(fs.readFileSync('.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/snapshots/after-drawing-studio.json', 'utf8'));
    if (!after.instructions.startsWith(before.instructions)) {
      console.error('FAIL: after.instructions does not start with before.instructions — content was replaced, not appended');
      process.exit(1);
    }
    console.log('PASS: Drawing Studio instructions append-only (existing training preserved)');
  "
```
</verification>

<success_criteria>
- All 5 in-UI agents have the `KIDCHAT_PRESET_GUIDANCE_v1` marker + appropriate guidance block in their live MongoDB `instructions` field (verifiable via `mongosh` or the after-snapshots).
- Stray `agent_F6ITBo7EuorE7vqrXsNAm` does NOT exist in `agents` collection; zero aclentries with the deleted agent's old `resourceId`.
- All 6 before-snapshot files (5 modified + 1 deleted) exist in the task directory and are non-empty.
- Both scripts are idempotent: running either a second time is a no-op.
- Task 3 UAT: parent reports PASS on all 4 checks (Test A, Test B, Test C, picker-shows-5-presets).
- Existing Drawing Studio clarification-first training is preserved (append-only edit, verified via prefix-check in `<verification>` section).
- CONTEXT.md locked decisions honored: phrasing matches "Tap the preset picker at the top and choose Drawing Studio ✨"; Drawing Studio refusal names all 4 text preset names; no auto-preset-switching (out of scope).
</success_criteria>

<output>
After completion, create `.planning/quick/260417-cs0-preset-aware-guidance-kids-told-to-switc/260417-cs0-SUMMARY.md` with:
- What shipped (5 agents updated, 1 deleted, idempotency proven)
- UAT results from Task 3 (verbatim parent response)
- Any guidance wording adjustments made during UAT iteration (if Test A/B/C failed and we re-ran the script with tweaked wording)
- Rollback path reminder: `snapshots/before-*.json` contains pre-edit `instructions` for all 5 agents; `before-image-generator-deleted.json` contains the full deleted doc + aclentries for recovery.
</output>
