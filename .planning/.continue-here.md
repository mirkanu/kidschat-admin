---
context: default
phase: ad-hoc
task: 8
total_tasks: 9
status: in_progress
last_updated: 2026-04-24T10:01:37.103Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Agent instructions are authoritative — The `systemPrompt` field in `librechat.yaml` has always been broken (wrong YAML indentation) and LibreChat ignores it entirely. The real prompts governing what kids see live in MongoDB `test.agents` collection, field `instructions`. Any prompt change MUST update agent instructions directly.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Editing YAML systemPrompt | The `systemPrompt` block in librechat.yaml was at 6-space indent inside `balance:` — invalid YAML, silently ignored by LibreChat | blocking | Always update MongoDB `agents.instructions` directly; YAML systemPrompt field has been removed |
| RAILWAY_API_TOKEN GraphQL 403 | The `RAILWAY_API_TOKEN` env var is a service-scoped token that returns 403 on all Railway GraphQL mutations | advisory | Use `railway` CLI directly (authenticated as user) for variable sets and redeploys |

<current_state>
Session ended mid-task. A GSD quick task was spawned to fix the admin UI prompt editor so it writes to MongoDB agent instructions. The task agent may still be running or may have stalled — its directory was not yet visible in `.planning/quick/` at pause time.

LibreChat is currently live and working correctly:
- All 4 KidsChat agents have the counselling rule (rule 7) in their instructions
- YAML is valid with 6 modelSpecs (5 chat presets + Image Search)
- Daily summary email was manually triggered for 2026-04-24 (cron will also fire at 8am UTC — duplicate expected)
</current_state>

<completed_work>

- ✅ Counselling rule (rule 7) added to all 4 KidsChat agent instructions in MongoDB (`test.agents` collection)
- ✅ Confirmed working via live chat test: "I'm feeling sad" → brief biblical encouragement + redirect to parents, no follow-up questions
- ✅ Fixed broken librechat.yaml — removed misplaced `systemPrompt` block that was invalidating YAML parse
- ✅ Image Search preset restored to YAML modelSpecs (was hidden when YAML was fixed and `enforce:true` took effect)
- ✅ LibreChat redeployed with valid 6-preset config
- ✅ Daily summary email manually triggered (sent: 2)
- 🔄 GSD quick task spawned for prompt editor fix — status unknown at pause
</completed_work>

<remaining_work>

- Task 8 (IN PROGRESS): GSD quick — fix admin UI prompt editor to update MongoDB agent instructions on deploy
  - Check `.planning/quick/` for new directory; if missing, re-run `/gsd-quick` with same context
  - Deploy route: `src/app/api/prompt-editor/deploy/route.ts`
  - Must update `instructions` field on all 4 agents (`KidsChat Friendly Tutor`, `KidsChat Casual Buddy`, `KidsChat Balanced Helper`, `KidsChat Standard Formal`)
  - Preserve each agent's unique TONE section when replacing shared sections
  - Prompt editor page should load from first agent's instructions (not `app_config.active_prompt`)

- Task 9 (NOT STARTED): Railway PAT for deploy route CONFIG_PATH fix
  - User needs to generate token at `railway.app/account/tokens`
  - Store as new env var (not RAILWAY_API_TOKEN which is service-scoped)
  - Update deploy route to use it
</remaining_work>

<decisions_made>

- Agent instructions in MongoDB are the authoritative prompt source, not YAML systemPrompt
- librechat.yaml `systemPrompt` block removed entirely (was broken dead code)
- Image Search safety is handled at API level (already built), not agent instructions — no changes needed
- Drawing Studio is fully safety-governed; Image Search relies on Openverse filtering + ACL gate
</decisions_made>

<blockers>

- Railway PAT: The `RAILWAY_API_TOKEN` env var returns 403 on GraphQL mutations. Deploy route's CONFIG_PATH update silently fails. Workaround: use railway CLI. Fix: user needs to provide a personal token from railway.app/account/tokens.
</blockers>

## Required Reading (in order)
1. `src/app/api/prompt-editor/deploy/route.ts` — current deploy route, needs agent instructions update added
2. `src/app/(dashboard)/prompt-editor/page.tsx` — loads initial prompt from `app_config.active_prompt`, should load from agent instead
3. MongoDB `test.agents` — run a quick query to see current agent instructions structure before editing

## Infrastructure State
- LibreChat: live at https://librechat-production-bff2.up.railway.app, recently redeployed, all 6 presets working
- CONFIG_PATH: `https://gist.githubusercontent.com/mirkanu/e23b999f1d3cd77726a97c20e26f0abf/raw/3a5cb7e3874a0c245c022ff45f1c30d2a0529d8e/librechat.yaml`
- MongoDB: accessible via `switchyard.proxy.rlwy.net:57501` (public proxy), credentials in `.env.local`
- Railway CLI: authenticated as manuelkuhs@gmail.com

<context>
The big discovery this session: the admin UI prompt editor has been editing a dead field (YAML systemPrompt) that LibreChat ignores. The real prompts are in MongoDB agent instructions. This was hidden because the broken YAML caused LibreChat to silently fall back to defaults for modelSpecs, but agents loaded their own instructions regardless. The counselling rule fix was applied correctly by directly updating MongoDB agents.
</context>

<next_action>
Check if `.planning/quick/` has a new directory created by the prompt-editor GSD task. If yes, check its status and resume/verify. If no directory exists, the agent stalled — re-run `/gsd-quick` with the prompt editor fix context (update deploy route to write agent instructions + update prompt editor page to load from agents).
</next_action>
