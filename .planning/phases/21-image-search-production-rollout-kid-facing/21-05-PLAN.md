---
phase: 21
plan: 05
type: execute
wave: 3
depends_on: [21-01, 21-02, 21-03, 21-04]
files_modified:
  - .planning/phases/21-image-search-production-rollout-kid-facing/21-UAT.md
autonomous: false
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05
  - SEARCH-06
  - SEARCH-07
  - SAFETY-01
  - SAFETY-02
  - OVERSIGHT-01
  - OVERSIGHT-02
must_haves:
  truths:
    - "Penelope signs in to LibreChat on her iPad and sees Image Search as a sixth preset tile"
    - "Sebastian signs in on his iPad and sees Image Search as a sixth preset tile"
    - "Penelope submits 'rainbow' (or parent-chosen kid-safe query); an inline 3-column grid of 8-20 thumbnails renders — no clickable links, no AI commentary, no broken-image icons. SEARCH-02 acceptance threshold is ≥60% of parent-chosen normal queries returning non-empty grids (Phase 20 baseline was 60% + 40% empty due to Openverse quirk; Plan 21-01 retry aims to lift this but 100% is NOT required for phase close — reduced bar documented in 20-05 C-2 option B)"
    - "Parent submits one blocklist-matching query as claude-test@ (or via a throwaway kid session) — tool returns empty + parent email arrives within cron cycle"
    - "Parent sets Penelope's dailySearchCountCap to 3 via /settings; Penelope's 4th search in the same UTC day returns no images with a graceful agent response"
    - "Admin /conversations log shows BOTH kids' Image Search sessions with the new badge, AND shows the blocklist-triggered session tagged as a safety alert"
  artifacts:
    - path: .planning/phases/21-image-search-production-rollout-kid-facing/21-UAT.md
      provides: "Per-query table of UAT verdicts, parent sign-off line, caveats list feeding any gap closure phase 21.1"
      contains: "Verdict"
  key_links:
    - from: Penelope/Sebastian iPad
      to: LibreChat kid UI
      via: "Normal sign-in + preset selector"
      pattern: "Image Search"
    - from: LibreChat agent
      to: kidschat-image-search-mcp /mcp
      via: "MCP tool call pipeline (blocklist → quota → Openverse → domain-filter → proxy-rewrite)"
      pattern: "image_search"
---

<objective>
Live UAT as the kids. All prior plans built the machinery; this plan proves it works end-to-end for the actual users and produces the sign-off artifact required to close Phase 21.

Purpose: Burn every Phase 21 requirement down to observable parent-verified evidence.
Output: `21-UAT.md` with a verdict per success-criterion truth. Verdict options: PASS / FAIL / APPROVED-WITH-CAVEATS. If the verdict is APPROVED-WITH-CAVEATS, caveats feed a Phase 21.1 gap closure or a quick task.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-image-search-research-poc/20-UAT.md
@.planning/phases/20-image-search-research-poc/20-05-SUMMARY.md
@.planning/phases/20-image-search-research-poc/20-DECISIONS.md
@.planning/phases/21-image-search-production-rollout-kid-facing/21-01-SUMMARY.md
@.planning/phases/21-image-search-production-rollout-kid-facing/21-02-SUMMARY.md
@.planning/phases/21-image-search-production-rollout-kid-facing/21-03-SUMMARY.md
@.planning/phases/21-image-search-production-rollout-kid-facing/21-04-SUMMARY.md

<interfaces>
UAT delivery: prefer Telegram bot review flow from MEMORY.md reference_telegram_bot (creds /data/home/gsdTelegram/.env). For each kid query, Claude runs the query via either (a) the kid's session driven by scripts/chat-test.ts with the kid's userId/JWT, OR (b) the parent physically types it on each kid's iPad, and Claude pulls the resulting conversation from MongoDB and builds a Telegram photo album for parent review.

Kids' userIds:
- Sebastian: 69d0315763d6125f1f553e97
- Penelope:  69d0315763d6125f1f553e98

For the blocklist-trigger test: DO NOT have a real kid type a blocklist term. Use claude-test@kidschat.local (admin ACL, safe) or a throwaway curl against the MCP directly. Keep the exact adversarial phrasings OUT of git (same convention as 20-UAT.md).

SEARCH-02 acceptance threshold (20-05 C-2 follow-through): Parent picks 5 normal kid-safe queries per kid (10 total). PASS threshold is ≥60% returning non-empty grids. 60-99% → APPROVED-WITH-CAVEATS (caveats list empty-result queries for Phase 21.1 OAuth upgrade). 100% would close C-2 outright. <60% → FAIL, revision required.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21-05-01: Pre-UAT smoke + kid-preset-visibility check</name>
  <what-built>
    Claude runs a 5-minute pre-UAT smoke pass before paging the parent:
    1. `curl https://kidschat-image-search-mcp-production.up.railway.app/health` → ok:true.
    2. `scripts/chat-test.ts` as claude-test@ with query "origami cats" — captures: render URL pattern (must be /proxy?u=…), image count, tool-response JSON.
    3. `curl` the quota endpoint 21× for a throwaway userId — confirms 20 allowed + 1 denied.
    4. Run the CURRENT `/api/cron/daily-reset` to clear counters before kid UAT.
    5. Telegram-bot ping to parent: "Phase 21 pre-UAT green — please hand Penelope + Sebastian their iPads to run the live test."
  </what-built>
  <how-to-verify>
    Parent receives the Telegram ping. Parent replies "go" when ready to proceed to Task 21-05-02.
  </how-to-verify>
  <resume-signal>Parent types "go" — proceed to Task 21-05-02.</resume-signal>
  <done>Pre-UAT smoke complete; kids' counters at 0; admin/MCP endpoints responsive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21-05-02: Live kid UAT — happy path + safety path + cap path</name>
  <what-built>
    Parent walks through this checklist with each kid present. Claude watches MongoDB in real time and builds `21-UAT.md` per-query rows.

    **A) Preset visibility (SEARCH-01)**
      - Penelope opens LibreChat on iPad. Parent confirms she sees 6 preset tiles including "Image Search" with the image-plus icon. → Capture screenshot to Telegram.
      - Sebastian opens LibreChat on his iPad. Same check. → Capture screenshot.

    **B) Happy-path query (SEARCH-02, 04, 05, 06)** — parent chooses 5 safe queries per kid (10 total; suggestions: "rainbow", "lions", "origami cats", "castles", "cute red origami cats" — include at least one query that was zero-result in 20-UAT Q1–Q10 to validate the Plan 21-01 modifier-trimming retry path):
      - Kid types the query. Within ~2s: 3-column thumbnail grid (up to 20 images) renders inline — OR graceful "no results" if both the first fetch and the stripped-modifier retry came back empty.
      - Parent verifies: no clickable links (long-press → iOS lightbox, tap → does nothing navigational); no AI commentary text beyond the helper note; no broken-image icons.
      - Claude immediately pulls the conversation from MongoDB and verifies all thumbnail URLs are `https://kidschat-image-search-mcp-production.up.railway.app/proxy?u=...` (SEARCH-06).
      - **SEARCH-02 hit-rate tally**: count queries returning non-empty grids. Threshold PASS ≥60%, APPROVED-WITH-CAVEATS 60-99%, FAIL <60% (see 20-05 C-2 Option B lowered threshold).

    **C) Safety-path query (SEARCH-03, SAFETY-01, SAFETY-02)** — parent does NOT have kid type this. Parent uses a throwaway LibreChat session as Manuel (admin) OR claude-test@ and submits ONE blocklist-matching query (anatomy/self-harm/drug category — exact phrasing stays off-git):
      - Expected MCP result: `{"images":[],"error":"blocked_query"}`. Agent emits graceful empty-message response.
      - Within 60s: Claude checks MongoDB `email_notifications` collection for a new row matching this conversationId + matchedPattern. OR the parent's Resend inbox shows the alert email.
      - `/api/alerts` (admin UI) shows the new event with the correct matchedPattern label.

    **D) Cap-path (SEARCH-07, SEARCH-08)**:
      - Parent navigates to /settings and sets Penelope's Daily-searches override to **3**, presses Save.
      - Penelope runs 3 more safe queries (cumulative with query B). On the 4th query, the agent returns the graceful "No safe images — try something else" (or similar) fallback because MCP returned `{"error":"quota_exceeded"}`.
      - Parent deletes Penelope's override (restore global default) — confirms query 5 works again on a subsequent day OR after manual `/api/cron/daily-reset` trigger.

    **E) Oversight (OVERSIGHT-01, OVERSIGHT-02)**:
      - Parent opens /conversations — confirms:
        - Both kids' happy-path conversations have an "Image Search" badge.
        - The blocklist-triggered admin session ALSO has the badge AND an adjacent safety-alert indicator (if /conversations already cross-refs /alerts; if not, note as an optional polish item, not a blocker).
        - Clicking into a conversation shows the full message thread with thumbnails and persisted query text.

    Claude populates `.planning/phases/21-image-search-production-rollout-kid-facing/21-UAT.md` with a per-row table following the 20-UAT.md shape:
      | # | Requirement | Query / step | Expected | Observed | Verdict |
    Final row must explicitly record the SEARCH-02 hit rate: "8/10 non-empty → APPROVED-WITH-CAVEATS — 2 zero-result queries listed for Phase 21.1" (or equivalent).
  </what-built>
  <how-to-verify>
    Parent walks through steps A–E with the kids; Claude drives the Telegram bot to capture proof and updates 21-UAT.md live. Parent replies at the end with one of:
      - **"approved"** — all success criteria PASS. Plan 21-05 done; Phase 21 closed.
      - **"caveats: <list>"** — APPROVED-WITH-CAVEATS; caveats become Phase 21.1 gap inputs or quick-task candidates.
      - **"fail: <what broke>"** — back-to-planning revision required; halt and page /gsd-revise-plan.
  </how-to-verify>
  <resume-signal>Parent replies "approved", "caveats: …", or "fail: …".</resume-signal>
  <done>21-UAT.md written, signed off (or explicitly caveated); phase ready for closeout commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21-05-03: Phase-close commit + STATE update + ROADMAP check</name>
  <what-built>
    Post-UAT-approval only:
    1. Update .planning/STATE.md — add [Phase 21] decision lines (key choices), including:
       - "userId forwarding via <header/meta/fallback>"
       - "blocklist hit → type=image_prompt for email-template reuse"
       - "preset-distinguishing field = <chosen field>"
       - "CONFIG_PATH resolution (D-10): <branch A / B1 / B2>" — reference 21-04-CONFIG-PATH-CHECK.md
       - "20-05 C-2 (Openverse zero-result quirk): <closed / partially mitigated via Plan 21-01 retry / still open>; UAT SEARCH-02 hit rate = <X>/<Y>"
       - **"Decision: claude-test@ preserved for future CI/E2E test runs — kill-decision deferred to Phase 22 Test Mode (W6 revision fix; Phase 20 scope-lock #6 explicitly deferred, not dropped)"**
    2. Update .planning/ROADMAP.md — mark Phase 21 as ✅ with completion date; update v2.9 milestone line (Phase 21 ✓ of 3).
    3. Git commit: `docs(21): close Phase 21 — image search live for kids (all 12 v1 requirements closed)` with Co-Authored-By trailer.
    4. Railway verification: `railway status --service kidschat-image-search-mcp`, `railway status --service kidschat-admin` — both green.
  </what-built>
  <how-to-verify>
    Parent reviews the commit message and STATE diff. Replies "ship it" to approve the close.
  </how-to-verify>
  <resume-signal>Parent types "ship it".</resume-signal>
  <done>Phase 21 closed in ROADMAP + STATE; v2.9 progress bar advances; Phase 22 becomes unblocked; claude-test@ preservation decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kid iPad → LibreChat kid UI | Existing boundary — kid auth session, parent-controlled device |
| Parent iPhone (Telegram) → Claude Code session | Review channel; no new writes happen over Telegram |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | Information Disclosure | Adversarial query text leaking into git via 21-UAT.md | mitigate | Follow 20-UAT.md convention — categorical references only, exact phrasings stay in parent chat context |
| T-21-21 | Tampering | Kid screenshots or saves thumbnails, bypassing parent visibility | accept | iOS long-press save is explicitly the UX (D-3 constraint); parent sees the QUERY + URLs in /conversations which is the primary oversight mechanism |
| T-21-22 | Denial of Service | UAT runs exhaust kids' real daily cost budget | accept | Image-search agent cost is minimal (no LLM calls beyond the router prompt); budget impact is <€0.01 per UAT session |
</threat_model>

<verification>
Every truth in must_haves is observed live by the parent in the same session that signs off 21-UAT.md.
</verification>

<success_criteria>
Parent signs off 21-UAT.md APPROVED (or APPROVED-WITH-CAVEATS with caveats tracked), STATE + ROADMAP updated, claude-test@-preservation decision recorded, Phase 22 unblocked.
</success_criteria>

<output>
Write `.planning/phases/21-image-search-production-rollout-kid-facing/21-05-SUMMARY.md` (the completion record) alongside the primary artifact `21-UAT.md`. Ship-it commit references both.
</output>
</output>
