---
phase: 18-email-alert-system
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/lib/daily-summary.ts
  - src/app/api/notify/daily-summary/route.ts
  - src/components/emails/daily-summary-email.tsx
  - src/lib/account-activity.ts
  - src/app/api/notify/account-activity/route.ts
  - src/components/emails/account-activity-email.tsx
  - railway-cron/index.mjs
autonomous: true
requirements: [EMAIL-DAILY-01, EMAIL-ACTIVITY-01]

must_haves:
  truths:
    - "Daily summary email sent to opted-in recipients with per-child stats for the last 24 hours"
    - "Account activity alerts sent on login events and settings changes"
    - "Railway cron calls both daily-summary and weekly-digest endpoints"
    - "New email templates match the visual style of existing safety-alert and weekly-digest emails"
  artifacts:
    - path: "src/lib/daily-summary.ts"
      provides: "Daily stats aggregation from MongoDB"
      exports: ["getDailyChildStats", "DailyChildStats"]
    - path: "src/app/api/notify/daily-summary/route.ts"
      provides: "POST endpoint for daily summary emails"
      exports: ["POST"]
    - path: "src/components/emails/daily-summary-email.tsx"
      provides: "React Email template for daily summary"
      exports: ["DailySummaryEmail"]
    - path: "src/lib/account-activity.ts"
      provides: "Account activity notification sender"
      exports: ["notifyAccountActivity", "AccountActivityType"]
    - path: "src/app/api/notify/account-activity/route.ts"
      provides: "POST endpoint for account activity alerts"
      exports: ["POST"]
    - path: "src/components/emails/account-activity-email.tsx"
      provides: "React Email template for account activity"
      exports: ["AccountActivityEmail"]
  key_links:
    - from: "src/lib/daily-summary.ts"
      to: "src/lib/notification-recipients.ts"
      via: "getRecipientsForType('dailySummary')"
      pattern: "getRecipientsForType"
    - from: "src/lib/account-activity.ts"
      to: "src/lib/notification-recipients.ts"
      via: "getRecipientsForType('accountActivity')"
      pattern: "getRecipientsForType"
    - from: "railway-cron/index.mjs"
      to: "/api/notify/daily-summary"
      via: "HTTP POST with cron secret"
      pattern: "daily-summary"
---

<objective>
Add daily summary emails and account activity alerts as new notification types, plus update the Railway cron to trigger daily summaries.

Purpose: Parents want more frequent updates than weekly (daily summary) and awareness of account events (logins, settings changes).

Output: Two new alert types fully implemented end-to-end (lib + API + email template), cron updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-email-alert-system-transactional-email-service-for-automated-parent-notifications/18-01-SUMMARY.md

@src/lib/weekly-digest.ts
@src/app/api/notify/weekly-digest/route.ts
@src/components/emails/weekly-digest-email.tsx
@src/components/emails/safety-alert-email.tsx
@src/lib/notify-safety-alert.ts
@railway-cron/index.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create daily summary email (lib + template + API route)</name>
  <files>src/lib/daily-summary.ts, src/components/emails/daily-summary-email.tsx, src/app/api/notify/daily-summary/route.ts</files>
  <action>
Create `src/lib/daily-summary.ts`:
- Model after `src/lib/weekly-digest.ts` but for 24-hour window instead of 7 days.
- Export `DailyChildStats` interface: `{ name, totalMessages, imageRequests, topPresets }`.
- Export `getDailyChildStats(db: Db)`: aggregate messages from last 24 hours, same pipeline as weekly but with 1-day window. Count image requests by checking for agent-related conversations (where `conv.agentId` exists). No `activeDays` (it's always 1 day).
- Export `formatDailyStats(raw)` as a pure function for testability (same pattern as weekly).

Create `src/components/emails/daily-summary-email.tsx`:
- React Email component matching the visual style of `weekly-digest-email.tsx` (blue header theme).
- Use green header instead of blue to visually distinguish from weekly.
- Props: `{ children: DailyChildStats[], date: string }`.
- Show per-child section: name, message count, image request count, presets used.
- Include a "View Dashboard" link to the admin URL.
- Keep it simple — this is a brief daily update, not a detailed report.

Create `src/app/api/notify/daily-summary/route.ts`:
- Model after `weekly-digest/route.ts`.
- Accept CRON_SECRET header auth OR admin session auth (same dual-auth pattern).
- Lazy import `getRecipientsForType` from notification-recipients. Get emails for `dailySummary`.
- Fall back to ADMIN users if recipients empty (same backward compat as Plan 01 migration).
- Call `getDailyChildStats(db)`, skip if no stats.
- Send via Resend with DailySummaryEmail template.
- Record in `email_notifications` collection with `type: "daily_summary"`.
- Subject: "KidsChat Daily Summary — {date}".
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx tsc --noEmit src/lib/daily-summary.ts src/app/api/notify/daily-summary/route.ts src/components/emails/daily-summary-email.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Daily summary email sends per-child stats for the last 24 hours to opted-in recipients. Template visually matches existing emails.</done>
</task>

<task type="auto">
  <name>Task 2: Create account activity alerts + update Railway cron</name>
  <files>src/lib/account-activity.ts, src/components/emails/account-activity-email.tsx, src/app/api/notify/account-activity/route.ts, railway-cron/index.mjs</files>
  <action>
Create `src/lib/account-activity.ts`:
- Export `AccountActivityType = "login" | "settings_change" | "recipient_added" | "recipient_removed"`.
- Export `notifyAccountActivity(input: { activityType: AccountActivityType, description: string, performedBy: string, ipAddress?: string })`:
  - Lazy import getRecipientsForType, get emails for `accountActivity`.
  - If no recipients, return early (no fallback to ADMIN — this is a new opt-in feature, defaults false).
  - Send via Resend with AccountActivityEmail template.
  - Record in `email_notifications` with `type: "account_activity"`.
  - Dedup: same activityType + performedBy within 5 minutes (prevent login spam).

Create `src/components/emails/account-activity-email.tsx`:
- React Email component with orange/amber header theme (distinct from red safety, blue weekly, green daily).
- Props: `{ activityType, description, performedBy, timestamp, dashboardUrl }`.
- Simple layout: what happened, who did it, when, link to dashboard.

Create `src/app/api/notify/account-activity/route.ts`:
- POST endpoint, auth required (session-based only, no cron secret).
- Body: `{ activityType, description, performedBy, ipAddress? }`.
- Validate activityType against allowed values.
- Call notifyAccountActivity.

Update `railway-cron/index.mjs`:
- After the existing weekly digest call, add a daily summary call.
- The cron runner should call `${adminUrl}/api/notify/daily-summary` with the same cron-secret header.
- Structure: run daily-summary first (it runs every day), then weekly-digest (only meaningful on Mondays but harmless to call daily — the API can skip if not Monday, OR just keep both calls and let Railway schedule handle frequency).
- Actually, better approach: Make the cron script accept a CRON_TYPE env var or just always call daily-summary. The Railway cron service schedule determines frequency. Add a second function `callDailySummary()` that POSTs to the daily-summary endpoint.
- Refactor main() to call both endpoints sequentially, logging results. Name the script more generically in the log output.

NOTE: Railway cron schedule must be updated to run daily (not just Mondays). This requires a manual Railway dashboard change: update the cron schedule from `0 8 * * 1` (Mondays 8am UTC) to `0 8 * * *` (daily 8am UTC). The weekly-digest endpoint already handles "no stats" gracefully, so calling it daily is safe — it just won't find 7-day-old messages on non-Mondays and will either send a slim digest or skip.

IMPORTANT: The weekly-digest endpoint should be updated to only send on Mondays. Add a day-of-week check at the top of the weekly-digest route: `if (new Date().getUTCDay() !== 1) return NextResponse.json({ skipped: true, reason: "not_monday" })`. This way the cron runs daily, daily-summary always executes, weekly-digest only executes on Mondays.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx tsc --noEmit src/lib/account-activity.ts src/app/api/notify/account-activity/route.ts src/components/emails/account-activity-email.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Account activity alerts send on login/settings events. Railway cron calls both daily-summary and weekly-digest. Weekly-digest auto-skips on non-Mondays.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run build` succeeds
- POST /api/notify/daily-summary with cron secret returns 200
- POST /api/notify/account-activity with valid session returns 200
- railway-cron/index.mjs calls both endpoints
- Weekly digest skips on non-Monday calls
</verification>

<success_criteria>
- Daily summary email aggregates and sends last-24-hour per-child stats
- Account activity alerts fire on login/settings changes (opt-in, defaults off)
- Railway cron triggers daily summary every day at 8am UTC
- Weekly digest continues to send only on Mondays
- All email templates visually consistent (distinct color themes per type)
- Notification history page shows new email types
</success_criteria>

<output>
After completion, create `.planning/phases/18-email-alert-system-transactional-email-service-for-automated-parent-notifications/18-03-SUMMARY.md`
</output>
