---
phase: 18-email-alert-system
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/(dashboard)/notifications/page.tsx
  - src/app/(dashboard)/notifications/loading.tsx
  - src/components/dashboard/notification-settings.tsx
  - src/components/dashboard/recipient-manager.tsx
autonomous: false
requirements: [EMAIL-SETTINGS-01]

must_haves:
  truths:
    - "Parents can add/remove email recipients from the Notifications page"
    - "Parents can toggle which alert types each recipient receives"
    - "The page shows both notification history and settings in a tabbed layout"
    - "UI follows shadcn/ui patterns with proper loading skeletons"
  artifacts:
    - path: "src/components/dashboard/recipient-manager.tsx"
      provides: "Add/remove email recipients with per-type toggle controls"
      min_lines: 80
    - path: "src/components/dashboard/notification-settings.tsx"
      provides: "Settings tab with recipient manager and alert type toggles"
      min_lines: 60
    - path: "src/app/(dashboard)/notifications/page.tsx"
      provides: "Tabbed layout: History tab + Settings tab"
      min_lines: 50
  key_links:
    - from: "src/components/dashboard/recipient-manager.tsx"
      to: "/api/notification-recipients"
      via: "fetch calls for CRUD"
      pattern: "fetch.*notification-recipients"
    - from: "src/app/(dashboard)/notifications/page.tsx"
      to: "src/components/dashboard/notification-settings.tsx"
      via: "Tab content rendering"
      pattern: "NotificationSettings"
---

<objective>
Build a proper Notification Settings UI so parents can manage who receives which alert types, replacing the simple checkbox toggles.

Purpose: Parents need to add both Mom and Dad as email recipients and control which alerts each person gets, all from within the admin dashboard.

Output: Enhanced /notifications page with tabbed History + Settings layout, recipient management with per-alert-type toggles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-email-alert-system-transactional-email-service-for-automated-parent-notifications/18-01-SUMMARY.md

@src/app/(dashboard)/notifications/page.tsx
@src/app/(dashboard)/notifications/loading.tsx
@src/components/dashboard/notification-prefs-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build recipient manager and notification settings components</name>
  <files>src/components/dashboard/recipient-manager.tsx, src/components/dashboard/notification-settings.tsx</files>
  <action>
Create `src/components/dashboard/recipient-manager.tsx` ("use client"):

1. Display a list of current recipients (fetched from GET /api/notification-recipients on mount).
2. Each recipient row shows:
   - Name + email (e.g. "Mom — mom@example.com")
   - Four toggle checkboxes: Safety Alerts, Weekly Digest, Daily Summary, Account Activity
   - A remove button (trash icon) with confirmation
3. "Add Recipient" section at bottom:
   - Two inputs: Name and Email
   - Add button (POST to /api/notification-recipients)
   - Optimistic add to list, revert on error with toast
4. Toggle changes PATCH /api/notification-recipients with { id, alerts: { [type]: boolean } }
5. Use optimistic updates for all mutations (toggle + add + remove). Show toast on error with revert.
6. Use shadcn Card for each recipient row, Button for actions, Input for the add form.
7. Show skeleton cards (3 placeholders) while loading recipients.
8. If no recipients exist, show a friendly empty state: "No recipients configured. Add an email address to start receiving alerts."

Create `src/components/dashboard/notification-settings.tsx` ("use client"):

Simple wrapper that renders:
1. A heading "Notification Recipients" with description "Add email addresses for parents who should receive alerts. Each recipient can opt into specific alert types."
2. The RecipientManager component
3. A note at the bottom: "Alert types: Safety Alerts (immediate), Daily Summary (daily at 8am UTC), Weekly Digest (Mondays at 8am UTC), Account Activity (on login/settings changes)"
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx tsc --noEmit src/components/dashboard/recipient-manager.tsx src/components/dashboard/notification-settings.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Recipient manager with add/remove/toggle functionality. Settings component wraps it with descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild notifications page with tabbed History + Settings layout</name>
  <files>src/app/(dashboard)/notifications/page.tsx, src/app/(dashboard)/notifications/loading.tsx</files>
  <action>
Update `src/app/(dashboard)/notifications/page.tsx`:

1. Add shadcn Tabs component with two tabs: "History" and "Settings".
2. "History" tab contains the existing notification history table (preserve all current code — badges, table, date formatting).
3. "Settings" tab renders the new NotificationSettings component (client component, so wrap in its own section).
4. Default to "History" tab.
5. Keep the page heading "Email Notifications" and summary badges above the tabs.
6. The page itself remains a server component — the Settings tab content is a client component island.

Update `src/app/(dashboard)/notifications/loading.tsx`:
- Show skeleton for the page heading, summary badges, and tab bar.
- Below tabs, show 6 skeleton table rows matching the history table shape.
- Follow CLAUDE.md skeleton rules: shimmer placeholders matching incoming content shape.

Remove the old `notification-prefs-toggle.tsx` dependency from wherever it was used (it was a simple checkbox on user rows — the new Settings tab replaces it). Do NOT delete the file yet (Plan 02 only modifies listed files), but the new UI supersedes it.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx tsc --noEmit src/app/\(dashboard\)/notifications/page.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Notifications page has tabbed layout. History tab shows email log. Settings tab shows recipient manager. Loading skeleton displays properly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify notification settings UI</name>
  <files>src/app/(dashboard)/notifications/page.tsx</files>
  <action>Deploy to Railway and verify the notification settings UI end-to-end. Run `railway up` from project root to deploy the latest build.</action>
  <verify>
    <automated>cd /data/home/KidAI && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: tabs work, recipients can be added/removed, alert toggles save, mobile responsive.</done>
  <what-built>Enhanced Notifications page with tabbed History + Settings layout, recipient management (add/remove email addresses, per-alert-type toggles)</what-built>
  <how-to-verify>
    1. Visit /notifications in the admin dashboard
    2. Verify two tabs appear: "History" and "Settings"
    3. History tab should show existing notification records (if any)
    4. Switch to Settings tab
    5. If no recipients configured, see empty state message
    6. Add a recipient: enter name "Mom" and email address, click Add
    7. Verify recipient appears with four alert type toggles
    8. Toggle one alert type off and on — verify it saves (no error toast)
    9. Add a second recipient "Dad" with a different email
    10. Remove one recipient — verify it disappears
    11. Check mobile responsiveness (recipient cards should stack vertically)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Notifications page loads with tabbed layout
- Settings tab shows recipient manager
- Add/remove/toggle recipients works with optimistic updates
- History tab preserves all existing notification log functionality
- Loading skeleton renders on page navigation
- Mobile responsive layout
</verification>

<success_criteria>
- Parents can manage notification recipients from the admin dashboard
- Each recipient has independent alert type preferences
- Tabbed UI cleanly separates history from settings
- All interactions provide immediate visual feedback (optimistic updates, toasts)
</success_criteria>

<output>
After completion, create `.planning/phases/18-email-alert-system-transactional-email-service-for-automated-parent-notifications/18-02-SUMMARY.md`
</output>
