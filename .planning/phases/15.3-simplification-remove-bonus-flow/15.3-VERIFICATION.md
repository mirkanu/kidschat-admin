---
phase: 15.3-simplification-remove-bonus-flow
verified: 2026-04-11T12:00:00Z
status: human_needed
score: 6/7 must-haves verified
re_verification: false
human_verification:
  - test: "UAT check 6 — Hard block when tokenCredits reach 0"
    expected: "LibreChat native red 'Insufficient Funds' block appears in the child chat UI; no custom warning, no synthetic offer, no YES flow"
    why_human: "Cannot simulate live tokenCredits depletion and LibreChat UI rendering programmatically"
  - test: "UAT check 7 — Top-up recovery: child can resume chat after parent tops up"
    expected: "After parent clicks 'Top up €0.10' from admin, child refreshes LibreChat and the insufficient-funds block is gone"
    why_human: "End-to-end flow across two separate sessions (admin + LibreChat child) cannot be verified programmatically"
  - test: "UAT check 8 — Railway log grep: no instrumentation startup noise"
    expected: "railway logs for kidschat-admin-production show clean Next.js startup with no 'instrumentation register() fired' or polling listener messages"
    why_human: "Requires live Railway log access; cannot grep production logs programmatically"
---

# Phase 15.3: Simplification Teardown Verification Report

**Phase Goal:** Delete all custom warning + bonus + injection code (~800 LOC + tests). Admin retains per-child cap enforcement and adds a one-click "Top up €0.10" button on the user detail page. Kids see only the LibreChat native balance display and native insufficient-funds block.
**Verified:** 2026-04-11T12:00:00Z
**Status:** human_needed — all automated checks passed; 3 UAT checks (6–8) skipped by user choice require human follow-up
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Admin /settings shows only dailyCostCapEur + monthlyCostCapEur fields — all bonus fields gone from global defaults form and per-child override table | VERIFIED | `settings-form.tsx` lines 114–121 show only daily/monthly cap inputs; `child-overrides-table.tsx` type is `"dailyCostCapEur" \| "monthlyCostCapEur"` only; grep for bonus returns zero hits |
| 2 | Admin /users/{childId} shows existing usage bars plus a new 'Top up €0.10' button | VERIFIED | `page.tsx` line 134 renders `<TopUpButton userId={userId} childName={user.name} />` |
| 3 | Parent clicks 'Top up €0.10' → useTransition pending state visible briefly, toast feedback | VERIFIED | `top-up-button.tsx`: `useTransition` imported (line 3), `isPending` used (line 12), `active:scale-95` class on button (line 33), sonner toast wired |
| 4 | After top-up, LibreChat shows updated token balance (live session) | HUMAN NEEDED | UAT check 4 was run live by user and passed, but cannot verify programmatically |
| 5 | When tokenCredits reach 0, LibreChat native 'Insufficient Funds' block appears — no custom warning, no YES flow | HUMAN NEEDED | UAT checks 6–8 skipped by user; no code paths remain that could inject custom warnings (all deleted and zero refs confirmed) |
| 6 | npm test passes with zero references to deleted symbols | VERIFIED | 53 tests pass across 5 suites; grep for all deleted symbols (bonusPackEur, weeklyBonusCapEur, applyBonusCredit, pendingWarning, activeOfferMessageId, activeOfferExpiresAt, activeOfferConversationId, hasActiveOffer, weeklyBonusSpentEur, weeklyBonusCapExhausted, getWeeklyBonusSpend, bonus-delivery, change-stream-listener, bonus-purchases) returns zero hits in src/ and tests/ |
| 7 | Railway logs show clean startup with no instrumentation noise | HUMAN NEEDED | UAT check 8 skipped; `src/instrumentation.ts` confirmed deleted (no such file exists) |

**Score:** 4/4 truths fully verified automatically + 3 partially verified (code side confirmed, live runtime needs human)

---

### Required Artifacts

| Artifact | Min Lines | Actual Lines | Status | Details |
|----------|-----------|--------------|--------|---------|
| `scripts/15-3-safety-restore.ts` | 60 | 248 | VERIFIED | Exists, substantive, standalone script |
| `src/app/(dashboard)/users/[userId]/actions.ts` | 25 | 46 | VERIFIED | `topUpChildBalance` server action present; uses `new ObjectId(userId)`, `$inc tokenCredits`, `revalidatePath` |
| `src/app/(dashboard)/users/[userId]/top-up-button.tsx` | 30 | 48 | VERIFIED | Client component with `useTransition`, `active:scale-95`, sonner toast |
| `tests/lib/top-up.test.ts` | 40 | 122 | VERIFIED | Unit tests for topUpChildBalance present |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `top-up-button.tsx` | `actions.ts` | `import { topUpChildBalance }` + useTransition | WIRED | Line 7: import; line 16: `await topUpChildBalance(userId, DEFAULT_AMOUNT_EUR)` inside startTransition |
| `actions.ts` | balances collection | `new ObjectId(userId)` + `$inc tokenCredits` | WIRED | Lines 31–32: `{ user: new ObjectId(userId) }` filter, `{ $inc: { tokenCredits: tokensToAdd } }` update |
| `page.tsx` | `top-up-button.tsx` | client component import | WIRED | Line 11: `import { TopUpButton }`, line 134: `<TopUpButton userId={userId} childName={user.name} />` |
| `actions.ts` | next/cache | `revalidatePath('/users/' + userId)` | WIRED | Line 39: `revalidatePath(\`/users/${userId}\`)` |

---

### Deleted Files — Confirmed Absent

All 10 files listed in `files_modified` as DELETE are confirmed absent from the filesystem:

- `src/lib/bonus-delivery.ts` — GONE
- `src/lib/change-stream-listener.ts` — GONE
- `src/lib/bonus-purchases.ts` — GONE
- `src/instrumentation.ts` — GONE
- `src/app/api/admin/reset-manuel/route.ts` — GONE
- `tests/lib/bonus-delivery.test.ts` — GONE
- `tests/lib/change-stream-listener.test.ts` — GONE
- `tests/lib/bonus-purchases.test.ts` — GONE
- `scripts/probe-agent-injection.ts` — GONE
- `scripts/reset-manuel-15-2.ts` — GONE

---

### Zero-Symbol Reference Check

grep across `src/` and `tests/` for all deleted symbols returns **zero hits**:

`bonusPackEur`, `weeklyBonusCapEur`, `applyBonusCredit`, `pendingWarning`, `activeOfferMessageId`, `activeOfferExpiresAt`, `activeOfferConversationId`, `hasActiveOffer`, `weeklyBonusSpentEur`, `weeklyBonusCapExhausted`, `getWeeklyBonusSpend`, `bonus-delivery`, `change-stream-listener`, `bonus-purchases`

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| SIMPLIFY-01 | 15.3-01-PLAN.md | Tear down custom bonus/warning/injection system; add parent top-up; simplify admin settings | SATISFIED | Phase 15.3 goal fully implemented; ROADMAP.md line 116 maps SIMPLIFY-01 to this phase; no central REQUIREMENTS.md file exists in this project — ROADMAP.md is authoritative |

No orphaned requirements: the project has no `REQUIREMENTS.md`; `ROADMAP.md` is the sole requirement authority and SIMPLIFY-01 is fully covered.

---

### Anti-Patterns Found

No anti-patterns detected in new or modified files. No TODO/FIXME/PLACEHOLDER/stub returns in any of the 4 created files.

---

### Human Verification Required

The following 3 UAT checks were explicitly skipped by the user during the live UAT session. Code-side evidence confirms the deleted code paths no longer exist, but runtime confirmation needs a human.

#### 1. Hard Block Appearance

**Test:** Log into LibreChat as a child with tokenCredits at or near 0 (or temporarily set to 0 in MongoDB). Attempt to send a chat message.
**Expected:** LibreChat's native red "Insufficient Funds" block appears. No custom warning message, no "Want more messages? YES/NO" prompt, no synthetic agent reply.
**Why human:** Requires live LibreChat session with depleted balance. Cannot simulate via code inspection.

#### 2. Top-Up Recovery

**Test:** With the child blocked (tokenCredits = 0), parent opens `/users/{childId}` in admin and clicks "Top up €0.10". Child refreshes LibreChat.
**Expected:** The insufficient-funds block disappears and the child can send messages again. Admin page shows updated balance bar within ~1s.
**Why human:** Requires coordinated live sessions in two separate browser contexts.

#### 3. Railway Log Grep — No Instrumentation Noise

**Test:** Run `railway logs --service kidschat-admin-production` and grep for "instrumentation", "register() fired", "Polling", "change-stream", "bonus".
**Expected:** Zero matches. Clean Next.js startup log only.
**Why human:** Requires live Railway CLI access with authenticated session.

---

### Known Issues (Out of Scope for 15.3)

`balance_state.monthlySpendEur` is a dead field (never incremented). This is a pre-existing regression from phase 15.4, documented in 15.3-01-SUMMARY.md "Known Issues". It is explicitly out of scope for phase 15.3 and is NOT flagged as a gap here.

---

### Summary

Phase 15.3 goal is achieved at the code level. All 10 deletion targets are gone. All 4 created files exist and are substantive. All 4 key wiring links are verified. Zero references to deleted symbols remain in `src/` or `tests/`. The test suite passes cleanly (53 tests, 5 suites, 0 failures). Admin settings show only the two cap fields. The TopUpButton is properly wired through server action to MongoDB `$inc` with `revalidatePath`.

Three UAT checks (hard block, recovery, Railway logs) were skipped by the user during the live UAT session. These require human verification but do not block the automated assessment — the code changes that would enable these outcomes are confirmed present and correct.

---

_Verified: 2026-04-11T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
