---
phase: 15.3-simplification-remove-bonus-flow
plan: 01
subsystem: ui, api, database
tags: [librechat, balance, top-up, teardown, simplification, railway]

# Dependency graph
requires:
  - phase: 15-safety-alert-extension-rate-limiting
    provides: bonus/warning system that was deleted in this phase
  - phase: 15.2-synthetic-message-rendering
    provides: agent injection approach that was superseded by this teardown
provides:
  - Parent "Top up €0.10" button on /users/{childId} admin page
  - Clean admin settings (only dailyCostCapEur + monthlyCostCapEur)
  - LibreChat native balance enforcement (hard block at 0) with no custom overlay
  - Zero injection, polling, or synthetic message code in codebase
affects:
  - Any future phase touching balance, usage limits, or admin user detail page

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Server action (topUpChildBalance) with useTransition pending state on client button
    - Direct $inc on balances.tokenCredits via ObjectId — no intermediate lib abstraction

key-files:
  created:
    - scripts/15-3-safety-restore.ts
    - src/app/(dashboard)/users/[userId]/actions.ts
    - src/app/(dashboard)/users/[userId]/top-up-button.tsx
    - tests/lib/top-up.test.ts
  modified:
    - src/lib/budget.ts (pruned bonus fields)
    - src/lib/settings.ts (pruned bonus defaults)
    - src/lib/weekly-digest.ts (pruned bonus stats)
    - src/components/emails/weekly-digest-email.tsx (pruned bonus section)
    - src/app/(dashboard)/settings/page.tsx (removed bonus fields)
    - src/app/(dashboard)/settings/settings-form.tsx (removed bonus inputs)
    - src/app/(dashboard)/settings/actions.ts (removed bonus handling)
    - src/app/(dashboard)/settings/child-overrides-table.tsx (removed bonus columns)
    - src/app/(dashboard)/settings/types.ts (pruned bonus types)
    - src/app/(dashboard)/users/[userId]/page.tsx (added TopUpButton)
    - tests/lib/budget.test.ts (removed bonus assertions)
    - tests/lib/rate-limits.test.ts (removed bonus references)

key-decisions:
  - "LibreChat native 'Insufficient Funds' red block replaces all custom 70% warning + YES confirmation flow"
  - "Parent top-up is manual one-click €0.10 increment; no self-service child flow"
  - "monthlySpendEur field in balance_state is a pre-existing dead field (never incremented) — out of scope for 15.3, flagged for follow-up"
  - "instrumentation.ts polling loop deleted entirely — no more setInterval listener at startup"

patterns-established:
  - "Top-up pattern: server action $inc tokenCredits + revalidatePath, client useTransition + sonner toast"

requirements-completed: [SIMPLIFY-01]

# Metrics
duration: ~90min (multi-session)
completed: 2026-04-11
---

# Phase 15.3 Plan 01: Simplification Teardown Summary

**Deleted ~800 LOC of custom bonus/warning/injection code, pruned bonus fields from settings and admin UI, and added a one-click parent top-up button — leaving LibreChat's native hard block as sole balance enforcement**

## Performance

- **Duration:** ~90 min (multi-session including UAT)
- **Started:** 2026-04-11T09:00:00Z (approx)
- **Completed:** 2026-04-11
- **Tasks:** 4 (3 auto + 1 UAT)
- **Files modified:** ~22 (10 deleted, 12 pruned/modified, 4 created)

## Accomplishments

- Deleted 10 source files: bonus-delivery.ts, change-stream-listener.ts, bonus-purchases.ts, instrumentation.ts, admin/reset-manuel route, 3 test files, 2 scripts
- Pruned bonus-related fields and UI from 12 existing files (settings page, budget lib, weekly-digest, email template, types, tests)
- Added `topUpChildBalance` server action + `TopUpButton` client component with useTransition pending state, active:scale-95 tactile feedback, and sonner toast — deployed to Railway
- Safety restore script (15-3-safety-restore.ts) ran successfully to clean stuck 15.2 state before teardown

## Task Commits

1. **Task 1: Safety restore — clean stuck 15.2 state** - `5ef01ad` (fix)
2. **Task 2: Delete custom bonus/warning system + prune fields** - `638d3d0` (refactor)
3. **Task 3: Add parent top-up button + deploy to Railway** - `40b3551` (feat)
4. **Task 4: Human UAT** - approved (partial, 5/8 checks; 3 skipped by user)

## Files Created/Modified

**Created:**
- `scripts/15-3-safety-restore.ts` — One-shot cleanup: restores agent instructions, unsets pendingWarning fields, drops bonus_purchases collection
- `src/app/(dashboard)/users/[userId]/actions.ts` — `topUpChildBalance` server action, $inc tokenCredits with ObjectId, revalidatePath
- `src/app/(dashboard)/users/[userId]/top-up-button.tsx` — Client component with useTransition, active:scale-95, sonner toast
- `tests/lib/top-up.test.ts` — Unit tests validating userId format, amount bounds, $inc call

**Deleted:**
- `src/lib/bonus-delivery.ts`, `src/lib/change-stream-listener.ts`, `src/lib/bonus-purchases.ts`
- `src/instrumentation.ts` (polling loop)
- `src/app/api/admin/reset-manuel/route.ts`
- `tests/lib/bonus-delivery.test.ts`, `tests/lib/change-stream-listener.test.ts`, `tests/lib/bonus-purchases.test.ts`
- `scripts/probe-agent-injection.ts`, `scripts/reset-manuel-15-2.ts`

**Pruned (bonus fields/UI removed):**
- `src/lib/budget.ts`, `src/lib/settings.ts`, `src/lib/weekly-digest.ts`
- `src/components/emails/weekly-digest-email.tsx`
- `src/app/(dashboard)/settings/` — page.tsx, settings-form.tsx, actions.ts, child-overrides-table.tsx, types.ts
- `src/app/(dashboard)/users/[userId]/page.tsx`
- `tests/lib/budget.test.ts`, `tests/lib/rate-limits.test.ts`

## Decisions Made

- Replaced entire custom 70% warning + bonus offer + YES-confirmation flow with LibreChat's native "Insufficient Funds" red block — zero custom overlay
- Parent top-up is deliberate one-click manual action (€0.10 increment) rather than any automated refill
- Admin settings page now shows only `dailyCostCapEur` and `monthlyCostCapEur` — all bonus pack / weekly cap / message template fields removed
- instrumentation.ts deleted: no polling loop runs at Next.js startup

## Deviations from Plan

None — plan executed exactly as written.

## UAT Status

**Partial pass: 5/8 checks passed; 3 explicitly skipped by user (not failures)**

Passed:
1. Admin /settings — only Daily + Monthly cap fields present
2. Admin /users/{sebastianId} — account card, usage bars, Top up button render
3. Top up click — pending state visible + toast "Topped up €0.10 for Sebastian"
4. LibreChat balance reflects top-up — token count updated
5. Kid chat — normal reply, no SYSTEM INSTRUCTION text, no synthetic offer

Skipped (user choice):
6. Hard block at 0 balance (insufficient-funds native block)
7. Top up → recovery flow
8. Railway logs grep for clean startup

## Known Issues / Follow-ups

**Pre-existing bug — monthlySpendEur dead field (out of scope for 15.3):**
- Admin /users/{childId} "This month" usage bar shows 0% even when the child has spent tokens
- Root cause: `balance_state.monthlySpendEur` is never incremented anywhere in the codebase — it was a placeholder field from phase 15-04 that never received write logic
- This is NOT a regression from 15.3; it predates this plan
- Fix: a follow-up `/gsd:quick` task should add `monthlySpendEur` increment logic to the cost-tracking path
- The "Today" bar (dailySpendEur) works correctly (showed 97% in UAT)

## Issues Encountered

None beyond the pre-existing monthly-bar bug documented above.

## User Setup Required

None — Railway deploy was performed automatically via `railway up` in Task 3.

## Next Phase Readiness

- Balance enforcement is simplified: LibreChat native hard block + admin top-up only
- Admin settings page is clean — two fields only
- No injection, no polling, no bonus collection
- Ready for any follow-up quick task on the monthly-bar bug or future phases

---
*Phase: 15.3-simplification-remove-bonus-flow*
*Completed: 2026-04-11*
