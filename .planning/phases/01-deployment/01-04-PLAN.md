---
phase: 01-deployment
plan: 04
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified: []
autonomous: false
requirements:
  - DEPL-04

must_haves:
  truths:
    - "LibreChat loads at the Railway public URL and the chat interface is visible"
    - "Sending a message receives a response from Claude Haiku 4.5"
    - "Navigating to /register shows an error, not a registration form"
    - "Social login options are absent from the login page"
    - "The GitHub Gist hosts librechat.yaml and CONFIG_PATH points to it — no API keys visible in Gist"
    - "All 3 Railway services are healthy"
  artifacts:
    - path: "(Railway project)"
      provides: "Three healthy services: LibreChat, MongoDB, Meilisearch"
    - path: "(GitHub Gist: librechat.yaml)"
      provides: "Public config file, no secrets"
  key_links:
    - from: "Phase 1 success criteria (ROADMAP.md)"
      to: "Each item in the checklist below"
      via: "Manual verification run by parent"
---

<objective>
Run the complete Phase 1 verification checklist to confirm every success criterion is met before proceeding to Phase 2.

Purpose: Phase 2 safety configuration requires a solid, verified baseline. This plan systematically checks every Phase 1 requirement so Phase 2 can begin with confidence. Any failures discovered here are cheaper to fix now than after Phase 2 is built on top of them.

Output: A SUMMARY.md that either confirms Phase 1 is complete and Phase 2 is ready to begin, or lists specific failures with remediation steps.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@.planning/phases/01-deployment/01-01-SUMMARY.md
@.planning/phases/01-deployment/01-02-SUMMARY.md
@.planning/phases/01-deployment/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Run the complete Phase 1 verification checklist</name>
  <files>N/A — browser and Railway dashboard verification</files>
  <action>
    Systematically verify all 5 Phase 1 success criteria from ROADMAP.md using the 10-item checklist below. This is the gate check before Phase 2 begins.
  </action>
  <what-built>
    Plans 01-03 have deployed LibreChat Lite on Railway, created an admin account, set all lockdown environment variables, and created a GitHub Gist with CONFIG_PATH wired. This is the final gate check before Phase 2 begins.
  </what-built>
  <how-to-verify>
    Work through each item. Mark each PASS or FAIL. If any item FAILS, note what you see.

    DEPLOYMENT CHECKS:
    [ ] 1. Navigate to the Railway public URL → LibreChat loads and shows a login form (not a blank page or error)
    [ ] 2. Railway dashboard shows ALL 3 services with healthy/green status (LibreChat, MongoDB, Meilisearch)
    [ ] 3. Railway plan is confirmed as Hobby ($5/month) — NOT free/trial

    API AND MODEL CHECKS:
    [ ] 4. Log in as admin → open a new chat → send "Hello, what model are you?"
         EXPECTED PASS: You receive a response mentioning Claude or Haiku within a few seconds. No "API key invalid" errors.
    [ ] 5. Confirm no model picker or endpoint dropdown is visible in the chat header after logging in

    REGISTRATION LOCKDOWN CHECKS (use INCOGNITO for these):
    [ ] 6. In incognito: navigate to [Railway URL]/register
         EXPECTED PASS: Shows an error message, "registration is not allowed", or redirects to /login — NOT a form with email/password fields
    [ ] 7. In incognito: navigate to [Railway URL]/login
         EXPECTED PASS: Shows ONLY email/password fields — NO Google, GitHub, Discord, or other social login buttons

    GITHUB GIST CONFIG CHECKS:
    [ ] 8. Open your GitHub Gist URL in incognito (not logged into GitHub)
         EXPECTED PASS: The librechat.yaml content is visible and contains NO strings starting with sk-ant- or any other credentials
    [ ] 9. In Railway Variables: CONFIG_PATH is set to a URL in the format:
         https://gist.githubusercontent.com/USERNAME/GIST_ID/raw/librechat.yaml
         (must end with /raw/librechat.yaml — NOT /raw/ with no filename)
    [ ] 10. In Railway LibreChat logs: no "Custom config file missing" or "YAML format invalid" errors appear

    Report results in the format:
    PASS: [item number] — [brief note]
    FAIL: [item number] — [what you saw instead]
  </how-to-verify>
  <verify>All 10 checklist items pass — maps to all 5 Phase 1 success criteria in ROADMAP.md</verify>
  <done>Phase 1 fully verified — LibreChat deployed, API connected, registration closed, Gist wired without secrets, all services healthy</done>
  <resume-signal>
    Report all 10 check results. If all 10 PASS, type "phase 1 complete". If any FAIL, describe the failure and we will create a remediation plan.
  </resume-signal>
</task>

</tasks>

<verification>
All 10 checklist items from Task 1 must pass. This maps directly to the Phase 1 success criteria in ROADMAP.md:
- Criteria 1: Checklist items 1-2 (LibreChat loads at URL, 3 services healthy)
- Criteria 2: Checklist item 4 (message receives Claude Haiku 4.5 response)
- Criteria 3: Checklist item 6 (/register shows error, not form)
- Criteria 4: Checklist item 7 (no social login buttons on /login)
- Criteria 5: Checklist items 8-9 (Gist hosts config, CONFIG_PATH correct, no API keys in Gist)
</verification>

<success_criteria>
All 10 checklist items verified as PASS (DEPL-04 — baseline verified). Phase 1 complete. Phase 2 (Safety Configuration) ready to begin.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment/01-04-SUMMARY.md` with:
- All 10 checklist results (PASS/FAIL for each)
- Overall Phase 1 status: COMPLETE or BLOCKED
- If BLOCKED: list of failing items and remediation steps taken
- Railway URL (confirm for Phase 2 reference)
- GitHub Gist URL (confirm for Phase 2 reference)
- Date Phase 1 completed
- Note: "Phase 2 (Safety Configuration) is ready to begin"

Also update `.planning/REQUIREMENTS.md` — mark the following requirements as complete:
- DEPL-01, DEPL-02, DEPL-03, DEPL-04
- AUTH-01, AUTH-02, AUTH-03, AUTH-04
- CONF-01, CONF-02, CONF-03
</output>
