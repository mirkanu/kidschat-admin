---
phase: 01-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - DEPL-03
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Sending a message in LibreChat receives a real response from Claude Haiku 4.5"
    - "Navigating to /register shows an error or redirect, NOT a registration form"
    - "The login page shows no Google, GitHub, or Discord login buttons"
    - "Only manually created accounts can authenticate"
  artifacts:
    - path: "(Railway LibreChat service environment variables)"
      provides: "ANTHROPIC_API_KEY, ENDPOINTS, all ALLOW_* lockdown vars"
  key_links:
    - from: "LibreChat service"
      to: "Anthropic API"
      via: "ANTHROPIC_API_KEY env var → outbound HTTPS to api.anthropic.com"
    - from: "Registration lockdown"
      to: "ALLOW_REGISTRATION=false + ALLOW_SOCIAL_LOGIN=false + ALLOW_SOCIAL_REGISTRATION=false"
      via: "All three env vars required — setting only one is insufficient"
---

<objective>
Configure all Railway environment variables that connect the Anthropic API and close every registration and social login path.

Purpose: This plan delivers two things: (1) the Anthropic API connection so Claude Haiku 4.5 can respond to messages, and (2) the complete registration lockdown so no unauthorized user can create an account. Both must happen before the app is shared with children.

Output: A redeployed LibreChat service where the API key is wired, all registration paths are closed, and a test message receives a real AI response.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-deployment/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Set all environment variables in Railway LibreChat service</name>
  <files>N/A — Railway dashboard action</files>
  <action>
    Add all required environment variables to the Railway LibreChat service. These variables configure the Anthropic API connection, restrict the endpoint to Anthropic only, and close all self-registration and social login paths. All variables must be set before triggering a single redeploy.

    REQUIRED — API and model:
    - ANTHROPIC_API_KEY = [your Anthropic API key, starts with sk-ant-]
    - ANTHROPIC_MODELS = claude-haiku-4-5
    - ENDPOINTS = anthropic

    REQUIRED — Registration lockdown (all three are required — setting only one is insufficient, per PITFALLS.md):
    - ALLOW_REGISTRATION = false
    - ALLOW_SOCIAL_LOGIN = false
    - ALLOW_SOCIAL_REGISTRATION = false

    REQUIRED — Allow admin-created accounts to log in without email verification:
    - ALLOW_UNVERIFIED_EMAIL_LOGIN = true

    RECOMMENDED — Security and operational:
    - TRUST_PROXY = 1
    - NO_INDEX = true
    - ALLOW_SHARED_LINKS_PUBLIC = false
    - MIN_PASSWORD_LENGTH = 12

    Do NOT add: OPENAI_API_KEY, GOOGLE_KEY, or other provider keys — omitting them removes those endpoints entirely. Do NOT put ANTHROPIC_API_KEY in librechat.yaml.

    Verify auto-generated variables from the template are present: MONGO_URI, JWT_SECRET, CREDS_KEY, CREDS_IV, MEILI_MASTER_KEY (Railway should have set these automatically).
  </action>
  <what-to-do>
    In the Railway dashboard, navigate to your LibreChat service → Variables tab. Add ALL of the variables listed in the action section above. Set them all before saving — Railway will trigger a single redeploy when you save. Wait for the LibreChat service to show healthy/green status (1-3 minutes).
  </what-to-do>
  <verify>Railway redeploy completes successfully, LibreChat service shows healthy in dashboard</verify>
  <done>All environment variables set in Railway, service redeployed and healthy, no startup errors in logs</done>
  <resume-signal>
    Type "variables set" and confirm: "Redeployment completed and LibreChat shows healthy in Railway dashboard."
  </resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify API connection and registration lockdown</name>
  <files>N/A — browser verification</files>
  <action>
    Confirm the Anthropic API key is accepted by sending a test message, and confirm all three registration lockdown variables are active by testing /register and /login in incognito.
  </action>
  <what-built>
    All environment variables have been set and the LibreChat service has redeployed with: Anthropic API key wired, model locked to Claude Haiku 4.5, and all registration paths closed.
  </what-built>
  <how-to-verify>
    Run ALL of these checks:

    1. API connection test:
       - Log in to LibreChat with your admin account
       - Open a new chat conversation
       - Type "Say hello in 5 words" and send
       - EXPECTED: You receive a short response within a few seconds. If you see an error about API keys, check that ANTHROPIC_API_KEY is set correctly in Railway Variables.

    2. Registration lockdown test (use INCOGNITO):
       - Open a new browser tab in INCOGNITO/PRIVATE mode
       - Navigate to [your Railway URL]/register
       - EXPECTED: You see an error page, "registration is not allowed" message, or a redirect to /login — NOT a registration form with email/password fields.
       - Still in incognito: navigate to [your Railway URL]/login
       - EXPECTED: You see ONLY an email/password form — NO Google, GitHub, or Discord login buttons.

    3. Service health check:
       - Return to Railway dashboard
       - Confirm all 3 services (LibreChat, MongoDB, Meilisearch) show healthy/green status

    If any check fails:
    - API error: Verify ANTHROPIC_API_KEY is set correctly with no extra spaces
    - Registration form still shows: Confirm ALLOW_REGISTRATION=false is set (use lowercase false, not "False") and the service has fully redeployed
    - Social buttons still show: Confirm ALLOW_SOCIAL_LOGIN=false is set
  </how-to-verify>
  <verify>API responds to test message, /register does not show form, /login has no social buttons, all 3 services healthy</verify>
  <done>Claude Haiku 4.5 responds to chat messages, all registration and social login paths confirmed closed</done>
  <resume-signal>
    Type "verified" if all 3 checks pass. Or describe what failed so the plan can be adjusted.
  </resume-signal>
</task>

</tasks>

<verification>
All checks from Task 2 must pass:
- Sending a chat message returns a response from Claude Haiku 4.5 (DEPL-03)
- /register does not show a registration form (AUTH-01)
- Login page has no social login buttons (AUTH-02, AUTH-03)
- Only the admin account created in Plan 01 can log in (AUTH-04)
</verification>

<success_criteria>
- ANTHROPIC_API_KEY configured, Claude Haiku 4.5 responds to messages (DEPL-03)
- ALLOW_REGISTRATION=false — /register returns error, not a form (AUTH-01)
- ALLOW_SOCIAL_REGISTRATION=false — no OAuth signup path exists (AUTH-02)
- ALLOW_SOCIAL_LOGIN=false — no social login buttons on /login (AUTH-03)
- Only manually created accounts can log in (AUTH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment/01-02-SUMMARY.md` with:
- Confirmation that all env vars are set
- Test message result (model responded successfully)
- Registration lockdown verification result (/register and /login behavior)
- Any issues encountered and how they were resolved
</output>
