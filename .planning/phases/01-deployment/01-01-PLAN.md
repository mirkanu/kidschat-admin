---
phase: 01-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - DEPL-01
  - DEPL-02

must_haves:
  truths:
    - "LibreChat is running and accessible at a Railway public HTTPS URL"
    - "MongoDB and Meilisearch services show healthy in Railway dashboard"
    - "A single admin account exists and can log in successfully"
    - "The Railway project is on the Hobby plan with 5 GB volumes"
  artifacts:
    - path: "(Railway project: LibreChat service, public URL)"
      provides: "Running LibreChat instance with 3 provisioned services"
    - path: "(MongoDB service in Railway project)"
      provides: "Persistent storage for accounts and conversations"
    - path: "(Meilisearch service in Railway project)"
      provides: "Conversation search index"
  key_links:
    - from: "LibreChat service"
      to: "MongoDB"
      via: "MONGO_URI env var (auto-wired by Railway template)"
    - from: "LibreChat service"
      to: "Meilisearch"
      via: "MEILI_HOST + MEILI_MASTER_KEY env vars (auto-wired)"
    - from: "Browser"
      to: "LibreChat service"
      via: "Railway public HTTPS URL"
---

<objective>
Deploy the LibreChat Lite Railway template and create the first admin account.

Purpose: Establish the running infrastructure that all subsequent configuration depends on. MongoDB and Meilisearch must be healthy before any YAML configuration or account creation can succeed.

Output: A live LibreChat instance on Railway with a working admin login. No safety configuration yet — this is the baseline platform verification step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Deploy LibreChat Lite Railway template and upgrade to Hobby plan</name>
  <files>N/A — Railway dashboard action</files>
  <action>
    Deploy the LibreChat Lite template on Railway and confirm all 3 services are healthy. Upgrade to Hobby plan if not already active (required for 5 GB MongoDB volume — the free/trial plan's 500 MB limit causes crash loops).
  </action>
  <what-to-do>
    1. Open https://railway.com/deploy/_fTxzh in your browser (LibreChat Lite template — NOT the full RAG template).
    2. Click "Deploy Now" and connect to your Railway account when prompted.
    3. Railway will provision 3 services: LibreChat, MongoDB, Meilisearch. Wait for all three to show green/healthy status in the Railway dashboard (this may take 3-5 minutes).
    4. CRITICAL: Verify you are on the Hobby plan ($5/month) BEFORE using the app. The free/trial plan has a 500 MB MongoDB volume limit that will cause crash loops. Go to Railway Dashboard → your account → Billing → confirm Hobby plan is active. If not, upgrade now.
    5. Once all services are healthy, locate the LibreChat service and click "Settings" to find the auto-generated public URL (format: `https://xxx.up.railway.app`). Copy this URL.
    6. Open the URL in your browser to confirm LibreChat loads and shows a login/register page.
  </what-to-do>
  <verify>All 3 Railway services show healthy, LibreChat loads at the public URL, Hobby plan confirmed</verify>
  <done>Railway project provisioned with 3 healthy services on Hobby plan, LibreChat accessible via public HTTPS URL</done>
  <resume-signal>
    Type "deployed" and paste the Railway public URL (e.g. https://xxx.up.railway.app). Also confirm: "All 3 services are healthy in Railway dashboard" and "Hobby plan is confirmed".
  </resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Create the admin account via the registration form</name>
  <files>N/A — browser action against Railway URL</files>
  <action>
    Create the first admin account via the LibreChat registration UI while ALLOW_REGISTRATION is still true (the Railway template default). This window closes once Plan 02 sets ALLOW_REGISTRATION=false.
  </action>
  <what-to-do>
    The Railway template deploys with ALLOW_REGISTRATION=true by default. Use this window to create the first admin account (the first account created becomes admin).

    IMPORTANT: Do this NOW, before environment variables are set in Plan 02 — once ALLOW_REGISTRATION=false is set and redeployed, the form disappears.

    1. Navigate to the Railway public URL (from Task 1).
    2. Click "Register" or navigate to /register.
    3. Create your admin account:
       - Email: your email address (e.g. parent@family.com)
       - Password: a strong password (minimum 12 characters — use a password manager)
       - Display name: your name
    4. Complete registration. You should be logged in and see the LibreChat chat interface.
    5. Send one test message using whatever default model appears — confirm you receive a response. (If it fails with an API key error, that is expected — you have not set ANTHROPIC_API_KEY yet. Just confirm the UI loads and the chat interface is visible.)
    6. Log out.

    Note: Emily-Kate's admin account will be created via CLI in Phase 3. Only YOUR account is created here via UI registration.
  </what-to-do>
  <verify>Admin account exists in LibreChat, login works, chat interface is visible after login</verify>
  <done>One admin account created with strong password, user is logged out, account email recorded for Plan 02 reference</done>
  <resume-signal>
    Type "admin created" and confirm: "I am logged out and the admin account email is: [your email]"
  </resume-signal>
</task>

</tasks>

<verification>
Before proceeding to Plan 02:
- Railway dashboard shows all 3 services (LibreChat, MongoDB, Meilisearch) with healthy/green status
- LibreChat loads at the Railway public HTTPS URL
- Admin account login works (can log in, see chat interface, log out)
- Railway Hobby plan is confirmed ($5/month, 5 GB volumes)
</verification>

<success_criteria>
- LibreChat Lite template provisioned on Railway (DEPL-01)
- LibreChat accessible at a Railway public HTTPS URL (DEPL-02)
- All 3 Railway services healthy (MongoDB 5 GB volume on Hobby plan)
- One admin account exists and can authenticate
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment/01-01-SUMMARY.md` with:
- Railway public URL
- Admin account email
- Confirmation that all 3 services are healthy
- Railway plan confirmed (Hobby)
- Any issues encountered during deploy
</output>
