---
phase: 20
plan: 06
type: execute
wave: 5
depends_on: [20-05]
files_modified:
  - .planning/phases/20-image-search-research-poc/20-DECISIONS.md (new)
  - .planning/STATE.md (append Phase 20 decisions)
  - (Railway) env var CONFIG_PATH on service librechat (REVERT to PROD_CONFIG_PATH_PRE)
autonomous: false
requirements_addressed: []

must_haves:
  truths:
    - "`20-DECISIONS.md` exists with FOUR locked decisions — Tool Mechanism, Search Provider, Hotlink Mitigation, Test Mode Architecture — each with: recommendation, rationale, rejected alternatives, confidence level, at least one RESEARCH.md citation"
    - "Railway `CONFIG_PATH` on service librechat is reverted to PROD_CONFIG_PATH_PRE (byte-for-byte match with what was captured in Plan 02)"
    - "LibreChat redeployed on the production Gist and `/health` returns 200"
    - "Penelope/Sebastian see no change in production — if they log in now, no Image Search preset appears"
    - "Dev Gist is PRESERVED (not deleted) for Phase 21 reuse — its URL and SHA remain recorded in 20-02-GIST-REFS.md"
    - "STATE.md is updated with the four Phase 20 decisions so Phase 21/22 planners inherit them"
    - "Existing 65-test jest suite still passes (`npm test` green)"
  artifacts:
    - path: ".planning/phases/20-image-search-research-poc/20-DECISIONS.md"
      provides: "Canonical record of the four Phase 20 architectural decisions, consumed by Phase 21 and Phase 22 planners"
      contains: "## Tool Mechanism, ## Search Provider, ## Hotlink Mitigation, ## Test Mode Architecture (four H2 sections)"
    - path: ".planning/STATE.md"
      provides: "Phase-20 decisions appended to Accumulated Context so downstream phases inherit"
  key_links:
    - from: "20-DECISIONS.md"
      to: "Phase 21 planner"
      via: "Each decision cites the RESEARCH.md section that backs it, plus any UAT findings from Plan 05 that shifted recommendation"
      pattern: "Four H2 sections, each with the five required sub-items"
    - from: "Railway CONFIG_PATH"
      to: "PROD_CONFIG_PATH_PRE (recorded in 20-02-GIST-REFS.md)"
      via: "exact-string revert"
      pattern: "post-revert value string-equals PROD_CONFIG_PATH_PRE"
---

<objective>
Lock the four Phase 20 architectural decisions into `20-DECISIONS.md` (the primary deliverable that unblocks Phase 21 and Phase 22), revert Railway `CONFIG_PATH` to the production Gist so kids' production experience is restored, and update STATE.md. Preserve the dev Gist for Phase 21 reuse.

Purpose: Phase 20 is a research+POC phase — its contract-to-downstream is this decisions document. The revert closes Pitfall 5 (forgetting to re-point CONFIG_PATH). Any caveats from Plan 05 UAT feed forward as Phase 21 followups.
Output: Locked decisions document + reverted production + updated STATE + preserved dev Gist reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-image-search-research-poc/20-CONTEXT.md
@.planning/phases/20-image-search-research-poc/20-RESEARCH.md
@.planning/phases/20-image-search-research-poc/20-VALIDATION.md
@.planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md
@.planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md
@.planning/phases/20-image-search-research-poc/20-UAT.md

<interfaces>
<!-- 20-DECISIONS.md required shape. Each of the four sections MUST contain all five sub-items. -->

```markdown
# Phase 20 — Locked Decisions

Date: <date>  ·  UAT Verdict: <APPROVED / APPROVED-WITH-CAVEATS>  ·  Informs: Phase 21, Phase 22

## Tool Mechanism
**Decision:** MCP server (Brave official `brave/brave-search-mcp-server`, streamable-http transport) declared in `librechat.yaml` and attached to the Image Search agent via its `tools: ["brave_image_search"]` array.

**Rationale:** <from RESEARCH.md D-13a + any UAT evidence>
**Rejected alternatives:**
- LibreChat built-in `web_search` — text-only; image not supported (RESEARCH.md §Headline Findings #1)
- Custom OpenAPI action — Agent Builder friction + OpenAPI spec maintenance
- LibreChat fork — incompatible with no-fork constraint

**Confidence:** HIGH (OQ1 resolved PASS in Plan 04 smoke log / OR pivot X taken — cite)
**Citation:** RESEARCH.md §Decision Recommendations D-13a; 20-04-SMOKE-LOG.md Verdict

## Search Provider
**Decision:** Brave Search API (accessed via Brave MCP server).
**Rationale:** <from RESEARCH.md D-13b + Plan 05 UAT safety sweep results>
**Rejected alternatives:** Serper (Google-SERP rescrape, weaker independent SafeSearch); Google CSE (weak SafeSearch on edge queries); Bing (being retired); Kagi (no family-tier API pricing)
**Confidence:** HIGH (20/20 UAT queries pass target; adversarial sweep clean)
**Citation:** RESEARCH.md §Decision Recommendations D-13b; 20-UAT.md Overall Verdict

## Hotlink Mitigation
**Decision:** <TRUST BRAVE CDN — NO PROXY IN PHASE 21 / OR BUILD PROXY IN PHASE 21 based on hotlink sample from 20-UAT.md>
**Rationale:** Brave thumbnails come from `imgs.search.brave.com` (Brave's own CDN); 5/5 hotlink sample in Plan 05 confirmed HTTP 200 / OR X/5 flaked, proxy needed.
**Rejected alternatives:** Proxy-all (unnecessary at current rate); accept-broken (poor UX)
**Confidence:** HIGH
**Citation:** RESEARCH.md §Decision Recommendations D-13c; 20-UAT.md Hotlink Sample

## Test Mode Architecture
**Decision:** Option B — Re-implement the image-search tool server-side in the admin dashboard using `@anthropic-ai/sdk` tool_use, sharing the Phase 21 image-search service.

**Rationale (each blocker cited to RESEARCH.md line number):**
- (i) **LibreChat JWT/cookie forgery required** — no clean mechanism in v0.8.4 for minting a service-account JWT with admin `_id` that passes LibreChat's API auth. See `20-RESEARCH.md:509-512` (OQ2) and `20-RESEARCH.md:495` (Assumption A5: "Option A requires auth-token machinery LibreChat does not expose").
- (ii) **SSE stream-format translation** — LibreChat streams its own SSE format; admin dashboard's existing Anthropic-SDK stream handler expects a different shape. Pure complexity tax. See `20-RESEARCH.md:230` ("LibreChat SSE → admin-dashboard stream translation: Re-implement via Anthropic SDK (Option B) — SDK already used in v2.1, v2.2, v2.4; translation code is pure complexity tax").
- (iii) **Upstream-stability coupling** — Option A binds admin release cadence to LibreChat upstream changes. See `20-RESEARCH.md:68` (Test Mode comparison row: "Option A = zero drift but requires LibreChat auth forgery + stream translation + upstream-stability bet. Not worth it for a 20-line router.").

Agent is a ~20-line router — duplication is cheap; parity drift mitigated by shared safety libraries and Phase 22 side-by-side UAT (TESTMODE-03).

**Rejected alternative:** Option A (proxy through LibreChat `/api/agents/chat`) — flipped from CONTEXT.md D-14 "lean toward A" after the three blockers cited above were surfaced during research. CONTEXT.md D-14's exact condition ("prefer Option A unless research surfaces a blocking issue") is satisfied by blockers (i)+(ii)+(iii).

**Confidence:** MEDIUM (upgrade to HIGH during Phase 22 source-code pass per `20-RESEARCH.md:399`)
**Citation:** `20-RESEARCH.md:392-399` (§Decision Recommendations D-14); `20-RESEARCH.md:68`, `20-RESEARCH.md:230`, `20-RESEARCH.md:495`, `20-RESEARCH.md:509-512` (specific blocker evidence)

---

## Phase 21 Followups (from Plan 05 caveats)
- <followup 1>
- <followup 2>

## Phase 22 Followups
- Source-code verify LibreChat v0.8.4's auth mechanism to finalize Option-B confidence upgrade
- Reuse Phase 21's image-search service for Test Mode tool execution
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 20-06-01: Write 20-DECISIONS.md + update STATE.md</name>
  <files>
    - .planning/phases/20-image-search-research-poc/20-DECISIONS.md (new)
    - .planning/STATE.md (append)
  </files>
  <read_first>
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Decision Recommendations (D-13a/b/c and D-14 — recommendation source)
    - .planning/phases/20-image-search-research-poc/20-UAT.md (verdict + caveats from Plan 05)
    - .planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md (OQ1 verdict, informs Tool Mechanism confidence)
    - .planning/phases/20-image-search-research-poc/20-CONTEXT.md (D-13, D-14 — the questions being answered)
  </read_first>
  <action>
    1. Compose `20-DECISIONS.md` using the shape from <interfaces>. Fill in each section:
       - **Tool Mechanism:** Copy the recommendation text. For "Rationale" quote 1-2 sentences from RESEARCH.md §D-13a and add one sentence citing the Plan 04 OQ1 outcome (e.g., "Plan 04 smoke log verdict PASS confirms streamable-http wire compat").
       - **Search Provider:** Copy recommendation. For "Rationale" cite the Plan 05 UAT adversarial-sweep outcome (e.g., "20-UAT.md records 0/5 adversarial leaks + 10/10 normal pass").
       - **Hotlink Mitigation:** READ the hotlink sample from 20-UAT.md. If 5/5 HTTP 200: Decision is "TRUST BRAVE CDN — no proxy needed in Phase 21 baseline (proxy becomes a Phase 21.x contingency only)". If <5/5: Decision is "BUILD NEXT.JS IMAGE PROXY IN PHASE 21" and cite the failed URL count.
       - **Test Mode Architecture:** Copy Option B recommendation verbatim. The "Rejected Alternatives" subsection MUST cite specific RESEARCH.md line numbers (see interfaces block: `20-RESEARCH.md:68`, `20-RESEARCH.md:230`, `20-RESEARCH.md:392-399`, `20-RESEARCH.md:495`, `20-RESEARCH.md:509-512`) for the three Option-A blockers (JWT/cookie forgery, SSE stream translation, upstream-stability coupling). Confidence stays MEDIUM until Phase 22 source-code pass.

    2. Add "Phase 21 Followups" bullets from 20-UAT.md caveats. If UAT verdict is APPROVED (no caveats), write `- None — UAT clean`.

    3. Add "Phase 22 Followups" bullets — always include at minimum: (a) source-code verify LibreChat v0.8.4 auth mechanism, (b) reuse Phase 21's image-search service in Test Mode.

    4. Append to STATE.md's `### Decisions` list (alphabetical by phase prefix or chronological — follow existing pattern). Add four lines:
       ```
       - [Phase 20]: Tool Mechanism — MCP via brave/brave-search-mcp-server (streamable-http transport), attached per-agent via tools array
       - [Phase 20]: Search Provider — Brave Search API; $5/month credit covers family scale; per-child daily search cap implemented in Phase 21
       - [Phase 20]: Hotlink Mitigation — <Trust Brave CDN / Proxy in Phase 21> per UAT hotlink sample result
       - [Phase 20]: Test Mode Architecture — Option B (re-implement server-side via Anthropic SDK), flipped from CONTEXT.md Option-A lean based on RESEARCH blockers
       ```

    5. Update STATE.md frontmatter `last_updated` timestamp and `last_activity` line.

    6. Verification: `grep -c '^## ' .planning/phases/20-image-search-research-poc/20-DECISIONS.md` must return ≥ 4 (four top-level H2 sections).
  </action>
  <verify>
    <automated>test -f .planning/phases/20-image-search-research-poc/20-DECISIONS.md && test "$(grep -cE '^## (Tool Mechanism|Search Provider|Hotlink Mitigation|Test Mode Architecture)$' .planning/phases/20-image-search-research-poc/20-DECISIONS.md)" -ge 4 && grep -q '20-RESEARCH.md:' .planning/phases/20-image-search-research-poc/20-DECISIONS.md && grep -q '\[Phase 20\]: Tool Mechanism' .planning/STATE.md && grep -q '\[Phase 20\]: Search Provider' .planning/STATE.md && grep -q '\[Phase 20\]: Hotlink Mitigation' .planning/STATE.md && grep -q '\[Phase 20\]: Test Mode Architecture' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - 20-DECISIONS.md exists with the four H2 sections named exactly: `Tool Mechanism`, `Search Provider`, `Hotlink Mitigation`, `Test Mode Architecture`
    - Each section contains Decision / Rationale / Rejected alternatives / Confidence / Citation lines
    - **Test Mode Architecture section's Rejected Alternatives subsection cites at least one RESEARCH.md line reference in `20-RESEARCH.md:<N>` format** (verified by `grep -q '20-RESEARCH.md:' 20-DECISIONS.md`)
    - STATE.md has four new `[Phase 20]:` decision lines
    - Phase 21 Followups + Phase 22 Followups sections present
  </acceptance_criteria>
  <done>Decisions locked in both the phase doc and STATE; Phase 21 + Phase 22 inherit them.</done>
</task>

<task type="auto">
  <name>Task 20-06-02: Revert Railway CONFIG_PATH to production Gist + redeploy LibreChat + regression</name>
  <files>(Railway) env var CONFIG_PATH on service librechat</files>
  <read_first>
    - .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md (PROD_CONFIG_PATH_PRE — the exact string to revert to)
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Common Pitfalls Pitfall 5 (forgetting to re-point CONFIG_PATH)
  </read_first>
  <action>
    1. Read PROD_CONFIG_PATH_PRE from 20-02-GIST-REFS.md.

    2. Set Railway env: `railway variables --service librechat --set CONFIG_PATH='<PROD_CONFIG_PATH_PRE>'`.

    3. Confirm: `test "$(railway variables get --service librechat CONFIG_PATH)" = "$(grep ^PROD_CONFIG_PATH_PRE= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)"` — must pass.

    4. Redeploy: `railway redeploy --service librechat`. Wait for SUCCESS (up to 3 min).

    5. Health check: `curl -fs https://librechat-production-bff2.up.railway.app/health` returns 200.

    6. Production sanity: log in as Sebastian in a fresh incognito window. Confirm the preset selector shows FIVE presets (Friendly Tutor, Casual Buddy, Balanced Helper, Standard Formal, Drawing Studio) — NOT six. The Image Search preset MUST NOT appear. If it does, the revert failed — repeat step 2 and investigate.

    7. PRESERVE the dev Gist. Do NOT delete it. Confirm it still exists: `gh gist view "$(grep ^DEV_GIST_ID= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" --raw | head -5` — must return yaml content. Phase 21 will re-use this Gist as its starting point.

    8. PRESERVE the Brave MCP Railway service (`kidschat-brave-mcp`). Do NOT tear down. Phase 21 will use it. Confirm: `railway service list --json | jq -r '.[].name' | grep -x kidschat-brave-mcp` — must match.

    9. PRESERVE the Image Search MongoDB agent doc. Do NOT delete. Phase 21 copies the prompt shape from it. Confirm: `mongosh $ADMIN_MONGO_URI --quiet --eval "db.agents.countDocuments({id:'$(grep ^IMAGE_SEARCH_AGENT_ID= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)'})"` returns 1.

    10. Regression: run `npm test`. 65 existing tests must still pass. Phase 20 added no new tests (D-07), so the count should be unchanged.

    11. Record the post-revert content hash: `PROD_GIST_SHA_POST=$(gh gist view "$(grep ^PROD_GIST_ID= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" --raw | shasum -a 256 | cut -d' ' -f1)`. Assert equals PROD_GIST_SHA_PRE: `test "$PROD_GIST_SHA_POST" = "$(grep ^PROD_GIST_SHA_PRE= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)"`. This is the final T-20-C assertion — production Gist byte-identical to pre-Phase-20 state.
  </action>
  <verify>
    <automated>test "$(railway variables get --service librechat CONFIG_PATH)" = "$(grep ^PROD_CONFIG_PATH_PRE= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" && curl -fs https://librechat-production-bff2.up.railway.app/health && test "$(gh gist view "$(grep ^PROD_GIST_ID= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" --raw | shasum -a 256 | cut -d' ' -f1)" = "$(grep ^PROD_GIST_SHA_PRE= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Railway `CONFIG_PATH` equals PROD_CONFIG_PATH_PRE verbatim
    - LibreChat `/health` returns 200 on production Gist
    - Production Gist content hash still equals PROD_GIST_SHA_PRE
    - Dev Gist preserved (readable via gh)
    - Brave MCP Railway service preserved
    - Image Search MongoDB agent doc preserved
    - `npm test` exits 0 with the same test count as before Phase 20
  </acceptance_criteria>
  <done>Production restored; staging artifacts preserved for Phase 21; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 20-06-03: Parent sign-off — verify production restored + Phase 20 closed</name>
  <what-built>
    Phase 20 is complete: decisions locked, production reverted, staging artifacts preserved. Final human-in-the-loop check confirms kids' production experience shows no trace of Phase 20 changes.
  </what-built>
  <how-to-verify>
    1. In a FRESH incognito window, log in to https://librechat-production-bff2.up.railway.app/ as Penelope (the real kid, not Sebastian — this is the oversight check, not UAT). Confirm:
       - Preset selector shows exactly 5 presets. NO Image Search preset.
       - Greeting messages unchanged.
       - Behavior unchanged.
    2. Log out. Log in as Sebastian. Same check — 5 presets, no Image Search.
    3. Open admin dashboard https://kidschat-admin-production.up.railway.app/. Confirm usual pages render (no errors from the preserved MongoDB agent doc bleeding in somewhere unexpected).
    4. Review `.planning/phases/20-image-search-research-poc/20-DECISIONS.md`. Confirm all four decisions are legible, each has rationale + rejected alternatives + confidence, and the Phase 21/22 followups make sense.
    5. Review `.planning/STATE.md` — confirm the four `[Phase 20]:` decision lines were appended in the right place.
    6. Approve: respond "approved — Phase 20 complete" to close. This triggers the orchestrator to commit the phase and update ROADMAP.md status to Complete.
    7. If any production regression or doc gap is found: respond with "rejected: <reason>" and Task 20-06-02 is repeated with fixes.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 20 complete" to close the phase, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway CONFIG_PATH | Revert must be byte-exact or kids see wrong config |
| Phase-20 staging artifacts | Preserved for Phase 21, must not leak into production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-C-05 | Tampering (production stays wrong) | Pitfall 5 (forgetting revert) | mitigate | Task 20-06-02 verification asserts CONFIG_PATH == PROD_CONFIG_PATH_PRE AND GIST_SHA_POST == GIST_SHA_PRE; Task 20-06-03 parent-eye visual confirmation |
| T-20-C-06 | Disclosure (Image Search preset accidentally live in prod) | Dev Gist lingers in CONFIG_PATH | mitigate | Task 20-06-03 Penelope-login check explicitly verifies 5 presets, not 6 |
| T-20-D-06 | Repudiation (decisions unclear for downstream phases) | 20-DECISIONS.md completeness | mitigate | Task 20-06-01 acceptance asserts 4 H2 sections exist; Task 20-06-03 parent reviews legibility |
</threat_model>

<verification>
- 20-DECISIONS.md has 4 H2 sections, each with the 5 required sub-items
- STATE.md has 4 new `[Phase 20]:` lines
- Railway CONFIG_PATH reverted and production Gist hash unchanged
- Dev Gist + Brave MCP service + MongoDB agent doc preserved
- `npm test` passes with unchanged test count
- Parent-sign-off recorded (Task 20-06-03 approval)
</verification>

<success_criteria>
Phase 20 is closed: four architectural decisions are locked, Phases 21 + 22 inherit them, production is untouched, staging artifacts are ready for Phase 21 reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/20-image-search-research-poc/20-06-SUMMARY.md` with: (a) final decisions (one-line each), (b) confirmation production restored, (c) preserved artifact references (dev Gist URL, Brave MCP service name, agent id), (d) parent sign-off timestamp, (e) Phase 21 + Phase 22 followup lists.
</output>
