---
phase: 20
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - (Railway) env var CONFIG_PATH on service librechat (temporarily set to DEV_CONFIG_PATH)
  - .planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md (MCP connectivity capture)
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "Railway service `librechat` has `CONFIG_PATH` set to the dev Gist commit-pinned raw URL (DEV_CONFIG_PATH from 20-02-GIST-REFS.md)"
    - "LibreChat has been redeployed against the dev Gist and `/health` returns 200"
    - "LibreChat startup logs show custom image-search MCP server initialized (`mcp.*image-search` or equivalent, no error traces)"
    - "A browser smoke test (parent logged in as Sebastian) confirms the Image Search preset appears in the preset selector and a search query returns tool-invocation activity"
    - "Open Question OQ1 (LibreChat ↔ image-search MCP streamable-http wire compat) is resolved — either PASS (continue to Plan 05) or FAIL with a documented pivot branch"
  artifacts:
    - path: ".planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md"
      provides: "Log of LibreChat startup, MCP handshake result, and the browser smoke test outcome"
    - path: "(Railway) CONFIG_PATH on librechat service"
      provides: "Staging deployment pointer — reverted in Plan 06"
  key_links:
    - from: "LibreChat startup"
      to: "image-search MCP Railway service"
      via: "streamable-http MCP handshake using url from dev Gist mcpServers.image-search"
      pattern: "mcp.*image-search|image_search.*registered"
    - from: "LibreChat preset selector"
      to: "MongoDB agents doc"
      via: "modelSpecs.list[image-search].preset.agent_id → db.agents.findOne({id:...})"
      pattern: "Image Search appears alongside Drawing Studio + 4 text presets"
---

<objective>
Swap Railway LibreChat's `CONFIG_PATH` to the dev Gist, redeploy, and verify end-to-end that: (a) LibreChat boots cleanly against the new yaml, (b) the custom image-search MCP server is reachable and the `image_search` tool is registered, (c) the Image Search preset is visible to a kid-role account in the browser, (d) a test query triggers actual MCP tool invocation. This resolves OQ1 (the one open wire-compat assumption from RESEARCH.md).

Purpose: Single point where integration either works or reveals a pivot need. Fail-fast — do not proceed to UAT (Plan 05) until OQ1 passes or a documented fallback is in place.
Output: A live staging LibreChat wired to the dev Gist + MongoDB agent + image-search MCP; signed-off smoke log; binary decision: continue to Plan 05 OR branch to a pivot (see fail-fast branch below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-image-search-research-poc/20-CONTEXT.md
@.planning/phases/20-image-search-research-poc/20-RESEARCH.md
@.planning/phases/20-image-search-research-poc/20-VALIDATION.md
@.planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md
@.planning/phases/20-image-search-research-poc/20-01-ENDPOINTS.md

<interfaces>
<!-- Railway CLI ops pattern — reproduced from Phase 15/16/17 precedent: -->
<!-- Set env var: railway variables --service librechat --set CONFIG_PATH='<URL>' -->
<!-- Trigger redeploy: railway redeploy --service librechat (or `railway up --service librechat` if source-build) -->
<!-- Tail logs: railway logs --service librechat --tail 300 -->
<!-- Health: curl -fs https://librechat-production-bff2.up.railway.app/health -->

<!-- Expected log signals (from RESEARCH.md Open Questions): -->
<!-- PASS: "mcp" or "MCP" with "image-search" and no error stack -->
<!-- FAIL (wire-compat): Connection refused, protocol mismatch, "unknown transport", JSON-RPC handshake error -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 20-04-01: Swap CONFIG_PATH to dev Gist + redeploy LibreChat</name>
  <files>(Railway) env var CONFIG_PATH on service librechat</files>
  <read_first>
    - .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md (DEV_CONFIG_PATH, PROD_CONFIG_PATH_PRE)
    - .planning/STATE.md §"Gist config push requires CONFIG_PATH update to new commit hash" (Phase 16 pitfall)
  </read_first>
  <action>
    1. Re-read PROD_CONFIG_PATH_PRE from 20-02-GIST-REFS.md. Confirm it matches the CURRENT live value:
       `LIVE=$(railway variables get --service librechat CONFIG_PATH)`
       `test "$LIVE" = "$(grep ^PROD_CONFIG_PATH_PRE= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)"` — must pass. If not, surface a checkpoint: "Live CONFIG_PATH doesn't match the captured baseline — something changed between Plan 02 and now. Refuse to swap until resolved."

    2. Read DEV_CONFIG_PATH from 20-02-GIST-REFS.md.

    3. Set the env var to dev: `railway variables --service librechat --set CONFIG_PATH='<DEV_CONFIG_PATH>'`. Confirm: `railway variables get --service librechat CONFIG_PATH` equals DEV_CONFIG_PATH.

    4. Trigger redeploy: `railway redeploy --service librechat` (or `railway up --service librechat` depending on how the service is configured — check `railway status` first).

    5. Wait for deploy to complete (up to 3 minutes). Poll `railway status --service librechat --json | jq -r '.deployments[0].status'` until SUCCESS.

    6. Health check: `curl -fs https://librechat-production-bff2.up.railway.app/health` must return 200 within 60s after SUCCESS.

    7. Capture the deployment ID for the smoke log: `DEPLOY_ID=$(railway status --service librechat --json | jq -r '.deployments[0].id')`.
  </action>
  <verify>
    <automated>test "$(railway variables get --service librechat CONFIG_PATH)" = "$(grep ^DEV_CONFIG_PATH= .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)" && curl -fs https://librechat-production-bff2.up.railway.app/health</automated>
  </verify>
  <acceptance_criteria>
    - `railway variables get --service librechat CONFIG_PATH` equals DEV_CONFIG_PATH (verbatim)
    - LibreChat `/health` returns 200
    - Deployment status is SUCCESS in Railway
    - PROD_CONFIG_PATH_PRE still matches what we captured (no drift during our Plan 02–04 window)
  </acceptance_criteria>
  <done>Staging LibreChat live on dev Gist; production Gist URL still recorded for revert.</done>
</task>

<task type="auto">
  <name>Task 20-04-02: Validate image-search MCP handshake in LibreChat logs (OQ1 resolution)</name>
  <files>.planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md (new)</files>
  <read_first>
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Open Questions Q1 (the wire-compat assumption)
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Assumptions Log A2 (MEDIUM risk fallback: stdio sidecar or OpenAPI tool)
  </read_first>
  <action>
    1. Tail LibreChat logs immediately after the Task 20-04-01 redeploy:
       `railway logs --service librechat --tail 500 > /tmp/librechat-boot.log 2>&1`

    2. Classify the MCP init outcome. Check for BOTH positive and negative signals:
       - POSITIVE: `grep -iE 'mcp.*image-search|image_search.*(registered|loaded|initialized)' /tmp/librechat-boot.log`
       - NEGATIVE: `grep -iE 'mcp.*error|mcp.*fail|image-search.*error|ECONNREFUSED|protocol.*mismatch|unknown transport|jsonrpc.*error' /tmp/librechat-boot.log`

    3. Write `.planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md` with:
       - Timestamp
       - Railway deploy ID
       - Excerpt of MCP-relevant log lines (first 20 matches of a broad `grep -i mcp` on the tail)
       - Verdict: PASS / FAIL / INCONCLUSIVE
       - If FAIL or INCONCLUSIVE: the specific error text

    4. Independent image-search MCP health re-check (sanity): `curl -fs $IMAGE_SEARCH_MCP_URL/health || curl -fs -X POST -H 'Content-Type: application/json' --data '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' $IMAGE_SEARCH_MCP_URL/mcp` — the second form should list `image_search` among tools, confirming the MCP server itself is healthy (isolates whether the problem is LibreChat-side vs server-side).

    5. **Fail-fast branch (do NOT skip this — the plan explicitly requires it):**
       If Task 20-04-02 verdict is FAIL (MCP handshake fails / LibreChat can't load `image_search`):

       - (a) Document the exact error in 20-04-SMOKE-LOG.md under a `## Pivot Assessment` heading.
       - (b) Attempt pivot variant 1: change dev Gist `mcpServers.image-search.type` from `streamable-http` to `sse` (LibreChat v0.8.4 supports both). Re-commit Gist (new SHA), update DEV_CONFIG_PATH, update Railway CONFIG_PATH, redeploy, retest. Record outcome.
       - (c) If variant 1 still fails: attempt variant 2 — deploy custom image-search MCP server in stdio mode inside a Node sidecar container alongside LibreChat (Assumption A2 fallback). This is materially larger scope — if reached, STOP and surface a checkpoint to the parent: "OQ1 failed; pivot to stdio-sidecar requires a new Railway service and LibreChat image rebuild. Approve to proceed OR approve pivot to custom OpenAPI tool (smaller change, agent calls Google CSE HTTP directly via an OpenAPI spec Gist)?"
       - (d) Do NOT proceed to Task 20-04-03 or Plan 05 until the pivot yields a PASS.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md && grep -qE '^Verdict:\s*(PASS|FAIL|INCONCLUSIVE)' .planning/phases/20-image-search-research-poc/20-04-SMOKE-LOG.md && ! grep -qE 'ECONNREFUSED|unknown transport|protocol.*mismatch' /tmp/librechat-boot.log 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - 20-04-SMOKE-LOG.md exists with a `Verdict:` line (PASS / FAIL / INCONCLUSIVE)
    - If PASS: logs show image-search MCP initialization without errors
    - If FAIL: SMOKE-LOG includes `## Pivot Assessment` section with the exact error AND which pivot path was attempted
    - MCP server independently health-checks (image-search MCP responds to `tools/list` with `image_search` in the result)
    - No unresolved error keywords (`ECONNREFUSED`, `unknown transport`, `protocol mismatch`) in the final boot log tail
  </acceptance_criteria>
  <done>OQ1 resolved: either MCP wire compat works (continue) or pivot path documented + parent-approved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 20-04-03: Parent browser smoke test (Sebastian login, Image Search preset visible, one query triggers tool call)</name>
  <what-built>
    Staging LibreChat redeployed against dev Gist with Image Search preset + image-search MCP wired. A minimal human-in-the-loop smoke check to confirm the preset surfaces and a query actually round-trips through Google CSE before committing parent time to the full 20-query UAT in Plan 05.
  </what-built>
  <how-to-verify>
    1. In a private/incognito browser, visit https://librechat-production-bff2.up.railway.app/ and log in as Sebastian. (Use Sebastian's credentials per CONTEXT.md D-06 — parent-as-kid-testing, the real kid does not UAT.)
    2. Confirm the preset selector shows SIX presets: Friendly Tutor, Casual Buddy, Balanced Helper, Standard Formal, Drawing Studio, **Image Search** (new). If Image Search is absent, the dev Gist is not loaded — check Task 20-04-01 steps and redeploy.
    3. Select the Image Search preset. Confirm the greeting text displays: "Hi! Tell me what to search for — I'll show you pictures. Try 'origami cats' or 'watercolor trees'."
    4. Send exactly this query: `origami cats`
    5. Observe the response. Expected: a row of 8 inline images (plain `<img>`, no link wrap, no text before/after). Acceptable alternative: if the provider returns 0 results for the query (unlikely), the exact text `No safe images for that search — try something else.` and nothing else.
    6. In parallel, in a second shell, `railway logs --service librechat --tail 100 | grep -iE 'image_search|mcp'` — should show a tool_call for `image_search` with `q=origami cats, safesearch=strict, count=10`.
    7. In browser devtools (right-click one of the images → Inspect), confirm the DOM shows `<img src="https://<thumbnail-cdn-host>/...">` with NO wrapping `<a href="...">`. This verifies option iii click-through policy per D-04.
    8. If ALL checks pass: respond "approved" to proceed to Plan 05 UAT.
    9. If ANY check fails: respond with the specific failure (absent preset / wrong greeting / text commentary appeared / links wrap images / MCP tool call did not fire). This triggers a fix-forward before Plan 05.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 05, or describe the failure mode for triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway CONFIG_PATH env | Points LibreChat at arbitrary yaml — misconfiguration = kids see wrong config |
| Dev Gist CDN caching | Commit-pinned URL avoids CDN staleness (Pitfall 2) |
| LibreChat ↔ image-search MCP protocol | OQ1 — unverified wire compat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-C-03 | Tampering | Wrong Gist URL pinned | mitigate | Task 20-04-01 verifies CONFIG_PATH equals DEV_CONFIG_PATH exactly before redeploy; Plan 06 reverts to PROD_CONFIG_PATH_PRE exactly |
| T-20-D-04 | Spoofing (commentary/links) | Rendering policy violation | mitigate | Task 20-04-03 inspects DOM for `<a>` wrap and checks for text commentary; blocking checkpoint before Plan 05 |
| T-20-A-03 | Information Disclosure | safesearch param downgraded | mitigate | Task 20-04-03 logs must show `safesearch=strict` on the tool_call; also verified in system prompt (Plan 03) |
| OQ1 | — | MCP wire compat | mitigate | Fail-fast branch in Task 20-04-02 with two documented pivot paths |
</threat_model>

<verification>
- `CONFIG_PATH` on librechat service equals DEV_CONFIG_PATH
- LibreChat `/health` returns 200
- 20-04-SMOKE-LOG.md has `Verdict: PASS` OR a documented pivot path approved by the parent
- Parent-approved checkpoint (Task 20-04-03): Image Search preset visible, query round-trips via MCP, images render inline with no link wrap
</verification>

<success_criteria>
Staging LibreChat + image-search MCP + Image Search agent all wired and visibly functional to a parent-driven Sebastian login. OQ1 resolved. Ready for full 20-query UAT in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/20-image-search-research-poc/20-04-SUMMARY.md` with: (a) deploy ID + timestamp, (b) OQ1 verdict (PASS / pivot-A / pivot-B), (c) parent smoke-test outcome (approved / issues), (d) DOM inspection result (no `<a>` wrap confirmed).
</output>
