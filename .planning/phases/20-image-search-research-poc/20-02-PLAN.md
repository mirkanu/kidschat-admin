---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - (GitHub) new Gist "kidschat-dev-librechat" (dev copy of production librechat.yaml)
  - .planning/phases/20-image-search-research-poc/20-01-ENDPOINTS.md (read-only reference to Task 01's URL)
  - .planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml (local copy for review + audit)
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "A new dev Gist named 'kidschat-dev-librechat' exists on the parent's GitHub account"
    - "The dev Gist is a byte-for-byte fork of the production Gist, PLUS one `mcpServers.image-search` block PLUS one new entry in `modelSpecs.list` for 'grep -E 'image-search'"
    - "The dev Gist's `mcpServers.image-search.url` points at the Railway internal URL captured in 20-01-ENDPOINTS.md"
    - "The dev Gist includes a commit-pinned raw URL (hash in path) that Plan 04 will use in Railway CONFIG_PATH"
    - "The production Gist SHA is unchanged (byte-identical) before and after this plan runs"
  artifacts:
    - path: "(GitHub Gist) kidschat-dev-librechat"
      provides: "Dev librechat.yaml with Image Search preset + MCP declaration"
      contains: "mcpServers:\n  image-search:, modelSpecs.list entry 'grep -E 'image-search'"
    - path: ".planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml"
      provides: "Local audit copy of the dev Gist content — reviewable in git"
    - path: ".planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md"
      provides: "Records DEV_GIST_ID, DEV_GIST_SHA, PROD_GIST_SHA_PRE (baseline for revert check in Plan 06)"
  key_links:
    - from: "dev Gist mcpServers.image-search.url"
      to: "kidschat-image-search-mcp Railway internal URL"
      via: "string substitution at Gist-write time (not env var — LibreChat would need ${VAR} resolved at startup; simpler to inline the URL)"
      pattern: "url: \"http[s]?://kidschat-image-search-mcp.railway.internal:8080/mcp\""
    - from: "dev Gist modelSpecs.list[].preset.agent_id for 'grep -E 'image-search'"
      to: "Plan 03's MongoDB agents doc id"
      via: "exact string match — both must use the same id value"
      pattern: "agent_id:\\s*\"agent_kidschat_imagesearch_<timestamp>\""
---

<objective>
Fork the production `librechat.yaml` Gist into a new dev Gist that declares the custom image-search MCP server AND adds the Image Search preset. Production Gist stays untouched. Dev Gist's raw-commit URL is captured so Plan 04 can swap Railway CONFIG_PATH to it.

Purpose: Isolation mechanism (D-08/D-09) — staging config lives in a separate Gist so a misconfig cannot affect production LibreChat. The commit-pinned raw URL is the unit of deploy.
Output: A dev Gist URL + commit SHA recorded in 20-02-GIST-REFS.md; a local audit copy under artifacts/dev-librechat.yaml for reviewability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-image-search-research-poc/20-CONTEXT.md
@.planning/phases/20-image-search-research-poc/20-RESEARCH.md
@.planning/phases/20-image-search-research-poc/20-VALIDATION.md
@.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-01-GIST-AFTER.yaml
@.planning/phases/20-image-search-research-poc/20-01-ENDPOINTS.md

<interfaces>
<!-- Starting-point yaml: 19-01-GIST-AFTER.yaml — the currently live production config. Fork this. -->
<!-- Required additions (per RESEARCH.md Pattern 1 and Pattern 3): -->

```yaml
# APPEND at top level, NOT inside `interface.mcpServers` (that block governs UI, not server declaration):
mcpServers:
  image-search:
    type: streamable-http
    url: "http://kidschat-image-search-mcp.railway.internal:8080/mcp"   # exact URL from 20-01-ENDPOINTS.md
    startup: true
    chatMenu: false                                               # kids must NOT see a generic MCP picker
    serverInstructions: false

# APPEND inside modelSpecs.list, AFTER the Drawing Studio entry:
    - name: image-search
      label: "Image Search"
      description: "Find safe images — origami, crafts, animals, art references"
      iconURL: "https://api.iconify.design/lucide/image-plus.svg?color=%23e2e8f0"
      default: false
      preset:
        endpoint: "agents"
        agent_id: "agent_kidschat_imagesearch_<TIMESTAMP>"        # MUST match the id Plan 03 seeds in MongoDB
        greeting: "Hi! Tell me what to search for — I'll show you pictures. Try 'origami cats' or 'watercolor trees'."
```

<!-- Do NOT change `interface.mcpServers.use: false` — that hides kid-facing MCP UI. It does NOT disable the server declaration. -->
<!-- Do NOT touch `endpoints`, `balance`, `fileConfig`, `interface` other blocks — byte-diff must be exactly the two additions above. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 20-02-01: Verify GITHUB_GIST_TOKEN + capture PROD_GIST_SHA_PRE baseline</name>
  <files>.planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md (new)</files>
  <read_first>
    - .planning/STATE.md §"[Phase 15-safety-alert-extension-rate-limiting]: ... GITHUB_GIST_TOKEN in Railway is expired and needs refresh"
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Environment Availability (Gist token validity flagged)
  </read_first>
  <action>
    1. Check token validity: `gh auth status` (must show authenticated) AND `gh api user --silent` (must return 200). If either fails, surface a blocking checkpoint to the parent: "GITHUB_GIST_TOKEN in Railway is stale (Phase 15 note). Please re-mint a fine-grained PAT with Gist scope only and set it in Railway + local `gh auth login --with-token`. Cannot proceed until this is fixed." — and stop.
    2. Identify the production Gist. Read `CONFIG_PATH` from the live LibreChat Railway service: `railway variables get --service librechat CONFIG_PATH`. Extract the Gist ID from the URL (format `https://gist.githubusercontent.com/<user>/<GIST_ID>/raw/<COMMIT_SHA>/librechat.yaml`).
    3. Capture the production SHA BASELINE: `PROD_GIST_RAW=$(gh gist view <PROD_GIST_ID> --raw)` then `PROD_GIST_SHA_PRE=$(echo "$PROD_GIST_RAW" | shasum -a 256 | cut -d' ' -f1)`. Store in 20-02-GIST-REFS.md.
    4. Also capture the committed production yaml commit SHA (the `<COMMIT_SHA>` in the URL) — store as `PROD_CONFIG_PATH_PRE` so Plan 06 can revert exactly.
    5. Write 20-02-GIST-REFS.md with keys: `PROD_GIST_ID`, `PROD_CONFIG_PATH_PRE`, `PROD_GIST_SHA_PRE` (content hash). `DEV_GIST_ID` and `DEV_GIST_SHA` will be appended in Task 20-02-02.
  </action>
  <verify>
    <automated>gh api user --silent && test -f .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md && grep -q 'PROD_GIST_SHA_PRE' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md && grep -q 'PROD_CONFIG_PATH_PRE' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status` authenticates successfully
    - 20-02-GIST-REFS.md contains PROD_GIST_ID, PROD_CONFIG_PATH_PRE (full URL with pinned commit), PROD_GIST_SHA_PRE (content sha256)
    - No edits made yet — production Gist byte-identical
  </acceptance_criteria>
  <done>Gist token verified, production baseline captured for revert check.</done>
</task>

<task type="auto">
  <name>Task 20-02-02: Create dev Gist (kidschat-dev-librechat) with Image Search preset + MCP declaration</name>
  <files>
    - (GitHub) new Gist "kidschat-dev-librechat"
    - .planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml (local audit copy)
    - .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md (append DEV_GIST_ID + DEV_GIST_SHA + DEV_CONFIG_PATH)
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-01-GIST-AFTER.yaml (starting point — verbatim fork source)
    - .planning/phases/20-image-search-research-poc/20-01-ENDPOINTS.md (image-search MCP internal URL)
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Pattern 1 (MCP declaration shape) and §Pattern 3 (preset shape)
    - .planning/phases/20-image-search-research-poc/20-RESEARCH.md §Common Pitfalls Pitfall 3 (chatMenu: false is mandatory)
  </read_first>
  <action>
    1. Start from the production Gist content (from Task 20-02-01 capture) — NOT from the local 19-01-GIST-AFTER.yaml, because production may have drifted since that snapshot. Fetch fresh: `gh gist view <PROD_GIST_ID> --raw > /tmp/dev-yaml-base.yaml`.

    2. Generate the agent id that Plan 03 will seed. Use a timestamp: `AGENT_ID="agent_kidschat_imagesearch_$(date +%s%3N)"`. Write this to 20-02-GIST-REFS.md as `IMAGE_SEARCH_AGENT_ID` — Plan 03 MUST use the same exact string.

    3. Append the `mcpServers` top-level block (copy from <interfaces>). Read the image-search MCP internal URL from 20-01-ENDPOINTS.md and inline it verbatim — do NOT use `${IMAGE_SEARCH_MCP_INTERNAL_URL}` (LibreChat does not reliably resolve shell-style refs in the MCP url field; RESEARCH's example used it illustratively). Use the actual URL string. If `.railway.internal` private networking is unavailable between services, fall back to the public URL (both work; internal is preferred for latency + not exposing the MCP publicly).

    4. Append the `image-search` modelSpecs entry AFTER the Drawing Studio entry. Use the exact shape from <interfaces>, substituting the real `AGENT_ID` from step 2.

    5. Write the result to `.planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml` (create `artifacts/` dir if missing). This is the reviewable diff.

    6. Create the dev Gist:
       `DEV_GIST_URL=$(gh gist create --desc "kidschat-dev-librechat (Phase 20 POC)" .planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml)`
       Extract `DEV_GIST_ID` from the URL (last path segment).

    7. Capture the commit-pinned raw URL: `DEV_GIST_SHA=$(gh api gists/$DEV_GIST_ID | jq -r '.history[0].version')`. Build `DEV_CONFIG_PATH="https://gist.githubusercontent.com/$(gh api user -q .login)/$DEV_GIST_ID/raw/$DEV_GIST_SHA/dev-librechat.yaml"`. (File name may differ; use the actual file name the Gist used.)

    8. Append `DEV_GIST_ID`, `DEV_GIST_SHA`, `DEV_CONFIG_PATH`, `IMAGE_SEARCH_AGENT_ID` to 20-02-GIST-REFS.md.

    9. Sanity checks:
       - `gh gist view $DEV_GIST_ID --raw | grep -c 'safesearch'` — actually, safesearch isn't set in the yaml (it's per-call at tool invocation time per the agent prompt in Plan 03), so skip this grep. Instead:
       - `gh gist view $DEV_GIST_ID --raw | grep -E 'grep -E 'image-search'` — both strings must be present.
       - `gh gist view $DEV_GIST_ID --raw | grep -E 'chatMenu:\s*false'` — MUST be present (Pitfall 3).
       - `curl -fsL "$DEV_CONFIG_PATH" | grep -E 'grep -E 'image-search'` — commit-pinned URL resolves to the expected content.
       - PROD_GIST unchanged: `test "$(gh gist view $PROD_GIST_ID --raw | shasum -a 256 | cut -d' ' -f1)" = "$PROD_GIST_SHA_PRE"` — MUST pass (T-20-C mitigation).
  </action>
  <verify>
    <automated>gh gist view $(grep '^DEV_GIST_ID=' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2) --raw | grep -qE 'grep -E 'image-search' && gh gist view $(grep '^DEV_GIST_ID=' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2) --raw | grep -qE 'grep -E 'image-search' && gh gist view $(grep '^DEV_GIST_ID=' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2) --raw | grep -qE 'chatMenu:\s*false' && test "$(gh gist view $(grep '^PROD_GIST_ID=' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2) --raw | shasum -a 256 | cut -d' ' -f1)" = "$(grep '^PROD_GIST_SHA_PRE=' .planning/phases/20-image-search-research-poc/20-02-GIST-REFS.md | cut -d= -f2)"</automated>
  </verify>
  <acceptance_criteria>
    - Dev Gist exists with both `image-search` and `image-search` strings present
    - `chatMenu: false` is set on the `image-search` MCP block (Pitfall 3)
    - `interface.mcpServers.use: false` preserved from production (kids don't see MCP UI)
    - `DEV_CONFIG_PATH` is a commit-pinned raw URL that resolves to the expected content
    - Production Gist byte-identical to PROD_GIST_SHA_PRE (T-20-C assertion)
    - `IMAGE_SEARCH_AGENT_ID` recorded in 20-02-GIST-REFS.md for Plan 03 to consume
    - `.planning/phases/20-image-search-research-poc/artifacts/dev-librechat.yaml` committed locally for review
  </acceptance_criteria>
  <done>Dev Gist created, pinned URL captured, production Gist untouched, audit copy committed to repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev-Gist ↔ Prod-Gist | Phase 20 must not modify production config (D-08) |
| Gist content ↔ Railway CONFIG_PATH | LibreChat fetches yaml at startup — misplaced content = live behavior change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-C-01 | Tampering | Production Gist accidentally clobbered | mitigate | Task 20-02-02 verification asserts PROD_GIST_SHA_PRE == PROD_GIST_SHA_POST (byte-identical) after dev Gist creation |
| T-20-C-02 | Tampering | Dev Gist confused for prod in Railway | mitigate | Plan 04 must explicitly use DEV_CONFIG_PATH; Plan 06 must revert to PROD_CONFIG_PATH_PRE (both recorded in 20-02-GIST-REFS.md) |
| T-20-D-01 | Spoofing | Generic MCP UI leaks in kid UI | mitigate | `chatMenu: false` on `mcpServers.image-search` (Pitfall 3); `interface.mcpServers.use: false` preserved from prod |
</threat_model>

<verification>
- 20-02-GIST-REFS.md contains all five keys: PROD_GIST_ID, PROD_CONFIG_PATH_PRE, PROD_GIST_SHA_PRE, DEV_GIST_ID, DEV_CONFIG_PATH, IMAGE_SEARCH_AGENT_ID, DEV_GIST_SHA
- Dev Gist content has `image-search` + `image-search` + `chatMenu: false`
- Production Gist content hash unchanged
- Local audit file `artifacts/dev-librechat.yaml` committed
</verification>

<success_criteria>
Dev Gist live and reviewable. Production Gist untouched. Agent ID locked so Plan 03 uses the same string.
</success_criteria>

<output>
After completion, create `.planning/phases/20-image-search-research-poc/20-02-SUMMARY.md` with: (a) DEV_GIST_URL (web view link), (b) DEV_CONFIG_PATH (commit-pinned raw URL), (c) IMAGE_SEARCH_AGENT_ID, (d) diff summary (mcpServers block + one modelSpecs entry added — nothing else changed), (e) confirmation that PROD_GIST_SHA_PRE unchanged.
</output>
