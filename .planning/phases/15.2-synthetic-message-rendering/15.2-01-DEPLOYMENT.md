# Phase 15.2 Plan 01 — Deployment Record

**Date:** 2026-04-11
**Branch decision:** GO — Option 7 (Agent System Prompt Context Injection)

---

## Task 2 — Implementation

**Commit:** b2a57b6 (feat: implement Option 7 agent system prompt injection)
**Railway deploy:** https://railway.com/project/784cfa32-257c-4e19-8ebb-37ea6931c9e2/service/83b7b7d1-76bd-4e99-8466-d2a1bf44f8d2

**What was deployed:**
- `sendBonusOfferMessage`: injects into `agents.instructions` instead of `messages` collection
- `restoreAgentSystemPrompt`: restores original instructions, clears pendingWarning
- `change-stream-listener`: delivery detection (5s buffer) + 10-min TTL sweep
- `BalanceState` extended with `pendingWarning` + `originalAgentInstructions` fields
- uuid@9 installed, all messageId generation uses uuidv4()

**Field path confirmed:** `agents.instructions` (query: `{id: agentId}`)
**Agent target:** `agent_wxgt6su7d3pcosiil3` (KidsChat Friendly Tutor, _id: 69d8b33dfec685bcff57cf5a)

---

## Task 3 — Manuel Reset

**Reset endpoint:** `POST /api/admin/reset-manuel` (temporary, CRON_SECRET auth)

**Reset response:**
```json
{
  "userId": "69cfd4edf4044c9e5e4c039a",
  "userName": "Manuel",
  "tokenCreditsSet": 10851,
  "eurEquivalent": 0.01,
  "balanceStateCleared": true,
  "syntheticsDeleted": 13
}
```

**What was cleared:**
- tokenCredits set to 10851 (~€0.01 — enough for 1-2 messages)
- balance_state: warnedAt70PctOn=null, activeOfferMessageId=null, pendingWarning=null, originalAgentInstructions=null
- 13 prior invisible synthetic "KidsChat Friendly Tutor" messages deleted
- Agent instructions force-restored (safety check)

---

## TODO: Post-UAT Cleanup

After UAT is confirmed:
1. Delete `src/app/api/admin/reset-manuel/route.ts` (temporary endpoint)
2. Revert `src/middleware.ts` to remove `api/admin` exclusion (or keep if other admin routes added later)
3. Optionally delete `scripts/reset-manuel-15-2.ts` (no longer needed)

---

## UAT Instructions (Task 4)

**URL:** https://librechat-production-bff2.up.railway.app
**User:** Manuel (login credentials same as normal)
**Balance at start:** ~€0.01 (10851 tokens — enough for 1-2 messages before 70% warning triggers)

**Expected flow:**

1. Open a private/incognito browser window (avoids stale React Query cache).
2. Navigate to https://librechat-production-bff2.up.railway.app
3. Log in as Manuel.
4. Open an existing conversation with the Friendly Tutor agent (or create a new one).
5. Send a normal message (e.g., "Tell me a story about a brave knight").
6. Wait for the agent's reply AND for the next poller tick (~60s after your message).
7. **CHECK 1 — 70% Warning:** Send another message. The agent's reply should BEGIN with the 70% warning text on its first line:
   ```
   📱 System message (not from your AI): You've used 70% of your daily chat budget. You have about 30% left before reaching your limit for today.
   ```
   (Followed by a blank line and the normal response.)
8. Continue sending messages until the budget is exhausted (~1-2 more messages).
9. **CHECK 2 — Bonus Offer:** When the budget hits zero, the agent's next reply should BEGIN with the bonus offer text. It will look like the `bonusMessageTemplate` from settings (e.g., "You've reached your limit. Type YES to unlock extra usage.").
10. Type `YES` as the next user message.
11. **CHECK 3 — Confirmation:** The agent's next reply should begin with:
    ```
    📱 System message (not from your AI): ✓ Bonus applied. You have €0.20 more to spend. Keep chatting!
    ```
    AND Manuel's balance in LibreChat Settings should have increased.
12. Verify chatting can continue after the YES.

**Timing note:** The poller runs every 60 seconds. After Manuel sends a message that crosses the 70% threshold, Claude's backend detects it within ~60s and injects the warning instruction. The warning text appears in the agent's reply to Manuel's NEXT message after the injection. So Manuel may need to wait up to 60s between triggering the threshold and seeing the warning.

**Type `approved`** if all three beats rendered visibly (warning inline, offer inline, YES credit applied).

---

## UAT Observations

*(To be filled in after Task 4 — copy-paste user's observations here)*
