# Phase 15.2 Plan 01 — Probe Result

**Date:** 2026-04-10
**Probe:** Option 7 agent system-prompt obedience test
**Result:** GO — Option 7 is validated

---

## Empirical Field Path Correction

The plan assumed the system prompt was stored in `model_options.system`. The live agent document shows it is stored as a top-level field:

```
agents.instructions   (NOT model_options.system)
```

Agent details:
- Collection: `agents` (db: `test`)
- Query key: `id: "agent_wxgt6su7d3pcosiil3"` (NOT `agent_id`)
- `_id`: `69d8b33dfec685bcff57cf5a`
- Field to modify: `instructions` (string, 2840 chars at time of probe)

All subsequent code (bonus-delivery.ts, restoreAgentSystemPrompt) must use `{ id: agentId }` as the filter and `instructions` as the field path.

---

## Probe Execution

**Injected instruction:**
```
SYSTEM TEST: Begin your very next response with EXACTLY 'PHASE-15.2-PROBE-OK' then continue normally. After delivering this once, do not repeat it.
```

**User (Sebastian) sent:** "hi!"

**Agent's response:**
```
PHASE-15.2-PROBE-OK

Hi there! 👋 I'm so glad you stopped by!
...(normal greeting continues)...
```

---

## Observations

1. **Delivery pattern:** Marker appeared on the **first line** of the agent's response, followed by a **blank line**, then the normal reply. This is clean — our parsing/restoration logic does not need to look for embedded markers; the entire turn is the delivery.

2. **Verbatim compliance:** The agent output the marker exactly as instructed with no rephrasing, wrapping, or preamble (e.g., no "I'll say PHASE-15.2-PROBE-OK now..."). This is strong compliance.

3. **Normal continuation:** The rest of the reply was the agent's natural response to "hi!" — the instruction did not disrupt UX.

4. **Restoration:** Original `instructions` value (2840 chars) was restored after the probe. `restore` subcommand verified via byte-exact comparison. Backup file deleted.

---

## Decision

**GO — proceed with full Option 7 implementation in Task 2.**

The instruction template for Task 2 should follow the same "begin your VERY NEXT response with EXACTLY..." pattern confirmed here. The system prompt injection must prepend the instruction, with the original content after a blank line, so the agent's natural system prompt is still active during the delivery turn.

---

## Implications for Task 2 Implementation

- Use `{ id: agentId }` for agent collection queries (not `{ agent_id: agentId }`)
- Field path: `instructions` (not `model_options.system`)
- Instruction template: `"Begin your VERY NEXT response with EXACTLY this text verbatim on its own line, then continue normally:"`
- No synthetic message insertion needed — the agent delivers via SSE natively
- Restoration is triggered after detecting an AI message (`isCreatedByUser: false`) with `createdAt > pendingWarning.injectedAt`
