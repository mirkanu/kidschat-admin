---
phase: 19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-03]
files_modified:
  - (optional, if defensive fix chosen) librechat.yaml fileConfig.maxSize — requires another Gist revision
  - (optional, if defensive fix chosen) src/lib/budget.ts — guard-rail for pre-deduct-without-refund
  - .planning/phases/19-.../19-04-DRAIN-FORENSICS.md (primary output)
  - .planning/phases/19-.../19-04-UPSTREAM-REPORT.md (if LibreChat bug filed)
autonomous: false

must_haves:
  truths:
    - "The 194,116-credit unexplained drain between 2026-04-16 18:35:20 and 18:38:45 UTC is explained OR documented as unexplainable with exhaustive evidence"
    - "If the drain cause is a LibreChat bug, a minimal reproduction + upstream issue link is recorded"
    - "If the drain cause is our code, the fix is implemented and deployed"
    - "We have at least ONE defensive guardrail in place to cap blast radius of this failure mode (image size limit OR balance-floor fuse OR both)"
    - "Extended thinking tokens accounting is confirmed or ruled out as a hidden undercount source"
  artifacts:
    - path: ".planning/phases/19-.../19-04-DRAIN-FORENSICS.md"
      provides: "End-to-end forensic timeline correlating Railway LibreChat logs with transaction writes and balance changes"
      min_lines: 60
    - path: "Gist librechat.yaml (optional new revision)"
      provides: "fileConfig.maxSize limit OR confirmation that we consciously did not add one"
  key_links:
    - from: "photo upload at 18:37:46 UTC"
      to: "balance drain 18:37–18:42 UTC"
      via: "Railway LibreChat service log correlation"
      pattern: "IMG_2210|IMG_2211|upload|stream|abort"
    - from: "LibreChat balance-deduction code path"
      to: "transactions collection write"
      via: "pre-deduct vs post-deduct order of operations"
---

<objective>
Forensically explain (or exhaustively document) the 194,116-credit drain that happened in Penelope's account today with NO transaction records, as flagged CRITICAL in Research Area 5 and Open Question #1. Implement at least one defensive guardrail so the blast radius is capped even if the root cause stays unknown.

Purpose: 89% of today's daily budget vanished with no traceable record. This is the single biggest unknown in the research. Even if we cannot fix the underlying LibreChat behavior, we MUST understand it well enough to decide: file upstream bug, add a balance-floor fuse, limit image upload sizes, or all three.

Output: A detailed forensic timeline report plus at least one defensive change deployed.

Depends on Plan 19-01 (so we are reading logs of a system that has the new config — we will not re-trigger the exact drain but the diagnostics environment is stable) and Plan 19-03 (so Penelope is restored and has credits available for any reproduction attempts we make).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-RESEARCH.md

Research Area 5 timeline (2026-04-16 UTC):
- 18:33:53 — image.jpg (0.95 MB) uploaded
- 18:34:14 — Turn 1 of conv 3aa0b4f3 (bunny + photo)
- 18:35:20 — Turn 2 response + transactions written (last traceable spend)
- 18:37:46 — IMG_2210.jpeg (576x768, 844 KB) + IMG_2211.jpeg (642x1999, 1.9 MB) uploaded
- 18:38:45 — conv 4594a8db: study question + both photos → NO response, NO tx
- 18:39:07 — conv 61046315: same photos + similar question → NO response, NO tx
- 18:42:38 — conv 665d16e9: drawing request only (no photos) → NO response, NO tx
- Net: 194,116 credits drained between 18:35:20 and 18:42:38 with no transactions

@.planning/STATE.md

STATE.md Phase 17 note: Railway internal MongoDB accessible externally via switchyard.proxy.rlwy.net:57501.
STATE.md Phase 17 note: librechat_safe MongoDB user blocks deletes on conversations+messages — make sure our forensic reads use the full-access admin creds, not the restricted librechat_safe ones.

<interfaces>
Conversations to inspect (Penelope's, 2026-04-16):
- 3aa0b4f3  (bunny + photo, has response + transactions) — normal baseline
- 4594a8db  (study question + 2 photos, NO response, NO tx) — incident
- 61046315  (similar, NO response, NO tx) — incident
- 665d16e9  (drawing only, no photos, NO response, NO tx) — incident

Fields on messages collection that matter:
- tokenCount (flat integer, may be missing)
- content[] (may have type:"think" blocks — research Open Q #5)
- error, errorMessage, aborted (LibreChat uses these on failed sends)
- files[] / attachments (attached photos)

Fields on balances doc:
- tokenCredits (current live balance)
- autoRefillEnabled (native LibreChat refill)
- lastRefill (native LibreChat field)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pull and correlate Railway LibreChat service logs for 18:33–18:45 UTC on 2026-04-16 with MongoDB message + transaction state</name>
  <files>
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md,
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-LOGS-LIBRECHAT.txt
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-RESEARCH.md (entire Area 5 and Open Question #1)
    - railway.toml (service name is `librechat` for the LibreChat service; `kidschat-admin-production` for the admin)
    - .planning/STATE.md (Phase 17 MongoDB access notes)
  </read_first>
  <action>
    Build a minute-by-minute forensic timeline and write it to `.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md`.

    Data sources to pull (use CLI, not dashboard):

    1. Railway LibreChat service logs for 2026-04-16 18:33:00–18:45:00 UTC:
       - `railway logs --service librechat` filtered with `grep -E "2026-04-16T18:3[3-9]|2026-04-16T18:4[0-5]"`
       - Also filter by relevance: `grep -iE "error|abort|stream|balance|deduct|transaction|spend|penelope|convId=3aa0b4f3|4594a8db|61046315|665d16e9"`
       - Note: Railway log retention may be limited. If logs older than 24h are gone, STATE that in the report and proceed with MongoDB-only evidence.
       - Write the raw filtered log dump to `.planning/phases/19-.../19-04-LOGS-LIBRECHAT.txt` (source material, appendix).

    2. MongoDB deep inspection. For each of the 4 conversations (3aa0b4f3, 4594a8db, 61046315, 665d16e9):
       - `db.conversations.findOne({conversationId: "<id>"})` — capture timestamps, model, user, agent_id
       - `db.messages.find({conversationId: "<id>"}).sort({createdAt: 1})` — every message with role, tokenCount, content[] shape, error fields, files/attachments, createdAt
       - `db.transactions.find({conversationId: "<id>"})` — all transactions linked to this conversation (may be zero)
       - `db.files.find({user: ObjectId("<penelopeUserId>"), createdAt: {$gte: ISODate("2026-04-16T18:30:00Z"), $lte: ISODate("2026-04-16T18:45:00Z")}})` — file sizes, mime types, dimensions

    3. Compute the accounting delta:
       - starting tokenCredits at 18:35:20 (after the last documented transaction) = 217,391 − 23,275 = 194,116
       - ending tokenCredits at 18:42:38 (current state today) = 0
       - missing: 194,116 credits across convs 4594a8db, 61046315, 665d16e9
       - Assign each of the 3 incident convs a suspected credit cost based on image vision tokens (research estimates ~9,400 input tokens per message with 2 big photos = ~9,400 credits at rate=1, plus output at rate=5 but output was zero)
       - If the math adds up to ~20–30K credits, then ~160K credits are STILL unexplained — possibly streaming-aborted long responses. If it adds up to ~194K, then these 3 convs fully account for the drain and the issue is purely "pre-deducted without transaction write".

    4. Cross-reference LibreChat balance-deduction semantics:
       - Check LibreChat's docker image version from Railway (`railway variables --service librechat | grep -iE "IMAGE|VERSION"`) and log in report.
       - Check LibreChat's GitHub issues for "balance drain", "pre-deduct", "streaming abort refund": https://github.com/danny-avila/LibreChat/issues?q=is%3Aissue+balance+drain and note any matches in the report.

    5. CLASSIFY the drain into one of four buckets:
       - A. "All explained by message-level vision cost" — math adds up; transactions should exist but don't (LibreChat write bug)
       - B. "Explained by streaming aborts" — responses were started (Anthropic charged) but aborted before LibreChat wrote transactions
       - C. "Pre-deduction without refund" — LibreChat reserved balance up-front, API call errored (e.g. Anthropic rejected oversize image), no refund path
       - D. "Unknown / insufficient evidence"

       The classification determines the defensive fix in Task 2.

    6. Write the report to 19-04-DRAIN-FORENSICS.md with sections: "Timeline", "Per-Conversation Accounting", "Accounting Delta Summary", "Classification", "Evidence Log", "LibreChat Version + Known Issues".
  </action>
  <verify>
    <automated>test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md &amp;&amp; grep -qE "Classification:" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md &amp;&amp; grep -qE "Timeline|Evidence Log|Accounting" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md &amp;&amp; test $(wc -l &lt; .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md) -ge 60</automated>
  </verify>
  <acceptance_criteria>
    - `19-04-DRAIN-FORENSICS.md` exists, at least 60 lines
    - Has sections named exactly: "Timeline", "Per-Conversation Accounting", "Accounting Delta Summary", "Classification", "Evidence Log", "LibreChat Version + Known Issues"
    - "Timeline" contains at least 10 entries between 18:33 and 18:45 UTC on 2026-04-16, each with a timestamp and what happened (drawn from both logs AND MongoDB evidence)
    - "Per-Conversation Accounting" has one subsection per conversation (3aa0b4f3, 4594a8db, 61046315, 665d16e9), each listing: message count, tokenCount values, transaction records count, files attached, error/aborted flags
    - "Accounting Delta Summary" states explicitly: "Of 194,116 unexplained credits, {N} are attributable to [source] and {M} are still unexplained"
    - "Classification" is one of A/B/C/D exactly (see action), with justification
    - "Evidence Log" file `19-04-LOGS-LIBRECHAT.txt` exists OR a statement "Railway log retention exceeded; no log evidence available" appears in the report
    - LibreChat docker image version is recorded in the final section
  </acceptance_criteria>
  <done>We know (or have exhaustively documented) what happened to the 194,116 credits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement at least one defensive guardrail based on the Task 1 classification</name>
  <files>
    (If guardrail is "image size limit") Gist librechat.yaml (new revision),
    (If guardrail is "balance floor fuse") src/lib/budget.ts (new function + admin call-site),
    (If guardrail is "image dimension limit") Gist librechat.yaml,
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md,
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-UPSTREAM-REPORT.md (if classification C)
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md (classification drives the choice)
    - src/lib/budget.ts (to understand where a floor-fuse would hook in)
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-01-GIST-AFTER.yaml (the current-live yaml we would edit; from Plan 19-01)
    - LibreChat docs on fileConfig: https://www.librechat.ai/docs/configuration/librechat_yaml/object_structure/fileconfig
  </read_first>
  <action>
    Choose EXACTLY ONE guardrail based on the Task 1 classification:

    - Classification A (vision cost, transaction write bug) → Guardrail: limit image file sizes via librechat.yaml fileConfig.
      Edit yaml: add `fileConfig.serverFileSizeLimit: 2` (MB) and `fileConfig.endpoints.default.fileSizeLimit: 2` (MB). Also add `fileConfig.avatarSizeLimit: 2` for completeness. Push new Gist rev + update CONFIG_PATH + redeploy LibreChat (same procedure as Plan 19-01 Task 2). Cap image vision tokens per request by capping file size.

    - Classification B (streaming aborts) → Guardrail: lower maxContextTokens (Plan 19-01 set 8000; lower to 4000) AND document the upstream issue.
      New Gist revision, push, update CONFIG_PATH, redeploy. Goal: cap the max possible streaming response and thus cap per-request blast radius.

    - Classification C (pre-deduction no refund) → Guardrail: balance floor fuse in our code.
      Add `setMinimumBalanceFloor(userId, floorCredits, db)` to src/lib/budget.ts and invoke it from a short-interval admin cron or inside topUpDailyBudget to prevent balances from dropping below floor without explicit admin override. ALSO write the body of an upstream LibreChat issue to `.planning/phases/19-.../19-04-UPSTREAM-REPORT.md` (do NOT file without user approval).

    - Classification D (unknown) → Guardrail: image size limit (same as A). Rationale: "we do not know cause but big images correlate, so cap them".

    Record the choice in `.planning/phases/19-.../19-04-GUARDRAIL.md` with sections:
      - `## Classification`: A/B/C/D
      - `## Guardrail Chosen`: name
      - `## Rationale`: 3–5 sentences
      - `## Implementation`: exact commits, diffs, yaml edits
      - `## Verification`: live proof (e.g. upload a 5MB image and get rejected; or run a script that tries to push tokenCredits below the floor and is blocked)

    IMPORTANT: Do NOT skip this task. Even Classification D mandates a guardrail. Phase goal "kids budget survives" is unmet if the drain mechanism is unmitigated.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md &amp;&amp; grep -qE "^## Classification" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md &amp;&amp; grep -qE "^## Guardrail Chosen" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md &amp;&amp; grep -qE "^## Implementation" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md &amp;&amp; grep -qE "^## Verification" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md</automated>
  </verify>
  <acceptance_criteria>
    - `19-04-GUARDRAIL.md` exists with all five sections (Classification, Guardrail Chosen, Rationale, Implementation, Verification) as `##` headings
    - "Classification" value matches the classification in 19-04-DRAIN-FORENSICS.md
    - "Implementation" section shows at least ONE concrete change was deployed: either a new Gist commit hash (with fileConfig entries grep-verifiable in the new yaml) OR a new exported function in src/lib/budget.ts (grep: `export async function setMinimumBalanceFloor` returns 1+ matches)
    - "Verification" section shows evidence the guardrail is LIVE (post-deploy proof, not just "should work") — e.g. a curl-upload response showing rejection, or a script output showing the floor blocked a write
    - If a new yaml revision was pushed, CONFIG_PATH on the LibreChat service must reflect the new hash (per Plan 19-01 procedure). Verify with `railway variables --service librechat | grep CONFIG_PATH` and record in Verification section.
    - If classification C was chosen, `19-04-UPSTREAM-REPORT.md` also exists (even if not filed; a draft body is sufficient)
  </acceptance_criteria>
  <done>At least one defensive guardrail is live; drain blast radius is capped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Confirm or rule out extended-thinking tokens as a hidden undercount (Open Question #5)</name>
  <files>
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-RESEARCH.md (Open Question #5)
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md (for message-level content[] shape evidence already captured)
  </read_first>
  <action>
    Quick factual confirmation task. Research Open Q #5 flagged: Claude responses contain `{"type":"think","think":"..."}` content blocks (~131 and 265 tokens observed). We need to confirm these are rolled into the completion token count that LibreChat bills at rate=5, or identify if they are NOT counted.

    Steps:

    1. Pick 3 transactions from today with completion records. For each, correlate to the corresponding message's content[] array in the messages collection.
    2. Compute: is `sum(chars_across_all_content_blocks) / 4` approximately equal to the transaction's `rawAmount` for `tokenType: "completion"`? If yes → thinking IS counted. If no → thinking may be omitted.
       - Approximate check: content blocks total ~X chars → ~X/4 tokens → ~X/4 × rate=5 credits. Compare to transaction.tokenValue.
    3. Alternative confirmation: check the Anthropic response message's usage metadata if it's stored on the message doc. Anthropic returns `usage.output_tokens` which includes thinking. If LibreChat uses that number directly for the transaction, thinking IS counted.

    Write a 20–40 line conclusion to `.planning/phases/19-.../19-04-THINKING-TOKENS.md` with:
      - `## Methodology`: how we checked
      - `## Evidence`: 3 transaction+message pairs with the math
      - `## Conclusion`: "Extended thinking IS / IS NOT counted in completion token billing"
      - `## Impact`: if IS NOT, estimate the daily undercount. If IS, close the open question.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md &amp;&amp; grep -qE "^## Methodology" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md &amp;&amp; grep -qE "^## Conclusion" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md &amp;&amp; grep -qiE "IS counted|IS NOT counted" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md</automated>
  </verify>
  <acceptance_criteria>
    - `19-04-THINKING-TOKENS.md` exists with Methodology / Evidence / Conclusion / Impact sections
    - Conclusion explicitly states either "IS counted" or "IS NOT counted" (one of those exact phrases)
    - Evidence section includes at least 3 transaction IDs or (_id + message.content summary) pairs with the char→token→credit math shown
  </acceptance_criteria>
  <done>Open Question #5 from research is closed with either confirmation or quantified impact of thinking-token undercount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review — forensic report quality, guardrail live, drain root cause understood (or exhaustively documented as not)</name>
  <files>
    (no file edits — verification only)
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-DRAIN-FORENSICS.md
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-GUARDRAIL.md
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-THINKING-TOKENS.md
  </read_first>
  <action>
    Pause for parent review. Do NOT close the phase until resume-signal is received.

    Parent to review:

    1. Read 19-04-DRAIN-FORENSICS.md. Is the classification (A/B/C/D) well-supported by the evidence? If the timeline entries or accounting do not convince you, flag in resume signal.

    2. Check the guardrail is LIVE. Depending on classification:
       - A or D: upload a 5MB image via LibreChat test mode or as a kid user — LibreChat should reject it with a file-too-large error. If it accepts, the yaml fileConfig did not take effect.
       - B: start a long conversation (>10 turns) and confirm the context window does not grow unbounded — check a transaction's prompt tokenCount plateaus around 4000 (not 8000+).
       - C: try to manually set a child's tokenCredits to a value below the floor (via admin action or direct DB write attempt from within the app); it should be blocked.

    3. Read 19-04-THINKING-TOKENS.md. Is the conclusion definitive? If "IS NOT counted" and the daily undercount is material (>5% of daily budget), open a STATE.md concern for a future phase.

    4. Optional — attempt a careful reproduction of the drain:
       - As Penelope, upload a 2MB photo (or larger if fileConfig permits), send a study question. Watch the balance before and after. If balance drops by more than 20K credits without a transaction record, the underlying bug is not fixed (only capped). Note this and decide whether to file upstream now vs later.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — awaits human resume-signal; no automated gate."</automated>
  </verify>
  <done>Parent has typed "approved" (or equivalent) in response to the resume-signal.</done>
  <what-built>
    - 19-04-DRAIN-FORENSICS.md — timeline correlation + classification
    - 19-04-LOGS-LIBRECHAT.txt — raw log evidence (or its absence documented)
    - 19-04-GUARDRAIL.md — defensive fix applied, with live verification
    - 19-04-THINKING-TOKENS.md — Open Q #5 closed
    - (If chosen) new Gist librechat.yaml revision with fileConfig limit; LibreChat redeployed
    - (If chosen) new function setMinimumBalanceFloor in src/lib/budget.ts
  </what-built>
  <how-to-verify>
    See `<action>` steps 1–4 above.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which check failed and what you saw.</resume-signal>
  <acceptance_criteria>
    - Parent has approved
    - Guardrail is live (step 2 passes)
    - Forensic classification and thinking-token conclusion both definitive
  </acceptance_criteria>
</task>

</tasks>

<verification>
- 19-04-DRAIN-FORENSICS.md has a definitive classification
- 19-04-GUARDRAIL.md shows a live, verified defensive change
- 19-04-THINKING-TOKENS.md closes Open Q #5
- Live LibreChat rejects oversized uploads (or whatever guardrail was chosen is live)
</verification>

<success_criteria>
- We have a defensible answer to "where did the 194k credits go", even if the answer is "LibreChat bug we cannot fix without source access, guardrail limits blast radius to X"
- Daily budget blast radius from any single failure mode is capped
- No open questions from research remain undocumented
</success_criteria>

<output>
After completion, create `.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-04-SUMMARY.md`
</output>
