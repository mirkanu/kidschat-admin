---
phase: 19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/cron/daily-reset/route.ts
  - (optional, additive) src/lib/budget.ts — observability log
  - (external) Railway kidschat-admin-production cron "daily-reset"
  - (MongoDB) balance_state docs for Sebastian + Penelope (restore lastDailyReset, trigger top-up)
autonomous: false

must_haves:
  truths:
    - "Penelope's tokenCredits are restored to at least eurToTokens(dailyCostCapEur) right now"
    - "balance_state.lastDailyReset for both children advances to today's UTC date"
    - "Root cause of the 5-day cron staleness is identified and recorded (was the cron not firing, failing auth, hitting a 500, or updating nothing due to $max being a no-op when credits already high)"
    - "The daily-reset cron is verifiably firing again (we can see log lines dated after the fix)"
    - "Observability: daily-reset route logs a `lastRun` timestamp to MongoDB (or equivalent) so silent failures are detectable in the admin UI"
  artifacts:
    - path: "src/app/api/cron/daily-reset/route.ts"
      provides: "Writes cron_state.daily_reset.lastRunAt on each invocation (observability)"
      contains: "daily_reset"
    - path: ".planning/phases/19-.../19-03-CRON-DIAGNOSIS.md"
      provides: "Documented root cause with evidence from Railway logs"
    - path: "(MongoDB) balance_state for Penelope"
      provides: "lastDailyReset = today UTC; linked balances.tokenCredits ≥ eurToTokens(0.50)"
  key_links:
    - from: "Railway cron schedule (railway.toml: 0 0 * * *)"
      to: "POST /api/cron/daily-reset"
      via: "curl -X POST with x-cron-secret header to admin URL"
      pattern: "curl.*/api/cron/daily-reset"
    - from: "daily-reset route"
      to: "balance_state.lastDailyReset"
      via: "topUpDailyBudget -> $set lastDailyReset"
      pattern: "lastDailyReset"
---

<objective>
Diagnose why the daily-reset cron has not run (or not updated lastDailyReset) for 5 days, repair the cron pipeline, restore Penelope's balance immediately, and add observability so this failure mode becomes visible next time.

Purpose: Research Area 6 showed `balance_state.lastDailyReset` is frozen at 2026-04-11 for both children while today is 2026-04-16. Penelope has 0 tokenCredits. This is blocking restoration irrespective of the fixes in Plans 01 and 02. We must fix the cron plumbing AND add a minimal observability hook so the admin can glance at the dashboard and see whether the cron fired today.

Output: Diagnosis file, a cron_state observability record, an updated daily-reset route that writes lastRunAt, and confirmed non-zero balances for both children with today's UTC date in balance_state.lastDailyReset.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-RESEARCH.md
@.planning/STATE.md

STATE.md critical notes:
- Phase 15.x: railway.toml config-as-code for daily-reset + monthly-reset crons (NOT the dashboard).
- Phase 15.x: Railway cron schedules require manual dashboard configuration — NO CLI/GraphQL API support available. THIS IS NOW OUT OF DATE if railway.toml [[deploy.crons]] works, but verify.
- Phase 17: Railway internal MongoDB accessible externally via switchyard.proxy.rlwy.net:57501.
- Phase 17: librechat_safe MongoDB user — LibreChat uses restricted creds; admin app uses separate full-access creds.
- Phase 15.4: Monthly cap enforcement choke point is daily-reset cron at midnight UTC; topUpDailyBudget gates on displayedMonthlySpendEur >= monthlyCostCapEur.

@src/app/api/cron/daily-reset/route.ts
@src/lib/budget.ts
@railway.toml

<interfaces>
From railway.toml — current schedule:
[[deploy.crons]]
name = "daily-reset"
schedule = "0 0 * * *"
command = "curl -fsS -X POST -H 'x-cron-secret: $CRON_SECRET' https://kidschat-admin-production.up.railway.app/api/cron/daily-reset"

From src/app/api/cron/daily-reset/route.ts — current behavior:
- Reads CRON_SECRET from env, compares to x-cron-secret header; 401 on mismatch
- Iterates all non-ADMIN users from users collection
- For each: accumulateYesterdaySpend(userId, db) then topUpDailyBudget(userId, db)
- Returns {reset, accumulated, errors} JSON

From STATE.md "cron_state" collection used for polling (Phase 15.x 15-04):
cron_state.poll_listener.lastSeenAt  — used by the in-process poller (now deleted per 15.3)
We will add a new key:
cron_state.daily_reset.lastRunAt     — new observability field for this plan
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnose — pull Railway logs and determine why daily-reset has not updated lastDailyReset for 5 days</name>
  <files>
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md
  </files>
  <read_first>
    - railway.toml (confirm cron schedule and command)
    - src/app/api/cron/daily-reset/route.ts (confirm route behavior)
    - src/lib/budget.ts (confirm topUpDailyBudget DOES $set lastDailyReset; confirm monthly-cap short-circuit path ALSO sets it — research noted this is the path that would update the field even when $max is a no-op)
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-RESEARCH.md (Area 6 Open Questions #2)
    - .planning/STATE.md (Phase 15.x notes on Railway cron)
  </read_first>
  <action>
    Investigate and write the findings to `.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md`. Do NOT change any code yet.

    Diagnostic steps (execute them all; record evidence for each):

    1. VERIFY THE RAILWAY CRON IS DEFINED AND ACTIVE ON THE SERVICE. Use the Railway CLI (per CLAUDE.md automate-Railway rule):
       - `railway service kidschat-admin-production` (select the service)
       - `railway logs --service kidschat-admin-production --deployment` and filter for lines matching `daily-reset`, `/api/cron/daily-reset`, or `reset=` between 2026-04-11 00:00 UTC and now.
       - If Railway CLI `logs` lacks filtering, pipe through grep: `railway logs --service kidschat-admin-production 2>&1 | grep -iE "daily-reset|cron|reset="`
       - Record the TIMESTAMPS of every `[daily-reset]` log line found in DIAGNOSIS.md under heading "Log Evidence — daily-reset invocations".
       - If there are ZERO log lines for 5 days → the cron is not firing at all (Railway config issue).
       - If there are log lines but they show errors (401 Unauthorized, 500, etc.) → the cron IS firing but failing (auth or runtime issue).
       - If there are log lines with `reset=N, accumulated=M, errors=0` and no errors BUT lastDailyReset is still April 11 → our code has a silent bug (e.g. a missed upsert, or the $set path is gated on monthly cap without also advancing date).

    2. CROSS-CHECK MONGO STATE. Connect via switchyard proxy. Record:
       - `db.balance_state.find({}, {userId: 1, lastDailyReset: 1, monthlySpendEur: 1, _id: 0})` — capture dates
       - `db.balances.find({}, {user: 1, tokenCredits: 1, _id: 0})` — capture current credit levels
       - Log these snapshots under "MongoDB Evidence" in DIAGNOSIS.md.
       - Compute: for each child, was monthlySpendEur >= monthlyCostCapEur on April 11? If yes → the monthly-cap gate in topUpDailyBudget fired. Re-read budget.ts:240-260 — note that the monthly-cap short-circuit DOES update lastDailyReset ($set). So even that path should advance the date. If it didn't, the cron never called this route at all.

    3. ATTEMPT MANUAL TRIGGER (READ-ONLY — just see what happens). From a machine that can reach the admin URL:
       - `curl -X POST -H "x-cron-secret: $CRON_SECRET" -v https://kidschat-admin-production.up.railway.app/api/cron/daily-reset`
       - Record HTTP status, response body, and full curl trace.
       - If 401 → CRON_SECRET mismatch between railway.toml env and admin service env.
       - If 200 → endpoint IS reachable and auth works; the cron schedule itself is not firing the curl.
       - If 500 / timeout → runtime error in the endpoint.
       - Do NOT retry repeatedly; one trigger is enough for diagnosis. Penelope restoration happens in Task 2.

    4. CHECK RAILWAY CRON-SERVICE CONFIG. Per STATE.md older note, Railway cron schedules may need dashboard config. Newer Railway supports `[[deploy.crons]]` in railway.toml; verify via `railway status --json` or Railway GraphQL API:
       - Query: `{ project { services { edges { node { name cronSchedule } } } } }` (adjust per Railway's current GraphQL schema).
       - If the kidschat-admin-production service has NO active cron schedule attached despite railway.toml declaring it, log this as the root cause. Fix: redeploy the admin service via `railway up --service kidschat-admin-production` to pick up railway.toml (serviceInstanceRedeploy may reuse cached image without re-reading railway.toml — per STATE.md Phase 15.x note).

    5. WRITE THE VERDICT SECTION in DIAGNOSIS.md:
       - "Root cause: <X>"
       - "Evidence: <log lines / mongo state / curl output>"
       - "Recommended fix: <a / b / c>"
       - "Blast radius: <X>"
  </action>
  <verify>
    <automated>test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md && grep -qiE "root cause|verdict" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md && grep -qE "Log Evidence|MongoDB Evidence|Manual Trigger" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `19-03-CRON-DIAGNOSIS.md` exists with the following headings (exact strings): "Log Evidence", "MongoDB Evidence", "Manual Trigger", "Root Cause", "Recommended Fix"
    - "Log Evidence" section contains at least one of: (a) a statement "No daily-reset log entries between 2026-04-11 and $(today) — cron not firing" with proof of how that was determined, OR (b) a list of actual log-line timestamps and their status codes
    - "MongoDB Evidence" contains the lastDailyReset value for both children AND their current tokenCredits — raw copy-paste from the query result
    - "Manual Trigger" contains the full curl command, HTTP status code, and response body from a manual POST to /api/cron/daily-reset
    - "Root Cause" explicitly identifies ONE of: (a) cron schedule inactive on Railway service, (b) CRON_SECRET mismatch, (c) endpoint 500, (d) endpoint 200 but silent bug in topUpDailyBudget — NO other verdicts allowed; if evidence is inconclusive, say "inconclusive — recommend adding observability in Task 2 and re-diagnose on next occurrence"
    - "Recommended Fix" maps to a concrete, executable action (e.g. "redeploy admin service with `railway up --service kidschat-admin-production`" or "update CRON_SECRET to match")
  </acceptance_criteria>
  <done>We know why the cron is stale and what to do about it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restore Penelope's balance immediately + apply the recommended fix from diagnosis</name>
  <files>
    (MongoDB) balances, balance_state for Penelope (and Sebastian if stale),
    (Railway) whatever the diagnosis recommends (CRON_SECRET update, redeploy, etc.),
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-RESTORE-EVIDENCE.md,
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-FIX-APPLIED.md
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md (THE recommended fix)
    - src/lib/budget.ts (topUpDailyBudget semantics — it uses $max which will preserve parent top-ups above the cap)
    - src/app/(dashboard)/users/[userId]/top-up-button.tsx (the existing admin UI path; already has a topUpChildBalance action that adds 0.10 EUR via $inc)
  </read_first>
  <action>
    Two things, in this order:

    (A) RESTORE PENELOPE IMMEDIATELY (do this BEFORE fixing the cron — her budget is at 0 and this is the user-facing pain point):

    Approach: Call the daily-reset endpoint for JUST Penelope. Two options; pick whichever works:

    Option 1 (preferred — uses the production code path, proves it works):
    Manually POST to /api/cron/daily-reset with the cron secret. This iterates all children. After it runs, Penelope's and Sebastian's tokenCredits should be ≥ eurToTokens(0.50) ≈ 543,478 (assuming Plan 02 has already raised the cap; if Plan 02 is still in-flight, the values will be eurToTokens(whatever is currently live) ≈ 217,391 at 0.20). Both cases are acceptable — the goal is NON-ZERO.

    Option 2 (fallback — direct top-up):
    Use the admin dashboard's existing TopUpButton (click "Top up €0.10" on Penelope's user page) or invoke `topUpChildBalance(penelopeUserId, 0.50)` directly. This uses `$inc` and does NOT advance `lastDailyReset`, so it restores credits but not observability — only use this if Option 1 fails.

    Verify post-restoration:
    - `db.balances.findOne({user: ObjectId(penelopeUserId)})` shows `tokenCredits > 0`
    - `db.balance_state.findOne({userId: penelopeUserId})` shows `lastDailyReset` = today UTC (if Option 1 was used)

    Capture before/after snapshots in `.planning/phases/19-.../19-03-RESTORE-EVIDENCE.md`.

    (B) APPLY THE DIAGNOSED FIX. Based on 19-03-CRON-DIAGNOSIS.md "Recommended Fix":
    - If fix is "redeploy admin service": `railway up --service kidschat-admin-production` (per STATE.md Phase 15.x: serviceInstanceRedeploy reuses cached image → must use `railway up` for config reload).
    - If fix is "update CRON_SECRET": use Railway GraphQL variableUpsert (same pattern as deploy route at src/app/api/prompt-editor/deploy/route.ts:144) to align the secret between the admin service and the cron invocation. DO NOT manually edit the dashboard.
    - If fix is "endpoint 500": read logs, fix the error in src/app/api/cron/daily-reset/route.ts, commit, redeploy.
    - If fix is "no cron schedule attached on Railway service": redeploy via `railway up` to pick up `[[deploy.crons]]` from railway.toml. If that doesn't attach the cron, add `[cron]` or whatever Railway's current config-as-code schema requires (check Railway docs / railway.json schema).
    - If "inconclusive": skip and move to Task 3 (observability will catch it next time).

    Record the fix applied and verification (run another manual POST, confirm balance_state.lastDailyReset updates) in `.planning/phases/19-.../19-03-FIX-APPLIED.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-RESTORE-EVIDENCE.md && grep -qE "tokenCredits.*[1-9]" .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-RESTORE-EVIDENCE.md && test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-FIX-APPLIED.md</automated>
  </verify>
  <acceptance_criteria>
    - `19-03-RESTORE-EVIDENCE.md` exists with "Before" and "After" sections
    - "Before" section shows a MongoDB snapshot of Penelope's `balances.tokenCredits` with value `0` (or whatever it was pre-restore)
    - "After" section shows Penelope's `balances.tokenCredits` > 100000 (i.e. a non-trivial positive number — either 217,391 or 543,478 depending on current dailyCostCapEur)
    - `19-03-FIX-APPLIED.md` exists; describes which of the diagnosis's "Recommended Fix" options was applied, with the exact command run AND its exit code / output
    - A second manual POST to /api/cron/daily-reset AFTER the fix returns 200 and Penelope's `balance_state.lastDailyReset` becomes today's UTC date. Record this post-fix verification in FIX-APPLIED.md under "Post-Fix Verification".
  </acceptance_criteria>
  <done>Penelope is unblocked; the underlying cron pipeline is fixed (or at least the inconclusive state is documented).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add observability — daily-reset route writes cron_state.daily_reset.lastRunAt on every invocation</name>
  <files>
    src/app/api/cron/daily-reset/route.ts,
    .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-OBSERVABILITY-EVIDENCE.md
  </files>
  <read_first>
    - src/app/api/cron/daily-reset/route.ts (current implementation — will extend it)
    - .planning/STATE.md (Phase 15.x note: cron_state collection exists from the poll_listener era; we reuse it)
  </read_first>
  <action>
    Extend `src/app/api/cron/daily-reset/route.ts` to write an observability record on every invocation. Minimal additive change:

    At the end of the POST handler, AFTER the loop but BEFORE the `return NextResponse.json(...)`, add:

    ```ts
    // Phase 19 observability — record last successful run so silent cron failures are detectable.
    try {
      await db.collection("cron_state").updateOne(
        { key: "daily_reset" },
        {
          $set: {
            key: "daily_reset",
            lastRunAt: new Date(),
            lastRunStats: { reset, accumulated, errors: errors.length },
          },
        },
        { upsert: true }
      );
    } catch (err) {
      // Non-fatal — don't fail the cron if observability write fails.
      console.error("[daily-reset] Failed to write cron_state:", err);
    }
    ```

    Do NOT:
    - Change the auth gate, loop, or return shape.
    - Add a new collection — reuse existing `cron_state` per STATE.md precedent (the `poll_listener` key used by the deleted Phase 15.3 poller is orthogonal; `daily_reset` is a NEW key in the same collection).
    - Add a separate "monthly_reset" key — out of scope for this plan; can be done in a follow-up.

    After the code change:
    1. Run `npx tsc --noEmit -p .` — must be clean.
    2. Commit and redeploy: `railway up --service kidschat-admin-production` (per the same reasoning as Task 2 — we need config/code reload, and serviceInstanceRedeploy may reuse cached image).
    3. Manually POST to /api/cron/daily-reset again and verify `db.cron_state.findOne({key: "daily_reset"})` returns a fresh `lastRunAt` ISO timestamp with `lastRunStats: {reset: N, accumulated: M, errors: 0}`.
    4. Record the post-deploy verification in `.planning/phases/19-.../19-03-OBSERVABILITY-EVIDENCE.md`.
  </action>
  <verify>
    <automated>grep -qE "cron_state" src/app/api/cron/daily-reset/route.ts && grep -qE "lastRunAt" src/app/api/cron/daily-reset/route.ts && grep -qE "daily_reset" src/app/api/cron/daily-reset/route.ts && npx tsc --noEmit -p . && test -f .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-OBSERVABILITY-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cron_state" src/app/api/cron/daily-reset/route.ts` returns ≥ 1
    - `grep -c "lastRunAt" src/app/api/cron/daily-reset/route.ts` returns ≥ 1
    - `grep -c "daily_reset" src/app/api/cron/daily-reset/route.ts` returns ≥ 1
    - `npx tsc --noEmit -p .` exits 0
    - The observability write is inside a try/catch that does NOT fail the response (grep for `// Non-fatal` or the try/catch pattern)
    - `19-03-OBSERVABILITY-EVIDENCE.md` exists and contains the output of `db.cron_state.findOne({key: "daily_reset"})` from a post-deploy manual trigger; the doc shows `lastRunAt` is a date after today's redeploy time
  </acceptance_criteria>
  <done>Next time the cron goes silent, the admin can detect it by reading cron_state.daily_reset.lastRunAt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — Penelope has credits, cron fires daily, observability visible</name>
  <files>
    (no file edits — verification only)
  </files>
  <read_first>
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-CRON-DIAGNOSIS.md
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-RESTORE-EVIDENCE.md
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-FIX-APPLIED.md
    - .planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-OBSERVABILITY-EVIDENCE.md
  </read_first>
  <action>
    Pause for parent verification. Do NOT proceed to the next wave / plan until the resume-signal is received.

    Parent to verify:

    1. Open the admin dashboard, go to /users — confirm Penelope's balance row shows a non-zero balance (not the "0.00 EUR" that was symptomatic of this phase).
    2. Go to /users/{penelopeId}; confirm the detail page shows credits. If Plan 19-02 shipped, cap should be 0.50.
    3. Ask Penelope to send one message in LibreChat; confirm it actually works (no "Insufficient Funds" block).
    4. Check cron_state observability:
       - Open a DB client (or use a quick admin route if one exists) and look at `db.cron_state.findOne({key: "daily_reset"})`. Confirm `lastRunAt` is today.
    5. Schedule wait — leave overnight. Tomorrow morning, check:
       - `balance_state.lastDailyReset` for both children = today's (tomorrow's) UTC date
       - `cron_state.daily_reset.lastRunAt` ≈ midnight UTC tonight
       - Both children have credits restored
       If any of these fail, the Railway cron is still not firing on schedule and Task 1's diagnosis may need revisiting.

    Approve if steps 1–4 pass AND step 5 passes the next day. Partial approval (1–4 OK, 5 pending) is acceptable — open a follow-up in STATE.md to check 5 tomorrow.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — awaits human resume-signal; no automated gate."</automated>
  </verify>
  <done>Parent has typed "approved" or "approved-pending-overnight".</done>
  <what-built>
    - 19-03-CRON-DIAGNOSIS.md with the root cause and recommended fix
    - 19-03-RESTORE-EVIDENCE.md with Penelope's before/after balance
    - 19-03-FIX-APPLIED.md documenting the fix applied
    - 19-03-OBSERVABILITY-EVIDENCE.md with a fresh cron_state.daily_reset.lastRunAt
    - src/app/api/cron/daily-reset/route.ts updated to write cron_state
  </what-built>
  <how-to-verify>
    See `<action>` steps 1–5 above.
  </how-to-verify>
  <resume-signal>Type "approved" (after step 5 verified) or "approved-pending-overnight" (if 1–4 OK, checking 5 tomorrow) or describe failures.</resume-signal>
  <acceptance_criteria>
    - Parent has approved
    - Penelope's balance is non-zero in the admin UI
    - cron_state.daily_reset.lastRunAt is today's date
  </acceptance_criteria>
</task>

</tasks>

<verification>
- cron_state.daily_reset.lastRunAt is fresh
- Penelope tokenCredits > 0
- balance_state.lastDailyReset = today UTC for both children
- Diagnosis file documents the root cause
</verification>

<success_criteria>
- Immediate: Penelope unblocked
- Near-term: Cron fires reliably at midnight UTC and observability proves it
- Long-term: Next silent cron failure is detectable via cron_state.daily_reset.lastRunAt freshness
</success_criteria>

<output>
After completion, create `.planning/phases/19-investigate-why-kids-20c-daily-budget-exhausts-after-2-3-que/19-03-SUMMARY.md`
</output>
