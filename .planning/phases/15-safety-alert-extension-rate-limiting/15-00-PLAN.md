---
phase: 15-safety-alert-extension-rate-limiting
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - jest.config.js
  - jest.setup.ts
  - tests/lib/safety-patterns.test.ts
  - tests/lib/rate-limits.test.ts
  - tests/lib/cost-ledger.test.ts
  - tests/lib/bonus-purchases.test.ts
  - tests/api/weekly-digest.test.ts
  - scripts/mongo-inspect.ts
  - .planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Running `npm test` executes Jest and reports a green suite (even if only placeholder specs)"
    - "Empty test files exist for every lib/route that Plan 01 and Plan 02 will create"
    - "Developer has inspected live Railway MongoDB and documented actual field names for balance/aclentries/messages/files"
    - "Synthetic message insertion test confirms LibreChat UI renders admin-inserted messages (go/no-go for Pattern 8)"
  artifacts:
    - path: "jest.config.js"
      provides: "Next.js-compatible Jest config"
    - path: "tests/lib/safety-patterns.test.ts"
      provides: "Test stub for Plan 01 safety pattern work"
    - path: "tests/lib/rate-limits.test.ts"
      provides: "Test stub for effective-limit resolver"
    - path: "tests/lib/cost-ledger.test.ts"
      provides: "Test stub for cost ledger"
    - path: "tests/lib/bonus-purchases.test.ts"
      provides: "Test stub for weekly bonus aggregation"
    - path: "tests/api/weekly-digest.test.ts"
      provides: "Test stub for weekly digest extension"
    - path: ".planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md"
      provides: "Documented field names from live DB + synthetic message verdict"
  key_links:
    - from: "package.json scripts.test"
      to: "jest"
      via: "npm test invocation"
      pattern: "\"test\":\\s*\"jest"
---

<objective>
Wave 0 infrastructure for Phase 15. Establish Jest as the test runner, create empty test files for every lib Plan 01/02 will build, and verify live MongoDB field shapes + synthetic message delivery via a one-shot inspection script. Nothing enforcement-related ships until this plan is green, because Plan 02's enforcement code depends on the actual field names (`user` vs `principalId`, `tokenCredits` vs `credit`) and on confirmation that admin-inserted messages render in the child's LibreChat UI.

Purpose: Nyquist compliance — every Plan 01/02 task has an `<automated>` target, and Plan 02's enforcement architecture is validated against the real DB before any code is written against assumed field names.
Output: Jest harness + 5 test stubs + MONGO-INSPECTION.md documenting live field shapes + human sign-off.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-RESEARCH.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-VALIDATION.md
@.planning/phases/14-enable-safeguard-image-generation/14-01-SUMMARY.md
@package.json
@src/lib/mongodb.ts
@src/lib/safety-patterns.ts

<interfaces>
<!-- Live field shapes are UNKNOWN until the inspection task runs. -->
<!-- Phase 14 delivered the 4 drawing agent IDs the enforcement code targets: -->

Drawing agent IDs (from Phase 14 SUMMARY):
```typescript
const DRAWING_AGENT_IDS = [
  "agent_wxgt6su7d3pcosiil3", // Friendly Tutor
  "agent_y4w1cvoyg77p9thed9", // Casual Buddy
  "agent_64q6z5s57552cpgl0hr", // Balanced Helper
  "agent_aiv99mzvdzquym6y89k", // Standard Formal
];
```

MongoDB client pattern (from src/lib/mongodb.ts):
```typescript
import { getMongoClient } from "@/lib/mongodb";
const db = (await getMongoClient()).db("test");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Jest and create empty test stubs</name>
  <files>package.json, jest.config.js, jest.setup.ts, tests/lib/safety-patterns.test.ts, tests/lib/rate-limits.test.ts, tests/lib/cost-ledger.test.ts, tests/lib/bonus-purchases.test.ts, tests/api/weekly-digest.test.ts</files>
  <action>
    Install Jest with Next.js TS preset (no watch mode, no UI runner).

    1. `npm install -D jest @types/jest ts-jest @jest/globals` (pin to stable versions compatible with the Next.js version in package.json — check it first).
    2. Add `"test": "jest --passWithNoTests"` and `"test:watch": "jest --watch"` to package.json scripts. The default `npm test` MUST NOT run in watch mode (it would hang CI / Nyquist sampling).
    3. Create `jest.config.js` with:
       - `preset: "ts-jest"`
       - `testEnvironment: "node"`
       - `testMatch: ["**/tests/**/*.test.ts"]`
       - `moduleNameMapper: { "^@/(.*)$": "<rootDir>/src/$1" }` (to match the `@/` alias used by the Next.js codebase)
       - `setupFilesAfterEach: ["<rootDir>/jest.setup.ts"]`
    4. Create `jest.setup.ts` with a single `jest.setTimeout(15000)` (MongoDB mocks may be slow).
    5. Create the five test stub files, each containing exactly one placeholder test so Jest reports PASS:

    ```typescript
    // tests/lib/safety-patterns.test.ts
    describe("safety-patterns", () => {
      it.todo("detects violent image prompts");
      it.todo("detects nudity image prompts");
      it.todo("detects horror image prompts");
      it.todo("detects real-person image prompts");
      it.todo("detects bypass framing image prompts");
      it("placeholder passes so suite is green", () => { expect(true).toBe(true); });
    });
    ```

    Mirror this shape for `rate-limits.test.ts` (todos: effective limits resolver for global defaults, per-child override precedence), `cost-ledger.test.ts` (todos: monthly spend aggregation, messageId dedup, char-formula fallback), `bonus-purchases.test.ts` (todos: weekly bonus cap aggregation, Monday UTC week-start calc, active credit remaining), `weekly-digest.test.ts` (todo: bonus totals appear in digest stats).

    Each file MUST compile cleanly via ts-jest. Do NOT import from `src/lib/*` yet (those files don't exist) — keep stubs self-contained.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm test -- --passWithNoTests</automated>
  </verify>
  <done>`npm test` exits 0, reports all 5 placeholder specs as PASS, and `it.todo` entries are listed. No ts-jest compile errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write and run live MongoDB inspection script</name>
  <files>scripts/mongo-inspect.ts, .planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</files>
  <action>
    Create `scripts/mongo-inspect.ts` — a one-shot Node script (runnable via `npx tsx scripts/mongo-inspect.ts`) that connects to the live Railway MongoDB using `MONGODB_URI` from the admin dashboard's environment and dumps the exact field shapes Plan 02 depends on.

    The script MUST print (and append to `15-00-MONGO-INSPECTION.md`) JSON results for:

    1. `db.balance.findOne()` — confirm whether the field is `tokenCredits` or `credit`, and the key field (`user` vs `userId`). If the collection is empty, log "EMPTY — balance.enabled is likely not set in librechat.yaml" and continue.
    2. `db.aclentries.findOne({ resource: /agent_/ })` — print the full document so we can see `user`/`principalId`, `resource`/`resourceId`, `permBits`, `roleId`. Phase 14 SUMMARY says there are 5 entries per agent (owner + viewers).
    3. `db.messages.findOne({ isCreatedByUser: false, tokenCount: { $exists: true } })` AND `db.messages.findOne({ isCreatedByUser: false })` — confirm whether `tokenCount.input/output` is populated on AI responses or if we need char-formula fallback for cost ledger.
    4. `db.files.find({ context: "image_generation" }).limit(3)` — confirm the `user` field shape and that `context: "image_generation"` is the correct filter.
    5. `db.agents.find({ id: { $in: DRAWING_AGENT_IDS } }).project({ id: 1, tools: 1 }).toArray()` — confirm all 4 Phase 14 agents exist with `dalle` in tools.
    6. `db.conversations.findOne({})` — print the shape so we know whether to use `conversationId` or `_id` for lookups.

    After each query, append a markdown row to `15-00-MONGO-INSPECTION.md` under a table: `| Collection | Field asked | Actual field name | Sample value | Notes |`.

    The script MUST be idempotent and read-only. It MUST NOT insert, update, or delete anything — this is pure inspection.

    After running the script, scaffold `15-00-MONGO-INSPECTION.md` with these sections:
    - `## Field Shape Findings` (populated by the script)
    - `## Plan 02 Implications` (to be filled by the human after the checkpoint below)
    - `## Synthetic Message Test` (populated by Task 3)
  </action>
  <verify>
    <automated>cd /data/home/KidAI && test -f scripts/mongo-inspect.ts && test -f .planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md && grep -q "Field Shape Findings" .planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</automated>
  </verify>
  <done>Script exists, is read-only, has been executed against live Railway MongoDB, and MONGO-INSPECTION.md contains a populated Field Shape Findings table with at least 6 rows.</done>
</task>

<task type="auto">
  <name>Task 3: Synthetic message insertion probe</name>
  <files>scripts/mongo-inspect.ts, .planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</files>
  <action>
    Extend `scripts/mongo-inspect.ts` with a SECOND mode, gated by `--synthetic-message` CLI flag. This mode performs a write-then-read probe that Pattern 8 of the research depends on:

    1. Prompt the operator (via stdin or CLI arg) for a test child's userId and an active conversationId (Manuel will supply one by opening LibreChat as the test child and reading the URL).
    2. Insert a synthetic AI message into `messages` collection matching the shape in Pattern 8:
       ```
       {
         _id: new ObjectId(),
         messageId: <hex of _id>,
         conversationId: <provided>,
         parentMessageId: null,
         isCreatedByUser: false,
         endpoint: "agents",
         agent_id: "agent_wxgt6su7d3pcosiil3",
         text: "PHASE-15-SYNTHETIC-TEST-" + Date.now(),
         model: "claude-haiku-4-5",
         sender: "Agent",
         createdAt: new Date(),
         updatedAt: new Date(),
         __v: 0
       }
       ```
    3. Also update `conversations.updatedAt` for that conversation (per Pitfall 6).
    4. Log the inserted messageId and tell the operator: "Now refresh LibreChat as the test child and confirm the message appears in the chat. Type OK or FAIL then press enter."
    5. Append result to MONGO-INSPECTION.md under `## Synthetic Message Test`: either "VERDICT: GO — synthetic insertion renders in LibreChat UI" or "VERDICT: NO-GO — bonus offer delivery must be redesigned before Plan 02". Include the messageId, timestamp, and any observations.
    6. Clean up: delete the synthetic message after the operator confirms the verdict so it doesn't pollute the child's chat history.

    If VERDICT is NO-GO, Plan 02 Task "bonus offer delivery" must be replaced with an alternative (e.g. the agent's own system instruction guiding it to offer bonuses when given a context flag) — flagged to the planner before Plan 02 starts.
  </action>
  <verify>
    <automated>grep -E "VERDICT: (GO|NO-GO)" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</automated>
  </verify>
  <done>MONGO-INSPECTION.md contains an explicit VERDICT line (GO or NO-GO) in the Synthetic Message Test section, and the synthetic message has been cleaned up from the live DB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review of MongoDB inspection + synthetic message verdict</name>
  <files>.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</files>
  <action>
    HUMAN CHECKPOINT — no code. Pause execution and ask the user to:

    1. Open `.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md`
    2. Verify the "Field Shape Findings" table has real values for: balance (tokenCredits field name), aclentries (user field name), messages.tokenCount presence, files.user field, all 4 drawing agents present
    3. Fill in the "Plan 02 Implications" section — at minimum:
       - "balance.enabled is [SET / NOT SET] in librechat.yaml — Plan 01 MUST [add / skip] YAML deploy task"
       - "aclentries use field name [user / principalId] — Plan 02 enforcement code MUST use this"
       - "messages.tokenCount [IS / IS NOT] populated — Plan 01 cost ledger MUST [use directly / apply char-formula fallback]"
    4. Confirm the Synthetic Message Test VERDICT is either GO or NO-GO
    5. If VERDICT is NO-GO, STOP and notify the planner — Plan 02's bonus offer delivery task needs a redesign before execution

    Resume signal: Type "approved — GO" (or "approved — NO-GO, replan bonus delivery") to proceed to Wave 1.
  </action>
  <verify>
    <automated>grep -q "Plan 02 Implications" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md && grep -qE "VERDICT: (GO|NO-GO)" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md</automated>
  </verify>
  <done>MONGO-INSPECTION.md has a populated "Plan 02 Implications" section and an explicit VERDICT (GO or NO-GO). User has approved via resume signal.</done>
</task>

</tasks>

<verification>
1. `npm test` exits 0 with at least 5 passing placeholder specs and a list of `it.todo` entries
2. `scripts/mongo-inspect.ts` exists and has been run (inspection doc populated)
3. MONGO-INSPECTION.md contains Field Shape Findings table AND Synthetic Message Test VERDICT
4. Human has reviewed and signed off, populating Plan 02 Implications section
</verification>

<success_criteria>
- Jest runs and reports green (`npm test` exits 0)
- Every Plan 01 and Plan 02 lib file has a corresponding test stub ready to be filled in
- Actual DB field names are documented so Plan 02 enforcement code references reality, not assumed names
- Synthetic message delivery is GO or the NO-GO has been flagged before Plan 02 starts
</success_criteria>

<output>
After completion, create `.planning/phases/15-safety-alert-extension-rate-limiting/15-00-SUMMARY.md` documenting:
- Jest version installed + config choices
- Exact field names found in live MongoDB (one table per collection)
- Synthetic message verdict and its impact on Plan 02
- Any decisions the human made in the checkpoint that affect downstream plans
</output>
