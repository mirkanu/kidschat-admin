---
phase: 15-safety-alert-extension-rate-limiting
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/lib/enforcement.ts
  - src/lib/bonus-delivery.ts
  - tests/lib/enforcement.test.ts
  - tests/lib/bonus-delivery.test.ts
  - src/app/api/cron/cost-ledger-sweep/route.ts
  - src/app/api/cron/limit-enforcement/route.ts
  - src/app/api/cron/bonus-detection/route.ts
  - src/app/api/cron/daily-reset/route.ts
  - src/app/api/cron/monthly-reset/route.ts
  - src/app/api/notify/weekly-digest/route.ts
  - src/lib/weekly-digest.ts
  - tests/api/weekly-digest.test.ts
  - src/middleware.ts
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/settings/loading.tsx
  - src/app/(dashboard)/settings/settings-form.tsx
  - src/app/(dashboard)/settings/actions.ts
  - src/app/(dashboard)/users/[userId]/page.tsx
  - src/app/(dashboard)/users/[userId]/loading.tsx
  - src/app/(dashboard)/alerts/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
autonomous: false
requirements:
  - IMG-ENFORCE-01
  - IMG-ENFORCE-02
  - IMG-ENFORCE-03
  - IMG-BONUS-01
  - IMG-BONUS-02
  - IMG-ADMIN-01
must_haves:
  truths:
    - "Cost-ledger-sweep cron reads new AI-response messages, calculates cost, inserts to cost_ledger, updates lastPoll — idempotent across runs"
    - "Limit-enforcement cron for an over-limit child (no bonus) removes their ACL entries for 4 drawing agents and inserts a bonus offer message into messages collection"
    - "Bonus-detection cron finds a child's 'YES' message within 5 min of offer, credits bonus_purchases, restores access"
    - "Hard-lock (monthly cap AND weekly bonus cap both hit) writes tokenCredits: 0 to balance collection and LibreChat blocks the next request"
    - "Daily-reset cron at midnight UTC restores aclentries for children whose lockReason is daily_image_cap"
    - "Monthly-reset cron on 1st UTC restores balance and clears monthly-cap locks"
    - "Weekly digest email contains a 'Bonus purchased this week' line per child"
    - "Admin settings page (/settings) edits global_defaults and per-child overrides; changes persist to MongoDB and next enforcement run uses them"
    - "User detail page (/users/[userId]) shows daily image count / daily message count / monthly spend EUR / active bonus credit / bonus purchases this week"
    - "Alerts page filter includes image_prompt type"
    - "Analytics page shows image count per child (30-day trend)"
    - "/settings has loading.tsx skeleton; /users/[userId] has loading.tsx skeleton; data-fetching regions wrapped in Suspense"
  artifacts:
    - path: "src/lib/enforcement.ts"
      provides: "lockImageAccess / unlockImageAccess / hardLockAllAccess / unlockAllAccess + enforceChildLimits orchestrator"
      exports: ["lockImageAccess", "unlockImageAccess", "hardLockAllAccess", "unlockAllAccess", "enforceChildLimits"]
    - path: "src/lib/bonus-delivery.ts"
      provides: "sendBonusOfferMessage + detectBonusConfirmation + applyBonusCredit"
      exports: ["sendBonusOfferMessage", "detectBonusConfirmation", "applyBonusCredit"]
    - path: "src/app/api/cron/cost-ledger-sweep/route.ts"
      provides: "POST handler — polling sweep every 2 min"
    - path: "src/app/api/cron/limit-enforcement/route.ts"
      provides: "POST handler — enforcement sweep every 2 min"
    - path: "src/app/api/cron/bonus-detection/route.ts"
      provides: "POST handler — YES detection every 1 min"
    - path: "src/app/api/cron/daily-reset/route.ts"
      provides: "POST handler — midnight UTC daily reset"
    - path: "src/app/api/cron/monthly-reset/route.ts"
      provides: "POST handler — 1st UTC monthly reset"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Admin settings editor"
    - path: "src/app/(dashboard)/users/[userId]/page.tsx"
      provides: "Per-child usage + bonus + override editor"
  key_links:
    - from: "src/app/api/cron/limit-enforcement/route.ts"
      to: "src/lib/enforcement.ts enforceChildLimits"
      via: "direct import + per-child loop"
      pattern: "enforceChildLimits"
    - from: "src/lib/enforcement.ts lockImageAccess"
      to: "MongoDB aclentries collection delete + locked_acl_entries insert"
      via: "getMongoClient()"
      pattern: "collection\\(['\"]aclentries['\"]\\)"
    - from: "src/lib/enforcement.ts hardLockAllAccess"
      to: "MongoDB balance collection tokenCredits: 0"
      via: "getMongoClient() updateOne"
      pattern: "tokenCredits:\\s*0"
    - from: "src/lib/bonus-delivery.ts sendBonusOfferMessage"
      to: "MongoDB messages collection insertOne + conversations updatedAt"
      via: "getMongoClient() insertOne + updateOne"
      pattern: "collection\\(['\"]messages['\"]\\)"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/app/(dashboard)/settings/actions.ts saveSettings"
      via: "server action on form submit"
      pattern: "use server"
    - from: "src/middleware.ts"
      to: "/api/cron/* bypass"
      via: "matcher exclusion (same pattern as /api/notify/*)"
      pattern: "/api/cron"
---

<objective>
Wave 2 — wire Plan 01's foundation libs into running enforcement, bonus purchase flow, and admin UI. Adds 5 Railway cron endpoints, an enforcement orchestrator, bonus delivery/detection logic, admin settings editor, user detail enhancements, alerts page filter, analytics page image trend, and a weekly digest extension. Requires Plan 01 to have deployed `balance.enabled: true` to LibreChat and for Wave 0 to have confirmed that synthetic message insertion renders in the child's UI (VERDICT: GO in 15-00-MONGO-INSPECTION.md).

Purpose: Make the phase goal observable — parents see image abuse alerts, runaway costs are blocked, kids can buy bonus credits via YES.
Output: 5 cron endpoints + 2 new lib files + 1 new admin settings page + enhanced user detail/alerts/analytics pages + weekly digest extension. One manual smoke-test checkpoint verifies the end-to-end flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-RESEARCH.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-VALIDATION.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-00-SUMMARY.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-01-SUMMARY.md
@src/lib/settings.ts
@src/lib/cost-ledger.ts
@src/lib/bonus-purchases.ts
@src/lib/safety-patterns.ts
@src/lib/notify-safety-alert.ts
@src/lib/mongodb.ts
@src/lib/weekly-digest.ts
@src/app/api/notify/weekly-digest/route.ts
@src/middleware.ts
@src/app/(dashboard)/users/[userId]/page.tsx
@src/app/(dashboard)/alerts/page.tsx
@src/app/(dashboard)/analytics/page.tsx
@./CLAUDE.md

<interfaces>
<!-- Contracts this plan delivers. Imports from Plan 01 are available. -->

```typescript
// src/lib/enforcement.ts — NEW
export const DRAWING_AGENT_IDS = [
  "agent_wxgt6su7d3pcosiil3",
  "agent_y4w1cvoyg77p9thed9",
  "agent_64q6z5s57552cpgl0hr",
  "agent_aiv99mzvdzquym6y89k",
];
export type LockReason = "daily_image_cap" | "daily_message_cap" | "monthly_cost_cap";
export async function lockImageAccess(userId: string, db: Db, reason?: LockReason): Promise<void>;
export async function unlockImageAccess(userId: string, db: Db, reason?: LockReason): Promise<void>;
export async function hardLockAllAccess(userId: string, db: Db): Promise<void>;
export async function unlockAllAccess(userId: string, db: Db, credits: number): Promise<void>;
export async function enforceChildLimits(userId: string, childName: string, db: Db): Promise<EnforcementResult>;

// src/lib/bonus-delivery.ts — NEW
export async function sendBonusOfferMessage(args: {
  userId: string; conversationId: string; agentId: string; template: string; db: Db;
}): Promise<string /* messageId */>;
export async function detectBonusConfirmation(userId: string, pending: PendingState, db: Db): Promise<boolean>;
export async function applyBonusCredit(userId: string, pending: PendingState, db: Db): Promise<void>;
```

Cron endpoint auth: `x-cron-secret` header matching `process.env.CRON_SECRET` (same pattern as Phase 13 /api/notify/*).
Cron schedules (configured in Railway):
- cost-ledger-sweep: every 2 minutes
- limit-enforcement: every 2 minutes (staggered +30s after ledger sweep)
- bonus-detection: every 1 minute
- daily-reset: 0 0 * * * UTC (midnight UTC)
- monthly-reset: 0 0 1 * * UTC (1st of month UTC)

Shadcn components to use: Card, Input, Label, Button, Table, Skeleton, Tabs, Separator, Dialog. Install missing ones via `npx shadcn@latest add <name>` — do NOT hand-roll.

Perceived performance requirements (MANDATORY from CLAUDE.md):
- Every new route MUST have loading.tsx returning a Skeleton matching the content shape
- Wrap data-fetching sections in <Suspense fallback={<Skeleton />}>
- Form submit buttons use useTransition and show pending state
- Prefetch enabled by default on next/link (don't disable)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build enforcement.ts + bonus-delivery.ts libs with unit tests (IMG-ENFORCE-01, IMG-ENFORCE-02, IMG-BONUS-02)</name>
  <files>src/lib/enforcement.ts, src/lib/bonus-delivery.ts, tests/lib/enforcement.test.ts, tests/lib/bonus-delivery.test.ts</files>
  <behavior>
    enforcement.test.ts:
    - lockImageAccess snapshots existing aclentries to locked_acl_entries then deletes them for DRAWING_AGENT_IDS only (other agent ACLs untouched)
    - unlockImageAccess re-inserts from locked_acl_entries with ordered:false; duplicate key errors (11000) are swallowed
    - hardLockAllAccess writes tokenCredits: 0 to balance collection AND calls lockImageAccess (belt-and-suspenders)
    - unlockAllAccess writes positive tokenCredits and restores ACL
    - enforceChildLimits with (imageCount >= limit, no bonus, weekly bonus not exhausted) → calls lockImageAccess + sendBonusOfferMessage
    - enforceChildLimits with (monthlyCostHit, no bonus, weekly bonus exhausted) → calls hardLockAllAccess
    - enforceChildLimits with (imageCount >= limit, active bonus credit > 0) → NO lock, NO offer
    - enforceChildLimits when already awaiting confirmation → NO new offer (idempotent)

    bonus-delivery.test.ts:
    - sendBonusOfferMessage inserts a doc into messages with isCreatedByUser:false, endpoint:"agents", correct agent_id, template text; also updates conversations.updatedAt
    - detectBonusConfirmation returns true for a user message with text "yes" / "YES" / "yes." created after confirmationOfferedAt within 5-min window
    - detectBonusConfirmation returns false for "no", "yes please later", or messages before confirmationOfferedAt
    - detectBonusConfirmation returns false if 5 minutes have elapsed (expiry)
    - applyBonusCredit for lockType "image_cap" → inserts bonus_purchases, unlocks ACL, clears pending state
    - applyBonusCredit for lockType "monthly_cap" → inserts bonus_purchases, adds tokenCredits via $inc, clears pending state
    - applyBonusCredit respects weekly bonus cap: throws or returns early if getWeeklyBonusSpend + packSize > weeklyBonusCap
  </behavior>
  <action>
    1. Create `src/lib/enforcement.ts`:
       - Export `DRAWING_AGENT_IDS` constant (the 4 IDs from Phase 14)
       - Implement `lockImageAccess`, `unlockImageAccess`, `hardLockAllAccess`, `unlockAllAccess` following RESEARCH.md Pattern 4 exactly — but use the EXACT field names from 15-00-MONGO-INSPECTION.md (not the research assumptions)
       - Implement `enforceChildLimits` following the high-level flow at the end of RESEARCH.md: parallel-fetch imageCount/messageCount/monthlySpend/weeklyBonus/activeBonusCredit via Plan 01 libs, compute shouldHardLock/shouldImageLock, and call the appropriate action
       - `awaitingBonusConfirmation` state tracked in `settings` collection under `override_{userId}` document (field: `awaitingBonusConfirmation: true`, `confirmationOfferedAt: Date`, `lockType: "image_cap"|"monthly_cap"`, `activeConversationId: string`) — per RESEARCH.md Pattern 6
    2. Create `src/lib/bonus-delivery.ts`:
       - `sendBonusOfferMessage` — implement exactly as RESEARCH.md Pattern 8, INCLUDING the `conversations.updatedAt` write (per Pitfall 6)
       - `detectBonusConfirmation` — query messages for "yes" / "YES" / "yes." (regex: `/^\s*yes\.?\s*$/i`) since confirmationOfferedAt, within 5-min window. Find the child's "active" conversationId by looking up the most recent conversation where the child is the user (or store it on the pending state when offering)
       - `applyBonusCredit` — implement RESEARCH.md Pattern 7; check weekly bonus cap before inserting; call unlockImageAccess or balance $inc depending on lockType
    3. Write both test files using the same in-memory Db mock pattern from Plan 01 Task 2. Key mock behaviors needed:
       - `aclentries.find({user, resource: $in}).toArray()` returns controllable array
       - `aclentries.deleteMany(query)` records the deletion
       - `locked_acl_entries.insertMany(docs)` records inserts
       - `balance.updateOne(query, update, {upsert: true})` records the update
       - `messages.insertOne(doc)` returns `{insertedId: new ObjectId()}`
       - `messages.findOne(query)` for bonus detection
       - `conversations.updateOne(query, update)` for the updatedAt touch
    4. All tests MUST run offline. NO real MongoDB connection.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm test -- --testPathPattern="enforcement|bonus-delivery"</automated>
  </verify>
  <done>Both test files green. enforcement.ts and bonus-delivery.ts exist with the exact exports in <interfaces>. Field names match 15-00-MONGO-INSPECTION.md. NO real DB access in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build 5 cron endpoints + middleware bypass + weekly digest bonus extension + Railway cron config (IMG-ENFORCE-03, IMG-BONUS-01, IMG-BONUS-02)</name>
  <files>src/app/api/cron/cost-ledger-sweep/route.ts, src/app/api/cron/limit-enforcement/route.ts, src/app/api/cron/bonus-detection/route.ts, src/app/api/cron/daily-reset/route.ts, src/app/api/cron/monthly-reset/route.ts, src/app/api/notify/weekly-digest/route.ts, src/lib/weekly-digest.ts, tests/api/weekly-digest.test.ts, src/middleware.ts</files>
  <action>
    1. Create `src/app/api/cron/cost-ledger-sweep/route.ts`:
       - POST handler with x-cron-secret auth (reject with 401 on mismatch, same pattern as Phase 13)
       - Read `cron_state` doc `_id: "cost_ledger_sweep"` for `lastPoll`, default 5 min ago
       - Fetch new AI-response messages (isCreatedByUser:false) since lastPoll, filter non-ADMIN via user join
       - For each, dedup on messageId, calculate cost via Plan 01 `calculateMessageCostEUR`, insert to cost_ledger
       - Update cron_state lastPoll
       - Return `{ processed: number, inserted: number }`

    2. Create `src/app/api/cron/limit-enforcement/route.ts`:
       - Iterate all non-ADMIN users from `users` collection
       - For each, call `enforceChildLimits(userId, childName, db)` from enforcement.ts
       - Collect results and return `{ enforced: ActionSummary[] }`

    3. Create `src/app/api/cron/bonus-detection/route.ts`:
       - Read settings collection for docs where `awaitingBonusConfirmation: true`
       - For each pending, call `detectBonusConfirmation` — on match, `applyBonusCredit`
       - Return `{ confirmed: number, expired: number }`

    4. Create `src/app/api/cron/daily-reset/route.ts`:
       - Find all `locked_acl_entries` with `lockReason: "daily_image_cap"` or `"daily_message_cap"`
       - For each unique userId, call `unlockImageAccess(userId, db, reason)`
       - Expire bonus_purchases where `expiresAt < now` (set `creditRemainingEUR: 0`)
       - Return `{ unlocked: number }`

    5. Create `src/app/api/cron/monthly-reset/route.ts`:
       - Find all children with `balance.tokenCredits: 0` (hard-locked)
       - Restore via `unlockAllAccess(userId, db, startBalanceCredits)` where startBalanceCredits = monthlyCostCapEUR / EUR_RATE * 1_000_000
       - Clear any `locked_acl_entries` with `lockReason: "monthly_cost_cap"`
       - Return `{ restored: number }`

    6. Extend `src/lib/weekly-digest.ts`:
       - Add `bonusPurchasesThisWeek: number` and `totalBonusSpendEUR: number` to `WeeklyChildStats`
       - `getWeeklyChildStats` — run a secondary aggregation on `bonus_purchases` since start-of-week, merge into the existing per-child row by userId
       - Keep `formatDigestStats` pure (see STATE.md Phase 13 decision) and extend the format to include "Bonuses: €X (N purchases)" line when > 0

    7. Extend `src/app/api/notify/weekly-digest/route.ts`:
       - No auth or orchestration changes — just consume the new fields
       - Email template gets a new "Bonus Purchases This Week" section (only rendered when bonusPurchasesThisWeek > 0)

    8. Extend `tests/api/weekly-digest.test.ts`:
       - Fill in the todo: digest output contains bonus line when child has purchases this week
       - digest output omits bonus line when child has no purchases
       - totalBonusSpendEUR aggregates correctly across multiple purchases

    9. Update `src/middleware.ts`:
       - Add `/api/cron/` to the auth bypass list (same pattern as `/api/notify/*` from Phase 13)

    10. Document Railway cron configuration in the plan SUMMARY (the human will need to add these via Railway CLI in the final smoke test task):
        ```
        cost-ledger-sweep:   */2 * * * *   → POST /api/cron/cost-ledger-sweep
        limit-enforcement:   1-59/2 * * * * → POST /api/cron/limit-enforcement  (staggered +1m)
        bonus-detection:     * * * * *      → POST /api/cron/bonus-detection
        daily-reset:         0 0 * * *      → POST /api/cron/daily-reset
        monthly-reset:       0 0 1 * *      → POST /api/cron/monthly-reset
        ```
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm test -- --testPathPattern=weekly-digest && test -f src/app/api/cron/cost-ledger-sweep/route.ts && test -f src/app/api/cron/limit-enforcement/route.ts && test -f src/app/api/cron/bonus-detection/route.ts && test -f src/app/api/cron/daily-reset/route.ts && test -f src/app/api/cron/monthly-reset/route.ts && grep -q "/api/cron" src/middleware.ts</automated>
  </verify>
  <done>5 cron endpoints exist with x-cron-secret auth, middleware bypasses /api/cron/*, weekly digest tests green with bonus section, cron schedules documented for Railway setup.</done>
</task>

<task type="auto">
  <name>Task 3: Admin settings page + user detail enhancements + alerts/analytics updates (IMG-ADMIN-01)</name>
  <files>src/app/(dashboard)/settings/page.tsx, src/app/(dashboard)/settings/loading.tsx, src/app/(dashboard)/settings/settings-form.tsx, src/app/(dashboard)/settings/actions.ts, src/app/(dashboard)/users/[userId]/page.tsx, src/app/(dashboard)/users/[userId]/loading.tsx, src/app/(dashboard)/alerts/page.tsx, src/app/(dashboard)/analytics/page.tsx</files>
  <action>
    Build the admin UI. MUST follow CLAUDE.md perceived performance rules (every route has loading.tsx skeleton; data fetches wrapped in Suspense; buttons use useTransition).

    1. `src/app/(dashboard)/settings/page.tsx`:
       - Server component that fetches `global_defaults` doc + all `override_*` docs + all non-ADMIN users from settings/users collections
       - Wraps the form in `<Suspense fallback={<SettingsFormSkeleton />}>`
       - Renders a `<Tabs>` with two tabs: "Global Defaults" and "Per-Child Overrides"
       - Global Defaults tab: form fields for dailyImageLimit, dailyMessageLimit, monthlyCostCapEUR, weeklyBonusCap, bonusPackSize, bonusMessageTemplate (textarea). Save button.
       - Per-Child Overrides tab: `<Table>` listing each child with inline editable fields (or an expand-to-edit row) — override values fall back to the global default display when null

    2. `src/app/(dashboard)/settings/loading.tsx`:
       - Skeleton matching the settings form shape (shadcn `Skeleton` blocks for each field, Tabs header placeholder)

    3. `src/app/(dashboard)/settings/settings-form.tsx`:
       - Client component using `use client`
       - `useTransition` for the save button so clicks show instant pending state
       - Invokes the server action from actions.ts
       - Shows `sonner` toast on success/error (reuse existing project pattern)

    4. `src/app/(dashboard)/settings/actions.ts`:
       - Server action `saveGlobalDefaults(formData)` — validates, writes to `settings` collection `_id: "global_defaults"`, calls `revalidatePath("/settings")`
       - Server action `saveChildOverride(userId, formData)` — validates, upserts `override_{userId}` doc
       - Server action `deleteChildOverride(userId)` — deletes the override doc (revert to global default)

    5. `src/app/(dashboard)/users/[userId]/page.tsx`:
       - EXTEND existing page (don't replace). Add a "Usage & Limits" section showing:
         - Image count today / limit (e.g. "4 / 10")
         - Message count today / limit
         - Monthly spend EUR / cap (progress bar via shadcn `Progress`)
         - Active bonus credit remaining
         - Bonus purchases this week count
       - Data fetched via Plan 01 libs (getImageCountToday, getDailyMessageCount, getMonthlySpendEUR, getActiveBonusCredit, getWeeklyBonusSpend)
       - Wrap the new section in its own `<Suspense fallback={<UsageSkeleton />}>` so it streams in independently of the existing page content

    6. `src/app/(dashboard)/users/[userId]/loading.tsx`:
       - Create if missing; skeleton matching both existing content + new usage section

    7. `src/app/(dashboard)/alerts/page.tsx`:
       - EXTEND: alerts query (safety-patterns scan) already returns `type` field. Add `"image_prompt"` to the filter dropdown so parents can filter specifically on image abuse alerts.
       - If the existing page has a "type" badge component, add a distinct visual treatment for image_prompt (e.g. orange badge labeled "Image")

    8. `src/app/(dashboard)/analytics/page.tsx`:
       - EXTEND: Phase 10's cost tracking already renders a 30-day cost trend via recharts. Add a second line (or a new small card) for "Images generated per day" using `files` collection aggregation with `context: "image_generation"` grouped by day over the last 30 days.
       - Wrap the new chart in a Suspense boundary with a `<Skeleton className="h-64 w-full" />` fallback

    9. Install any missing shadcn components first:
       ```
       npx shadcn@latest add tabs progress skeleton
       ```
       (Check which are already present in components/ui first — only add missing ones.)

    DO NOT add search functionality to settings page (deferred).
    DO NOT add refund/reversal UI (deferred).
    DO NOT add tiered bonus packs (deferred — single admin-configured amount only).
    DO NOT add Slack/SMS alert options (deferred).
    DO NOT add a child-facing usage widget (deferred).
    DO NOT add per-session bonus caps (deferred — weekly cap only).
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm run build 2>&1 | tail -40 && test -f src/app/\(dashboard\)/settings/page.tsx && test -f src/app/\(dashboard\)/settings/loading.tsx</automated>
  </verify>
  <done>Next.js production build succeeds, all new routes exist with loading.tsx, settings page edits persist to MongoDB via server actions, user detail page shows usage section, alerts page filters image_prompt, analytics page shows image trend.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy to Railway + configure cron schedules + env vars</name>
  <files>scripts/deploy-librechat-yaml.ts, .planning/phases/15-safety-alert-extension-rate-limiting/15-02-DEPLOYMENT.md</files>
  <action>
    Deploy the admin dashboard to Railway and configure the 5 new cron schedules. Use Railway CLI directly — do NOT ask the user to click through the dashboard (see memory: Automate Railway). After fixes, always redeploy (see memory: Always Redeploy).

    1. Ensure `CRON_SECRET` env var is set on the admin dashboard Railway service; if missing, generate one via `openssl rand -hex 32` and set via `railway variables --set CRON_SECRET=<value>`.
    2. Ensure `USD_TO_EUR_RATE` is set (default 0.92 is hardcoded in cost-ledger.ts but allow override).
    3. Push the current branch to the remote Git so Railway picks up the build, OR run `railway up` targeting the admin dashboard service (whichever is the established pattern — check Phase 13 SUMMARY).
    4. After deploy, register the 5 cron schedules:
       - Use `railway cron` CLI or the Railway GraphQL API to create 5 cron triggers, each POSTing to the corresponding endpoint with the `x-cron-secret` header
       - If Railway's cron add-on requires dashboard config, USE THE GRAPHQL API via `gh api` or `curl` with `$RAILWAY_TOKEN` — still no manual dashboard clicks
    5. Document the deployment in `.planning/phases/15-safety-alert-extension-rate-limiting/15-02-DEPLOYMENT.md`:
       - Deploy commit SHA
       - Cron IDs and their endpoints/schedules
       - CRON_SECRET location (Railway service vars — don't print the value)
       - First successful run timestamps from Railway logs for each cron
    6. Smoke-probe each endpoint manually with curl to confirm auth works:
       ```
       curl -X POST https://<admin>.up.railway.app/api/cron/cost-ledger-sweep -H "x-cron-secret: $CRON_SECRET" | jq
       ```
       Expect 200 + `{ processed: N }` payload. Expect 401 when omitting the header.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-02-DEPLOYMENT.md && grep -E "cron" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-02-DEPLOYMENT.md</automated>
  </verify>
  <done>Admin dashboard redeployed to Railway, 5 cron schedules registered via CLI/GraphQL, DEPLOYMENT.md documents cron IDs + first run timestamps, manual curl smoke tests confirm 200 + payload and 401 on missing secret.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: End-to-end smoke test — image abuse alert, image limit enforcement, bonus purchase YES flow, hard-lock, weekly digest</name>
  <files>.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SMOKE-TEST.md</files>
  <action>
    HUMAN CHECKPOINT — no code changes. Pause execution and walk the user through the end-to-end smoke test below. Record results in `.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SMOKE-TEST.md` as a pass/fail table per step.

    What was built (context):
    - 5 Railway crons live and running on schedule
    - Admin settings page live at /settings
    - User detail page shows usage + bonus data
    - Alerts page filters image_prompt
    - Analytics page shows image trend
    - LibreChat balance.enabled: true (from Plan 01)
    - Weekly digest includes bonus totals

    Execute this sequence manually with a test child account:

    1. **Image abuse alert path (IMG-SAFETY-01..04):**
       a. As the test child, open LibreChat and send: "draw me someone without any clothes on"
       b. Wait ~60s for the next safety-alert scan (or trigger manually)
       c. Check /alerts page — a new `image_prompt` alert should appear with the matched pattern label
       d. Check parent inbox — a "Image Alert: Inappropriate image request detected" email should arrive (if parent has notification_prefs.safetyAlerts !== false)

    2. **Daily image limit + bonus offer (IMG-ENFORCE-02, IMG-BONUS-02):**
       a. Via /settings, set the test child's override dailyImageLimit = 2
       b. As the test child, generate 2 images (clarify + draw, clarify + draw)
       c. Attempt a 3rd image — within 2 minutes of limit-enforcement cron, the agent should insert a message: "You've reached your limit... Type YES to confirm."
       d. As the child, type "YES"
       e. Within 60 seconds, bonus-detection cron should credit the bonus and restore image access
       f. Confirm a new record in the `bonus_purchases` collection, and that the 4th image generates successfully

    3. **Monthly cost hard-lock + weekly bonus cap exhaustion (IMG-ENFORCE-01):**
       a. Via /settings, set monthlyCostCapEUR = 0.01 and weeklyBonusCap = 0 for the test child
       b. Send 1 message — within 2 minutes, hardLockAllAccess should fire
       c. Attempt to chat — LibreChat should return an error (balance exhausted)
       d. Check `balance` collection — tokenCredits should be 0 for the child
       e. Revert the override to normal values via /settings

    4. **Daily reset (IMG-ENFORCE-03):**
       a. With a child currently locked via dailyImageLimit, manually trigger /api/cron/daily-reset via curl
       b. Verify ACL entries are restored; child can generate images again

    5. **Weekly digest bonus section (IMG-BONUS-01):**
       a. Trigger /api/notify/weekly-digest manually via curl
       b. Check parent inbox — the digest should include a "Bonus Purchases This Week" section listing the test child's purchase from step 2

    6. **Admin settings page perceived performance (CLAUDE.md):**
       a. Visit /settings — confirm the loading skeleton appears instantly, no white flash
       b. Click Save on Global Defaults — confirm the button shows pending state immediately (no "dead click" gap)
       c. Visit /users/[testChildUserId] — confirm the usage section streams in independently of the rest of the page (both have skeletons first)

    Cleanup: Delete test bonus_purchases records, reset test child's override, restore any modified settings.

    Resume signal: Type "approved" if all 6 verification steps pass, or describe failures (which step, what happened vs expected).
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SMOKE-TEST.md && grep -qiE "pass|approved|fail" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SMOKE-TEST.md</automated>
  </verify>
  <done>All 6 smoke-test steps executed, results recorded in 15-02-SMOKE-TEST.md, and the user has typed "approved" (or failures have been documented and fixed before re-running).</done>
</task>

</tasks>

<verification>
- Unit tests green for enforcement.ts, bonus-delivery.ts, weekly-digest
- Next.js production build succeeds
- 5 Railway cron endpoints deployed and registered
- Manual curl smoke tests pass (200 with valid secret, 401 without)
- Human checkpoint: all 6 end-to-end flows verified
</verification>

<success_criteria>
- IMG-ENFORCE-01: Monthly cap hard-locks via balance.tokenCredits: 0 (verified in smoke test step 3)
- IMG-ENFORCE-02: Daily image limit hides DALL-E via ACL removal (verified in smoke test step 2)
- IMG-ENFORCE-03: Daily/monthly reset crons restore access (verified in smoke test step 4 + unit tests)
- IMG-BONUS-01: Weekly digest includes bonus totals (verified in smoke test step 5 + unit test)
- IMG-BONUS-02: YES in chat credits bonus and restores access (verified in smoke test step 2 + unit test)
- IMG-ADMIN-01: Admin settings page edits persist and take effect (verified in smoke test step 2 + step 6)
- All dashboard routes comply with CLAUDE.md perceived performance rules (loading.tsx, Suspense, useTransition)
</success_criteria>

<output>
Create `.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SUMMARY.md` documenting:
- Final enforcement architecture (ACL + balance tiers as implemented)
- Cron schedule table with IDs
- Any field-name adjustments from 15-00-MONGO-INSPECTION.md that changed the research plan
- Deferred follow-ups (e.g. refund UI, tiered packs) captured for future phases
- Performance notes: actual cron runtime observed on Railway logs
</output>
