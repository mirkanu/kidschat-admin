---
phase: 15-safety-alert-extension-rate-limiting
plan: "03"
type: execute
wave: 1
depends_on: [15-02]
files_modified:
  - scripts/change-stream-probe.ts
  - scripts/verify-instrumentation.ts
  - .planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md
autonomous: false
gap_closure: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Operator has empirically confirmed whether Railway-hosted MongoDB supports change streams by opening a watch cursor on the messages collection and observing at least one insert event"
    - "Operator has empirically confirmed whether Next.js 15 instrumentation.ts runs register() under the current Railway `next start` setup (evidence: log line printed on boot)"
    - "Operator has visually confirmed whether LibreChat's child sidebar renders balances.tokenCredits natively (screenshot or described behavior)"
    - "15-03-DECISIONS.md locks in: (a) change-stream vs 1-cron fallback, (b) native token display vs deferred € display, (c) instrumentation.ts vs /api/cron/tick"
  artifacts:
    - path: "scripts/change-stream-probe.ts"
      provides: "Standalone read-only probe that opens a change stream on messages collection, logs the first N events with resumeToken, and exits"
    - path: "scripts/verify-instrumentation.ts"
      provides: "One-file Next.js instrumentation smoke test — confirms register() fires on `next start` in local dev"
    - path: ".planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md"
      provides: "Locked architectural decisions that Plan 15-04 depends on"
  key_links:
    - from: "scripts/change-stream-probe.ts"
      to: "Railway MongoDB via MONGODB_URI"
      via: "MongoClient.watch() on messages collection"
      pattern: "\\.watch\\("
    - from: "15-03-DECISIONS.md"
      to: "Plan 15-04 instrumentation approach"
      via: "Explicit decision line: `change_stream: yes|no`"
      pattern: "change_stream:"
---

<objective>
Risk-kill the three assumptions that Plan 15-04's architecture depends on BEFORE writing any instrumentation code. Phase 15.1's goal is to replace the 5-cron polling pipeline with a change-stream listener inside Next.js instrumentation.ts — but if Railway's managed MongoDB doesn't support change streams, or if instrumentation.ts doesn't work in our build setup, or if LibreChat's native balance sidebar doesn't actually render tokenCredits, Plan 15-04 needs a different architecture.

Purpose: Empirical answers locked into `15-03-DECISIONS.md` before Plan 15-04 commits to an approach. No code production; pure verification.
Output: 2 probe scripts, 1 decision document, human sign-off on path forward.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15.1-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SUMMARY.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-00-MONGO-INSPECTION.md
@src/lib/mongodb.ts
@package.json

<interfaces>
<!-- What we already know from prior plans -->

MongoDB client pattern (src/lib/mongodb.ts):
```typescript
import { getMongoClient } from "@/lib/mongodb";
const db = (await getMongoClient()).db("test");
```

From 15-00 inspection — confirmed collections and fields:
- `messages` collection: `{ _id, messageId, conversationId, isCreatedByUser, text, user, createdAt, ... }`
- `balances` collection (plural): `{ user, tokenCredits }` — was empty in 15-00 pre-balance-enable, should now be populated after 15-01 enabled balance.enabled: true
- Drawing agent IDs: `agent_wxgt6su7d3pcosiil3`, `agent_y4w1cvoyg77p9thed9`, `agent_64q6z5s57552cpgl0hr`, `agent_aiv99mzvdzquym6y89k`

Test child account: Sebastian (userId known to operator)
LibreChat URL: https://librechat-production-bff2.up.railway.app
Admin Dashboard: https://kidschat-admin-production.up.railway.app

Change stream basics (driver: mongodb npm package, already in project):
```typescript
const changeStream = db.collection("messages").watch(
  [{ $match: { operationType: "insert", "fullDocument.isCreatedByUser": true } }],
  { fullDocument: "updateLookup" }
);
for await (const event of changeStream) {
  console.log(event.operationType, event.fullDocument?.text);
}
```

Next.js 15 instrumentation hook shape:
```typescript
// src/instrumentation.ts
export async function register() {
  if (process.env.NEXT_RUNTIME === "nodejs") {
    console.log("[instrumentation] register() fired on", new Date().toISOString());
    // Start change stream listener here
  }
}
```
`next.config.js` needs `experimental.instrumentationHook: true` in Next 14; in Next 15 it's enabled by default — confirm by reading the project's next.config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run the change-stream probe against Railway MongoDB</name>
  <files>scripts/change-stream-probe.ts</files>
  <action>
    Create `scripts/change-stream-probe.ts` — a one-shot read-only Node script (runnable via `npx tsx scripts/change-stream-probe.ts`) that proves whether Railway-hosted MongoDB supports change streams.

    Requirements:
    1. Read `MONGODB_URI` from env (use `dotenv/config` OR `railway run npx tsx scripts/change-stream-probe.ts` to inject). Do NOT hardcode a URI.
    2. Connect to the `test` database.
    3. Open a change stream on `messages` collection filtered to `{ operationType: "insert", "fullDocument.isCreatedByUser": true }` with `{ fullDocument: "updateLookup" }`.
    4. Log a startup line: `[probe] listening on messages collection — send a message as Sebastian in LibreChat now`.
    5. For the first 3 events received (or until 5 minutes elapse), log: `[event N] operationType=<op> messageId=<id> text="<text truncated to 80 chars>" resumeToken=<token>`.
    6. On error, log the full error object and exit with code 1.
    7. If no events arrive within 5 minutes, log `[probe] TIMEOUT — no events received, change streams may not be supported or replica set not configured` and exit with code 2.
    8. On success (3 events received), log `[probe] SUCCESS — change streams supported` and exit with code 0.
    9. Write the result (SUCCESS / TIMEOUT / ERROR + resumeToken sample) to `.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md` (append to a `## Change Stream Probe` section; create the file if missing).

    The script must be read-only: NO inserts, updates, deletes. Just `watch()`.

    To run: Operator executes `railway run --service <admin-service> npx tsx scripts/change-stream-probe.ts` (or uses a local .env with the MongoDB URI copied from Railway), then in a separate browser tab sends 3 messages as Sebastian in LibreChat.

    If Railway's CLI cannot inject the correct MONGODB_URI, the operator can fetch it once via `railway variables --json --service <admin>` and export it locally.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/scripts/change-stream-probe.ts && grep -q "\.watch(" /data/home/KidAI/scripts/change-stream-probe.ts</automated>
  </verify>
  <done>Script exists, has been executed against Railway MongoDB, and the outcome (SUCCESS, TIMEOUT, or ERROR) is recorded in 15-03-DECISIONS.md under `## Change Stream Probe`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Next.js 15 instrumentation.ts works in current build</name>
  <files>scripts/verify-instrumentation.ts, .planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md</files>
  <action>
    Confirm that Next.js 15 `instrumentation.ts` `register()` fires on `next start` in the current build setup WITHOUT committing any instrumentation changes to the main codebase.

    1. Check the project's Next.js version: `cat /data/home/KidAI/package.json | grep '"next"'`. Record it in 15-03-DECISIONS.md.
    2. Check `next.config.*` for `experimental.instrumentationHook`. In Next 15 the hook is enabled by default; in Next 14 it must be set to true. Record the finding.
    3. Create `scripts/verify-instrumentation.ts` — NOT a Next.js file, just a documentation + test-harness script that:
       - Explains in a header comment how to run the verification manually
       - Contains a step-by-step checklist the operator follows
       - Provides the exact content of a throwaway `src/instrumentation.ts` to temporarily add (with a console.log boot line like `[INSTRUMENTATION-VERIFY] register() fired at <ISO>`)
       - Provides the exact commands to build + start the app locally: `npm run build && npm start` (or `npx next start` if that's the convention)
       - Provides the success criterion: "look for the line `[INSTRUMENTATION-VERIFY] register() fired at` in stdout within 5 seconds of `next start` completing"
       - Reminds the operator to `git checkout src/instrumentation.ts` (revert) after the test so the throwaway file doesn't leak into plan 15-04
    4. The operator runs the manual verification:
       - Temporarily creates `src/instrumentation.ts` per the script's instructions
       - Runs `npm run build && npm start`
       - Observes whether `[INSTRUMENTATION-VERIFY]` appears in the terminal
       - Records outcome (FIRED / NOT-FIRED / ERROR) in 15-03-DECISIONS.md under `## Instrumentation Hook Verification`
       - Reverts (deletes) the throwaway `src/instrumentation.ts`
    5. If FIRED → Plan 15-04 can use instrumentation.ts for the change stream listener. If NOT-FIRED → Plan 15-04 must use the `/api/cron/tick` fallback (1 cron service every minute).

    Do NOT commit a permanent `src/instrumentation.ts` in this plan. That's Plan 15-04's job.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/scripts/verify-instrumentation.ts && grep -q "INSTRUMENTATION-VERIFY" /data/home/KidAI/scripts/verify-instrumentation.ts</automated>
  </verify>
  <done>scripts/verify-instrumentation.ts exists with the full checklist, operator has run the manual verification, and 15-03-DECISIONS.md records the outcome (FIRED/NOT-FIRED/ERROR) under `## Instrumentation Hook Verification`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — LibreChat native balance sidebar + decision lock</name>
  <files>.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md</files>
  <action>
    HUMAN CHECKPOINT — no code. Operator performs three verifications and locks the architectural decisions for Plan 15-04.

    What was built (context):
    - scripts/change-stream-probe.ts (run in Task 1)
    - scripts/verify-instrumentation.ts (run in Task 2)
    - 15-03-DECISIONS.md draft with probe + instrumentation outcomes

    Verifications (record each in 15-03-DECISIONS.md):

    1. **LibreChat native balance sidebar check** (VISUAL):
       a. Open https://librechat-production-bff2.up.railway.app in an incognito window
       b. Log in as Sebastian (test child)
       c. Look at the sidebar / user menu / wherever LibreChat surfaces balance info
       d. Record under `## LibreChat Balance UI` in 15-03-DECISIONS.md one of:
          - `NATIVE_TOKEN_DISPLAY_VISIBLE: <describe exactly what the kid sees — is it "Credits: 10,000,000"? "$10.00"? A progress bar? Nothing?>`
          - `NATIVE_TOKEN_DISPLAY_HIDDEN: <describe where it should have appeared>`
       e. If hidden, check LibreChat's account settings / profile / menu — some LibreChat installs require the user to enable a balance widget
       f. Screenshot the relevant area and save to `.planning/phases/15-safety-alert-extension-rate-limiting/15-03-balance-sidebar.png` (or reference "see terminal description")

    2. **Review Task 1 probe outcome** and Task 2 instrumentation outcome (both already recorded). Confirm they're present.

    3. **Lock decisions** in 15-03-DECISIONS.md under a `## Locked Decisions for Plan 15-04` section — fill in EVERY line:

       ```
       change_stream: yes | no
       instrumentation_hook: yes | no
       approach:
         - If change_stream=yes AND instrumentation_hook=yes → instrumentation.ts + mongo change stream listener (primary path per 15.1-CONTEXT.md)
         - If change_stream=yes AND instrumentation_hook=no → /api/cron/tick every 1 min, inside handler open short-lived change stream with resume-after-last-seen
         - If change_stream=no AND instrumentation_hook=yes → instrumentation.ts running a 5s setInterval that polls messages by createdAt > lastSeen
         - If change_stream=no AND instrumentation_hook=no → /api/cron/tick every 1 min polling messages by createdAt > lastSeen (simplest fallback)
       native_balance_display: visible | hidden | partial
         # If hidden, Plan 15-05 MUST decide whether to invest in a LibreChat frontend patch or defer
       reset_crons_mechanism: railway.toml | railway cron CLI | railway dashboard
         # Prefer railway.toml per user memory (Automate Railway)
       resumeToken_persistence: balance_state.resumeToken | cron_state.resumeToken | none
       ```

    4. Confirm the document contains the DECIDED path (one bolded line saying something like: **DECIDED: instrumentation.ts + mongo change stream, resumeToken stored in cron_state**).

    Resume signal: Type "approved — <DECIDED path>" to proceed to Plan 15-04, OR describe a blocker (e.g. "change streams not supported, need to rethink" — which triggers replan).

    If the change stream probe TIMED OUT and no events were seen, DO NOT proceed until the operator explicitly picks a fallback path — Plan 15-04's architecture hinges on this answer.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md && grep -q "Locked Decisions for Plan 15-04" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md && grep -qE "change_stream: (yes|no)" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md && grep -qE "approach:" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-03-DECISIONS.md</automated>
  </verify>
  <done>15-03-DECISIONS.md contains: probe outcome, instrumentation outcome, native balance observation, and a fully-filled `Locked Decisions for Plan 15-04` block with an explicit DECIDED line. Operator has approved via resume signal.</done>
</task>

</tasks>

<verification>
- scripts/change-stream-probe.ts exists and has been executed
- scripts/verify-instrumentation.ts exists and has been executed
- 15-03-DECISIONS.md contains: `## Change Stream Probe`, `## Instrumentation Hook Verification`, `## LibreChat Balance UI`, `## Locked Decisions for Plan 15-04` with all fields filled
- No `src/instrumentation.ts` committed (that's Plan 15-04's job)
- Operator has approved and chosen a DECIDED path
</verification>

<success_criteria>
- Plan 15-04 has an empirically-validated architecture instead of a guess
- If change streams don't work, Plan 15-04 already knows to use the polling fallback
- If instrumentation.ts doesn't work, Plan 15-04 already knows to use /api/cron/tick
- LibreChat native balance display status is documented so Plan 15-05 can decide whether to invest in a frontend patch
</success_criteria>

<output>
Create `.planning/phases/15-safety-alert-extension-rate-limiting/15-03-SUMMARY.md` documenting:
- The three probe outcomes (change stream, instrumentation hook, native balance UI)
- The locked path Plan 15-04 will follow
- Any LibreChat balance UI surprises (e.g. if it shows tokens but kids won't understand the number, Plan 15-05 knows to address)
</output>
</content>
</invoke>