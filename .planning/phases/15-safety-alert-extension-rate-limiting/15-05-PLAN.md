---
phase: 15-safety-alert-extension-rate-limiting
plan: "05"
type: execute
wave: 3
depends_on: [15-04]
files_modified:
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/settings/loading.tsx
  - src/app/(dashboard)/settings/settings-form.tsx
  - src/app/(dashboard)/settings/actions.ts
  - src/app/(dashboard)/settings/child-overrides-table.tsx
  - src/app/(dashboard)/users/[userId]/page.tsx
  - src/app/(dashboard)/users/[userId]/loading.tsx
  - src/app/(dashboard)/page.tsx
  - src/components/dashboard/daily-spend-overview-card.tsx
  - src/components/dashboard/usage-bars.tsx
autonomous: false
gap_closure: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "/settings global defaults tab has 4 inputs only: dailyCostCapEur, monthlyCostCapEur, bonusPackEur, weeklyBonusCapEur (plus bonusMessageTemplate textarea) — NO dailyImageLimit, NO dailyMessageLimit fields"
    - "/settings per-child overrides tab lets admin edit each child's dailyCostCapEur + monthlyCostCapEur + bonusPackEur + weeklyBonusCapEur; Save persists to MongoDB settings collection with key=child_override shape; Reload shows the saved values correctly"
    - "Per-child override persistence is verified end-to-end: edit value → save → refresh page → value is still there → delete override → refresh → global default shows"
    - "/users/{userId} usage section shows 2 progress bars only: 'Daily spend EUR / dailyCostCapEur' and 'Monthly spend EUR / monthlyCostCapEur' — plus active bonus credit line if present"
    - "Dashboard home (/) has a top-level card 'Today's spend per child' listing every non-admin child with a progress bar (currentDailyEur / dailyCostCapEur)"
    - "All new/modified routes have loading.tsx skeletons per CLAUDE.md; data-fetching sections wrapped in Suspense; form buttons use useTransition for instant pending state"
    - "Human verification confirmed: (a) kid sees balance in LibreChat sidebar OR documented workaround, (b) 70% warning fires via synthetic message in chat, (c) offer → YES → credit flow works end-to-end, (d) admin can edit per-child overrides and they take effect on next budget evaluation"
  artifacts:
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Simplified settings editor — new schema only, per-child overrides tab working"
    - path: "src/app/(dashboard)/settings/actions.ts"
      provides: "saveGlobalDefaults, saveChildOverride, deleteChildOverride — rewritten against new schema, per-child persistence BUG FIXED"
      exports: ["saveGlobalDefaults", "saveChildOverride", "deleteChildOverride"]
    - path: "src/app/(dashboard)/settings/child-overrides-table.tsx"
      provides: "Client component for per-child override editing with useTransition pending state"
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Dashboard home with new top-level 'Today's spend per child' card"
    - path: "src/components/dashboard/daily-spend-overview-card.tsx"
      provides: "Reusable card that lists all children + daily progress bars (used on dashboard home)"
    - path: "src/components/dashboard/usage-bars.tsx"
      provides: "Reusable 2-bar component (daily + monthly) used on /users/{userId}"
  key_links:
    - from: "src/app/(dashboard)/settings/actions.ts saveChildOverride"
      to: "settings collection upsert keyed by {key: 'child_override', userId}"
      via: "getMongoClient() + updateOne upsert + revalidatePath"
      pattern: "child_override"
    - from: "src/app/(dashboard)/users/[userId]/page.tsx"
      to: "src/lib/budget.ts evaluateChildState"
      via: "direct import (async in server component)"
      pattern: "evaluateChildState"
    - from: "src/components/dashboard/daily-spend-overview-card.tsx"
      to: "src/lib/budget.ts evaluateChildState per child"
      via: "Promise.all over non-admin users list"
      pattern: "evaluateChildState"
    - from: "src/app/(dashboard)/settings/settings-form.tsx"
      to: "actions.ts saveGlobalDefaults (server action)"
      via: "useTransition + form action"
      pattern: "useTransition"
---

<objective>
Admin UI rewrite + per-child override persistence fix + dashboard home spend overview. Consumes the new schema and `budget.ts` API from Plan 15-04 and ships the parent-facing visibility layer. Ends with a human verification checkpoint covering the 4 end-to-end flows that define phase 15.1 success: native balance visibility in LibreChat, 70% warning delivery, bonus offer → YES flow, and per-child override edits taking effect.

Purpose: Make the new budget architecture observable to the admin and prove the end-to-end flow works in production. Also fix the Plan 15-02 per-child overrides persistence bug at its root (new schema is simpler, so the bug becomes easier to kill).
Output: Rewritten settings page, refactored user detail usage section, new dashboard home spend overview card, human-verified end-to-end smoke test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15.1-CONTEXT.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-02-SUMMARY.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-03-SUMMARY.md
@.planning/phases/15-safety-alert-extension-rate-limiting/15-04-SUMMARY.md
@src/lib/budget.ts
@src/lib/settings.ts
@src/lib/bonus-purchases.ts
@src/lib/mongodb.ts
@src/app/(dashboard)/settings/page.tsx
@src/app/(dashboard)/settings/settings-form.tsx
@src/app/(dashboard)/settings/actions.ts
@src/app/(dashboard)/users/[userId]/page.tsx
@src/app/(dashboard)/page.tsx
@./CLAUDE.md

<interfaces>
<!-- Contracts this plan consumes from Plan 15-04 -->

```typescript
// From src/lib/budget.ts (Plan 15-04)
export async function getEffectiveBudget(userId: string, db: Db): Promise<EffectiveBudget>;
export async function evaluateChildState(userId: string, db: Db): Promise<ChildState>;
export interface ChildState {
  userId: string;
  remainingEur: number;
  dailyCapEur: number;
  dailyPctRemaining: number;      // 0..1 — used for progress bars
  monthlySpendEur: number;
  monthlyCapEur: number;
  monthlyCapExhausted: boolean;
  weeklyBonusSpentEur: number;
  weeklyBonusCapExhausted: boolean;
  hasActiveOffer: boolean;
}
export interface EffectiveBudget {
  dailyCostCapEur: number;
  monthlyCostCapEur: number;
  bonusPackEur: number;
  weeklyBonusCapEur: number;
  bonusMessageTemplate: string;
}
```

Per-child override doc shape (new, from Plan 15-04 migration):
```javascript
{
  key: "child_override",
  userId: "<mongodb user _id as string>",
  dailyCostCapEur?: 0.05,        // optional; falls back to global
  monthlyCostCapEur?: 1.00,
  bonusPackEur?: 0.10,
  weeklyBonusCapEur?: 0.25,
}
```
Global defaults:
```javascript
{
  key: "global_defaults",
  dailyCostCapEur: 0.10,
  monthlyCostCapEur: 2.00,
  bonusPackEur: 0.20,
  weeklyBonusCapEur: 0.50,
  bonusMessageTemplate: "You've reached your limit. Type YES to unlock extra usage."
}
```

Known bug from Plan 15-02 being fixed here:
> "Per-Child Overrides tab broken — does not persist or apply overrides correctly. Root cause: likely a server action / form data wiring issue in settings-form.tsx + actions.ts"

Fix approach: Rewrite the overrides tab as a dedicated `<ChildOverridesTable>` client component where each row is its own small form bound to `saveChildOverride(userId, formData)` via `useTransition`. Each row has its own save button. Global save button saves global defaults. Separate forms = no shared state bugs.

Dashboard home card shape (new):
```
┌───────────────────────────────────────────────┐
│ Today's spend per child                       │
│                                                │
│ Sebastian      [████████░░] €0.08 / €0.10     │
│ Penelope       [███░░░░░░░] €0.03 / €0.10     │
│                                                │
│ [Edit limits →]                                │
└───────────────────────────────────────────────┘
```

Shadcn components (verify installed, add if missing): Card, Progress, Table, Input, Label, Button, Tabs, Textarea, Skeleton.
```bash
ls src/components/ui/
# If progress.tsx missing: npx shadcn@latest add progress
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /settings page against new schema — global tab + working per-child overrides tab</name>
  <files>src/app/(dashboard)/settings/page.tsx, src/app/(dashboard)/settings/loading.tsx, src/app/(dashboard)/settings/settings-form.tsx, src/app/(dashboard)/settings/actions.ts, src/app/(dashboard)/settings/child-overrides-table.tsx</files>
  <action>
    Rewrite `/settings` to use the new schema from Plan 15-04 and fix the per-child override persistence bug.

    1. `src/app/(dashboard)/settings/actions.ts` — REWRITE:
       - `"use server"` directive at top
       - `saveGlobalDefaults(formData: FormData)`:
         - Parse dailyCostCapEur, monthlyCostCapEur, bonusPackEur, weeklyBonusCapEur (all parseFloat, validate > 0), bonusMessageTemplate (string, max 500 chars)
         - Upsert `settings` collection doc `{ key: "global_defaults" }` via `updateOne({ key: "global_defaults" }, { $set: {...} }, { upsert: true })`
         - `revalidatePath("/settings")` and `revalidatePath("/")` (dashboard home reads limits for the overview card)
         - Return `{ ok: true }` on success, `{ ok: false, error: string }` on failure
       - `saveChildOverride(userId: string, formData: FormData)`:
         - CRITICAL: take userId as a SEPARATE parameter, not from formData (avoids the known wiring bug)
         - Parse each optional field. If a field is empty string → exclude it (override falls back to global). If a field is numeric → include it.
         - Upsert `settings` collection doc `{ key: "child_override", userId }` with ONLY the set fields via `$set` and `$unset` for cleared fields
         - revalidatePath("/settings") and "/users/[userId]" and "/"
         - Return `{ ok: true, userId }` so the client can show a per-row toast
       - `deleteChildOverride(userId: string)`:
         - Deletes the doc `{ key: "child_override", userId }` entirely
         - Returns { ok: true }
       - Write unit-level smoke: document how to test manually since actions.ts is server-only

    2. `src/app/(dashboard)/settings/page.tsx` — REWRITE:
       - Server component
       - Fetch: global_defaults doc (or HARDCODED_DEFAULTS), all child_override docs, all non-admin users from `users` collection
       - Pass globals + users + overrides array to a `<SettingsForm>` wrapper (client component)
       - Wrap in Suspense with loading.tsx skeleton
       - Add a "Last updated" timestamp from the global doc

    3. `src/app/(dashboard)/settings/settings-form.tsx` — REWRITE:
       - `"use client"` directive
       - Renders shadcn `<Tabs>` with "Global Defaults" + "Per-Child Overrides"
       - Global tab: one form with 4 number inputs + 1 textarea + Save button, uses `useTransition`, invokes saveGlobalDefaults, shows sonner toast
       - Per-Child tab: renders `<ChildOverridesTable users={users} globals={globals} overrides={overrides} />`
       - Shows "Last saved: <timestamp>" feedback after successful saves

    4. `src/app/(dashboard)/settings/child-overrides-table.tsx` — NEW, the bug fix:
       - `"use client"` directive
       - Accepts `users`, `globals`, `overrides` props
       - Renders a `<Table>` with columns: Child name, Daily cap (€), Monthly cap (€), Bonus pack (€), Weekly bonus cap (€), Actions
       - Each row is an INDEPENDENT `<form>` with its own useTransition state, bound to `saveChildOverride.bind(null, user._id.toString())` (server action closure pattern — no FormData userId field, eliminates wiring bug)
       - Each input's placeholder shows the global default value; empty input means "use global"
       - Each row has: Save button (save this row's overrides) + Delete button (clear all overrides for this child → deleteChildOverride)
       - Per-row toast on save success/failure via sonner
       - Per-row pending state (disabled + spinner) via useTransition while saving
       - Input values are controlled from a local state initialized from the override prop, so after save + revalidate the row shows the saved value (not empty)

    5. `src/app/(dashboard)/settings/loading.tsx` — UPDATE to match new shape: Tabs header skeleton + 4 number input skeletons + textarea skeleton + table of 2-3 skeleton rows for per-child tab.

    6. Verify `npx shadcn@latest add progress tabs textarea` — only add if missing. Commit any new shadcn files.

    Anti-wiring-bug checklist (must verify before marking done):
    - [ ] saveChildOverride takes userId as a function parameter, NEVER from formData
    - [ ] Each row is a separate `<form>` element (not one big form with rowId fields)
    - [ ] Row state is initialized from the override prop, not empty
    - [ ] After save, revalidatePath runs so server component re-reads and props update
    - [ ] If override is empty (all fields null), row still renders with placeholders

    Run `npm run build` — must succeed before commit.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm run build && test -f src/app/\(dashboard\)/settings/child-overrides-table.tsx && grep -q "saveChildOverride" src/app/\(dashboard\)/settings/actions.ts && grep -q "dailyCostCapEur" src/app/\(dashboard\)/settings/actions.ts && ! grep -q "dailyImageLimit" src/app/\(dashboard\)/settings/page.tsx</automated>
  </verify>
  <done>Settings page rewritten against new schema; per-child overrides tab uses independent row forms with useTransition; saveChildOverride takes userId as closure parameter; no dailyImageLimit/dailyMessageLimit references remain in /settings code; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor /users/{userId} usage section to 2 bars + add dashboard home spend overview card</name>
  <files>src/app/(dashboard)/users/[userId]/page.tsx, src/app/(dashboard)/users/[userId]/loading.tsx, src/app/(dashboard)/page.tsx, src/components/dashboard/daily-spend-overview-card.tsx, src/components/dashboard/usage-bars.tsx</files>
  <action>
    1. `src/components/dashboard/usage-bars.tsx` — NEW:
       - Server component accepting `userId: string` prop (and optional `childName` for display)
       - Internally awaits `evaluateChildState(userId, db)` from budget.ts
       - Renders exactly 2 shadcn `<Progress>` bars:
         - "Today: €X / €Y" with progress = dailySpentEur / dailyCapEur (note: use `1 - dailyPctRemaining` since ChildState exposes pct remaining)
         - "This month: €X / €Y" with progress = monthlySpendEur / monthlyCapEur
       - Below the bars, a small line: "Active bonus credit: €X" (only if > 0) and "Bonus purchases this week: N (€X)"
       - Color coding: green if <70%, amber if 70-100%, red if ≥100% or hasActiveOffer
       - NO image count / message count bars — those columns are gone

    2. `src/app/(dashboard)/users/[userId]/page.tsx` — UPDATE:
       - REMOVE the 5 old usage bars (image count, message count, monthly spend, active bonus, weekly bonus)
       - REPLACE with `<Suspense fallback={<UsageBarsSkeleton />}><UsageBars userId={params.userId} childName={user.name} /></Suspense>`
       - Keep the rest of the page intact (email, role, conversations list, etc.)
       - Remove unused imports (getImageCountToday, getDailyMessageCount, getMonthlySpendEUR, getActiveBonusCredit, getWeeklyBonusSpend — some of these are gone with cost-ledger.ts deletion in Plan 15-04; use budget.evaluateChildState exclusively)

    3. `src/app/(dashboard)/users/[userId]/loading.tsx` — UPDATE to match the new 2-bar shape.

    4. `src/components/dashboard/daily-spend-overview-card.tsx` — NEW:
       - Server component with no props (fetches its own data)
       - Fetches all non-admin users from `users` collection
       - For each user, awaits `evaluateChildState(userId, db)` (Promise.all for parallelism)
       - Renders a shadcn `<Card>` with:
         - Header: "Today's spend per child"
         - Body: a table / list with one row per child: `{name}  {Progress bar}  €{dailySpent} / €{dailyCap}`
         - Footer: a link to /settings via next/link with "Edit limits →"
       - Handles edge case: zero non-admin users → renders "No children yet" empty state

    5. `src/app/(dashboard)/page.tsx` — UPDATE dashboard home:
       - Add `<Suspense fallback={<DailySpendOverviewSkeleton />}><DailySpendOverviewCard /></Suspense>` as the TOP card (above existing trust-center content)
       - Do not break existing Phase 7 trust home content; just prepend the new card
       - Skeleton matches the card shape (2-3 rows of progress bar placeholders)

    6. Verify `next/link` prefetch is enabled by default on both the "Edit limits" link and any child name links (do not disable prefetch).

    Run `npm run build` — must succeed.
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npm run build && test -f src/components/dashboard/daily-spend-overview-card.tsx && test -f src/components/dashboard/usage-bars.tsx && grep -q "evaluateChildState" src/components/dashboard/usage-bars.tsx && grep -q "DailySpendOverviewCard" src/app/\(dashboard\)/page.tsx</automated>
  </verify>
  <done>Dashboard home shows "Today's spend per child" top card listing every child. /users/{userId} shows 2-bar usage (daily + monthly) via evaluateChildState. Both routes have matching skeleton loading states. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy to Railway + smoke test the admin UI</name>
  <files>.planning/phases/15-safety-alert-extension-rate-limiting/15-05-DEPLOYMENT.md</files>
  <action>
    Deploy the admin dashboard (with the new UI) to Railway. Use `railway up` for a fresh source build (per Plan 15-02 lesson: serviceInstanceRedeploy reuses cached image).

    1. `railway up --service <admin-service>` — fresh build
    2. Wait for deploy to complete; tail Railway logs to confirm no runtime errors on boot
    3. Visit https://kidschat-admin-production.up.railway.app in an incognito window; confirm:
       - Dashboard home loads with "Today's spend per child" card visible
       - /settings loads with 2 tabs (Global Defaults + Per-Child Overrides)
       - /users/{sebastian_id} loads with the 2-bar usage section
    4. Browser smoke test (record each in 15-05-DEPLOYMENT.md):
       - Edit global dailyCostCapEur from 0.10 to 0.12 → save → refresh → confirm value stuck
       - Edit Sebastian's per-child dailyCostCapEur to 0.05 → save → refresh → confirm value stuck
       - Delete Sebastian's override → refresh → confirm row shows 0.12 (global default) as placeholder
       - Revert global back to 0.10 for the human verification step
    5. Document in 15-05-DEPLOYMENT.md:
       - Deploy commit SHA
       - Screenshots or described observations of the 3 key pages
       - Smoke test outcomes

    No cron changes in this task — those were done in Plan 15-04.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-05-DEPLOYMENT.md</automated>
  </verify>
  <done>Admin dashboard redeployed with Plan 15-05 changes; DEPLOYMENT.md documents 3 pages loaded correctly and the per-child override persistence smoke test passed end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end human verification — 4 flows that define phase 15.1 success</name>
  <files>.planning/phases/15-safety-alert-extension-rate-limiting/15-05-SMOKE-TEST.md</files>
  <action>
    HUMAN CHECKPOINT — no code. Operator executes the four end-to-end flows that define phase 15.1 success and records results in 15-05-SMOKE-TEST.md.

    What was built (context):
    - Phase 15 original: safety patterns, legacy 5-cron pipeline (now deleted)
    - Plan 15-03: risk-killed change stream + instrumentation + native balance UI assumptions
    - Plan 15-04: new budget.ts, new schema, change stream listener (or /api/cron/tick fallback), 2 reset crons in railway.toml, migration run
    - Plan 15-05 (this plan): rewritten /settings, 2-bar /users/{userId}, new dashboard home spend overview card

    **Flow 1 — Kid sees balance in LibreChat sidebar (depends on 15-03 native balance UI finding):**
    a. Open LibreChat as Sebastian in incognito
    b. Look at the sidebar / user menu
    c. Expected: kid sees some form of balance indicator (token count, progress bar, or €-value — whatever LibreChat renders natively with balance.enabled)
    d. Record in SMOKE-TEST.md: exact visual observation
    e. If nothing is visible → this is a KNOWN DEFERRED item per 15-03; document as "deferred to future phase, not a failure of 15.1"

    **Flow 2 — 70% warning fires via synthetic message:**
    a. Via /settings (admin), set Sebastian's override dailyCostCapEur to 0.01 (very low so kid will hit 70% fast)
    b. Run the migration or manually trigger daily-reset to re-top-up: `curl -X POST .../api/cron/daily-reset -H "x-cron-secret: $CRON_SECRET"`
    c. Verify `balances.tokenCredits` is now eurToTokens(0.01) ≈ ~10_869
    d. As Sebastian, send 2-3 moderate-length messages in LibreChat
    e. Expected: within a few seconds (Path A, change stream) or within 1 minute (Path B, tick cron), a synthetic warning message appears in the chat: "You've used 70% of your daily chat budget..."
    f. Verify `balance_state.warnedAt70PctOn` is today's date (via mongo-inspect or admin dashboard)
    g. Send another 2 messages → verify no duplicate warning fires (idempotent)

    **Flow 3 — Bonus offer → YES → credit flow:**
    a. Continue from Flow 2; keep sending messages until tokenCredits hits 0
    b. Expected: a bonus offer synthetic message appears in the chat: "You've reached your limit. Type YES to unlock extra usage. (€0.20 bonus pack)"
    c. Verify `balance_state.activeOfferMessageId` is set and `activeOfferExpiresAt` is +5 min from now
    d. As Sebastian, type "YES" and send
    e. Expected: within a few seconds (Path A) or 1 minute (Path B), bonus is credited
    f. Verify `bonus_purchases` collection has a new row with packSizeEUR=0.20 for Sebastian
    g. Verify `balances.tokenCredits` increased by eurToTokens(0.20)
    h. Verify `balance_state.activeOfferMessageId` is null (cleared)
    i. Send another message as Sebastian — should succeed (balance positive)

    **Flow 4 — Admin per-child override edit takes effect:**
    a. Via /settings Per-Child Overrides tab, change Sebastian's dailyCostCapEur from 0.01 back to 0.10
    b. Save → refresh the page → confirm the value is STILL 0.10 (persistence working, Plan 15-02 bug FIXED)
    c. Visit /users/{sebastian_id} → confirm the "Today: €X / €0.10" bar shows the new cap
    d. Trigger daily-reset: `curl -X POST .../api/cron/daily-reset -H "x-cron-secret: $CRON_SECRET"`
    e. Verify balances.tokenCredits ≈ eurToTokens(0.10) ≈ 108_695
    f. Visit dashboard home / → confirm Sebastian's row in "Today's spend per child" shows the new cap

    **Flow 5 — Perceived performance (CLAUDE.md compliance):**
    a. Visit /settings → confirm skeleton appears instantly (no white flash)
    b. Visit / → confirm the spend overview card has its own Suspense skeleton before data loads
    c. Click Save on global defaults → confirm button shows pending state immediately
    d. Click Save on a per-child row → confirm only that row shows pending state (other rows unaffected)

    Cleanup: Reset Sebastian's overrides and global settings to reasonable production values. Delete test bonus_purchases records if desired.

    Record each flow's outcome in SMOKE-TEST.md as a table:
    | Flow | Status | Notes |
    |------|--------|-------|
    | 1. Kid sees balance | pass/fail/deferred | ... |
    | 2. 70% warning | pass/fail | ... |
    | 3. Bonus offer → YES → credit | pass/fail | ... |
    | 4. Per-child override edit | pass/fail | ... |
    | 5. Perceived performance | pass/fail | ... |

    Resume signal: Type "approved" if all flows pass (or Flow 1 is documented deferred), or describe failures step-by-step.
  </action>
  <verify>
    <automated>test -f /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-05-SMOKE-TEST.md && grep -qiE "pass|approved|fail|deferred" /data/home/KidAI/.planning/phases/15-safety-alert-extension-rate-limiting/15-05-SMOKE-TEST.md</automated>
  </verify>
  <done>All 5 flows recorded in SMOKE-TEST.md. Flows 2, 3, 4, 5 PASS. Flow 1 either passes or is documented as deferred per 15-03 findings. Operator has approved via resume signal.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds after Task 1 and Task 2
- /settings uses only new schema fields (dailyCostCapEur, monthlyCostCapEur, bonusPackEur, weeklyBonusCapEur, bonusMessageTemplate)
- No dailyImageLimit or dailyMessageLimit references remain in /settings or /users/[userId] code
- saveChildOverride accepts userId as a closure parameter, not from FormData
- Each per-child override row is an independent <form> with its own useTransition
- Dashboard home has a "Today's spend per child" card at the top, above existing trust content
- /users/{userId} shows 2 progress bars (daily + monthly) via evaluateChildState
- All new/modified routes have loading.tsx skeletons and Suspense boundaries per CLAUDE.md
- Human smoke test has recorded pass/fail/deferred for all 5 flows
- Per-child override persistence bug from Plan 15-02 is verified FIXED in Flow 4
</verification>

<success_criteria>
- Per-child overrides persist end-to-end (plan 15-02 bug fixed)
- Admin sees daily spend for every child at a glance on the dashboard home
- Admin sees 2-bar usage on /users/{userId} sourced from budget.evaluateChildState
- Kid sees balance natively in LibreChat (or Flow 1 is documented deferred with a clear path forward)
- 70% warning fires in-chat exactly once per day per child via synthetic message
- Bonus offer → YES → credit flow works end-to-end in production
- All routes comply with CLAUDE.md perceived performance rules (loading.tsx, Suspense, useTransition)
</success_criteria>

<output>
Create `.planning/phases/15-safety-alert-extension-rate-limiting/15-05-SUMMARY.md` documenting:
- Final settings page architecture (independent row forms, server action closure pattern)
- Root cause of Plan 15-02 per-child override bug and how this plan fixed it
- Dashboard home layout changes (new card at top)
- /users/{userId} migration from 5-bar to 2-bar usage
- Smoke test outcomes for all 5 flows
- Any deferred items discovered during human verification
- Phase 15.1 closing statement: which of the original 15.1-CONTEXT.md goals are now shipped, and which (if any) are genuinely deferred to a future phase
</output>
