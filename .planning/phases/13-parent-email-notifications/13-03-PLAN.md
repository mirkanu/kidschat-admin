---
phase: 13-parent-email-notifications
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/app/(dashboard)/notifications/page.tsx
  - src/app/(dashboard)/notifications/loading.tsx
  - src/app/api/notify/history/route.ts
  - src/app/api/users/[id]/route.ts
  - src/app/(dashboard)/users/page.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: false
requirements:
  - NOTIFY-04
  - NOTIFY-05
must_haves:
  truths:
    - "Admin can view a table of all sent email notifications with type, recipient, date, and status"
    - "Admin can toggle safety alert and weekly digest email preferences per user"
    - "Notification preferences default to enabled when not explicitly set"
    - "Notifications page is accessible from the sidebar navigation"
  artifacts:
    - path: "src/app/(dashboard)/notifications/page.tsx"
      provides: "Admin page showing sent notification history"
    - path: "src/app/(dashboard)/notifications/loading.tsx"
      provides: "Skeleton loading state for notifications page"
    - path: "src/app/api/notify/history/route.ts"
      provides: "GET endpoint for notification history"
      exports: ["GET"]
    - path: "src/app/(dashboard)/users/page.tsx"
      provides: "Updated users page with notification preference toggles"
  key_links:
    - from: "src/app/(dashboard)/notifications/page.tsx"
      to: "email_notifications MongoDB collection"
      via: "Direct MongoDB query in server component"
      pattern: "email_notifications.*find"
    - from: "src/app/(dashboard)/users/page.tsx"
      to: "users collection notification_prefs"
      via: "MongoDB updateOne on notification_prefs field"
      pattern: "notification_prefs"
---

<objective>
Build the admin notification history page, add notification preference toggles to the users page, and add sidebar navigation.

Purpose: Parents can review what emails have been sent and control their notification preferences, completing the email notification feature set.
Output: Notification history UI, preference controls, sidebar link.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-parent-email-notifications/13-RESEARCH.md
@.planning/phases/13-parent-email-notifications/13-01-SUMMARY.md
@.planning/phases/13-parent-email-notifications/13-02-SUMMARY.md

@src/app/(dashboard)/users/page.tsx
@src/app/(dashboard)/layout.tsx
@src/app/(dashboard)/alerts/page.tsx

<interfaces>
<!-- From Plan 01 + Plan 02: email_notifications MongoDB collection schema -->
<!--
{
  _id: ObjectId,
  type: "safety_alert" | "weekly_digest",
  sentAt: Date,
  to: string[],
  childName?: string,
  weekOf?: string,
  resendId?: string,
  meta: Record<string, unknown>
}
-->

<!-- From Plan 02: notification_prefs field on users collection -->
<!--
users.notification_prefs = {
  safetyAlerts: boolean,  // default: true
  weeklyDigest: boolean   // default: true
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build notification history page and API</name>
  <files>src/app/(dashboard)/notifications/page.tsx, src/app/(dashboard)/notifications/loading.tsx, src/app/api/notify/history/route.ts, src/app/(dashboard)/layout.tsx</files>
  <action>
    1. Create `src/app/api/notify/history/route.ts` — GET endpoint:
       - Auth check with session
       - Connect to MongoDB `test` db
       - Query `email_notifications` collection: sort by `sentAt` desc, limit 100
       - Return array of notification records with `_id` serialized as string

    2. Create `src/app/(dashboard)/notifications/loading.tsx`:
       - Skeleton matching table layout: heading skeleton, 8 row skeletons with 4 columns each
       - Use shadcn Skeleton component

    3. Create `src/app/(dashboard)/notifications/page.tsx`:
       - Server component (following project pattern — direct MongoDB query, NOT fetch to own API)
       - Auth check, redirect to /login if not authenticated
       - Query `email_notifications` directly from MongoDB: sort by sentAt desc, limit 100
       - Render page heading "Email Notifications" with description
       - Summary badges: total count, safety alerts count, weekly digests count
       - Table with columns: Type (badge — red for safety_alert, blue for weekly_digest), Recipient(s), Child/Subject, Date (formatted), Resend ID (truncated)
       - Handle empty state: "No notifications sent yet" message
       - Use shadcn Table, Badge, Card components
       - Wrap data section in Suspense with skeleton fallback

    4. Update `src/app/(dashboard)/layout.tsx`:
       - Add "Notifications" link to sidebar navigation with Bell icon from lucide-react
       - Place it after Alerts in the nav order
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Notification history page shows sent emails in a table, accessible from sidebar, loading skeleton renders during data fetch</done>
</task>

<task type="auto">
  <name>Task 2: Add notification preference toggles to users page</name>
  <files>src/app/(dashboard)/users/page.tsx, src/app/api/users/[id]/route.ts</files>
  <action>
    1. Check if `src/app/api/users/[id]/route.ts` exists. If it does, add a PATCH handler for updating notification_prefs. If not, create it:
       - PATCH handler: Auth check, parse body for `notification_prefs: { safetyAlerts?: boolean, weeklyDigest?: boolean }`
       - Update user document: `db.collection("users").updateOne({ _id: new ObjectId(id) }, { $set: { notification_prefs: body.notification_prefs } })`
       - Return updated user

    2. Update `src/app/(dashboard)/users/page.tsx`:
       - For each ADMIN user row, add two small toggle switches (or checkboxes) for "Safety Alerts" and "Weekly Digest"
       - Default both to checked/enabled when `notification_prefs` field is missing (treat undefined as enabled)
       - On toggle change, call PATCH `/api/users/{id}` with updated `notification_prefs`
       - Show optimistic update — toggle immediately, revert on error with toast
       - Use shadcn Switch component if available, otherwise a styled checkbox
       - Only show notification prefs for ADMIN-role users (children don't receive emails)
       - If the users page is a server component, extract the toggle row into a small client component `NotificationPrefsToggle` that handles the PATCH call
  </action>
  <verify>
    <automated>cd /data/home/KidAI && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Admin users page shows notification preference toggles for ADMIN users, toggles persist to MongoDB via PATCH endpoint</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete email notification system</name>
  <files>N/A</files>
  <action>
    Human verifies the complete email notification system: safety alert emails, weekly digest emails, notification history page, and preference toggles.
  </action>
  <verify>Human confirms all features work correctly</verify>
  <done>All notification features verified working end-to-end</done>
  <what-built>Complete email notification system: safety alert emails, weekly digest emails, notification history page, and preference toggles</what-built>
  <how-to-verify>
    1. Visit the admin dashboard and navigate to the new "Notifications" page in the sidebar
    2. Verify the page loads with a table (may be empty if no emails sent yet)
    3. Navigate to the Users page — ADMIN users should show Safety Alerts and Weekly Digest toggles
    4. Toggle a preference off and refresh — it should persist
    5. Visit the Alerts page — if there are safety events, check Railway logs or Resend dashboard for sent emails (requires RESEND_API_KEY to be configured)
    6. To test weekly digest manually: `curl -X POST https://kidschat-admin-production.up.railway.app/api/notify/weekly-digest -H "x-cron-secret: YOUR_SECRET" -H "Content-Type: application/json" -d '{"trigger":"manual"}'`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` — clean build with no errors
2. Notifications page renders with table layout
3. Users page shows preference toggles for ADMIN users
4. Sidebar has Notifications link with Bell icon
5. PATCH /api/users/:id updates notification_prefs in MongoDB
</verification>

<success_criteria>
- Notification history page shows all sent emails in reverse chronological order
- Notification preference toggles work with optimistic UI updates
- Preferences default to enabled when not set
- Sidebar navigation includes Notifications link
- Build passes cleanly
- Human verification confirms full flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/13-parent-email-notifications/13-03-SUMMARY.md`
</output>
